components:
  schemas:
    APIReference:
      description: |-
        Identifies a target API — the strict (all-required) variant.

        Used in responses and as the canonical identity tuple.
      properties:
        name:
          title: Name
          type: string
        vendor:
          title: Vendor
          type: string
        version:
          title: Version
          type: string
      required:
      - vendor
      - name
      - version
      title: APIReference
      type: object
    APIReferenceRequest:
      description: Relaxed variant for request bodies where partial identification is allowed.
      properties:
        catalog_api_id:
          anyOf:
          - maxLength: 255
            type: string
          - type: 'null'
          title: Catalog Api Id
        name:
          default: ''
          title: Name
          type: string
        vendor:
          title: Vendor
          type: string
        version:
          default: ''
          title: Version
          type: string
      required:
      - vendor
      title: APIReferenceRequest
      type: object
    AccessRequestFileRequest:
      description: Request body for filing an access request.
      properties:
        items:
          items:
            $ref: '#/components/schemas/AccessRequestItemRequest'
          minItems: 1
          title: Items
          type: array
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
      required:
      - items
      title: AccessRequestFileRequest
      type: object
    AccessRequestItemRequest:
      description: |-
        A single line-item in a file request.

        **Permission rules:** Rules control which upstream API operations the broker
        allows through a direct agent↔credential binding. They are enforced per
        (agent, credential) pair, so they can only be attached to credential:bind
        items — not scope:grant. Include them (or a shared ``rule_set_id``)
        directly on the credential:bind item when filing the access request, and
        the approver's decision persists them on the binding.
      properties:
        action:
          enum:
          - bind
          - grant
          - provision
          title: Action
          type: string
        resource_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Resource Id
        resource_reference:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Resource Reference
        resource_type:
          enum:
          - credential
          - scope
          title: Resource Type
          type: string
        rule_set_id:
          anyOf:
          - type: string
          - type: 'null'
          description: Shared permission rule set for the binding (credential:bind only), as an alternative to inline rules. While attached, the set's ordered list is the binding's effective policy.
          title: Rule Set Id
        rules:
          anyOf:
          - items:
              $ref: '#/components/schemas/jentic_one__control__web__schemas__access_requests__PermissionRuleSchema'
            type: array
          - type: 'null'
          description: 'Permission rules for the binding (credential:bind only). Rules are evaluated first-match-wins by the broker; if no rule matches, the request is denied. Example: [{"effect": "allow", "path": ".*"}].'
          title: Rules
      required:
      - resource_type
      - action
      title: AccessRequestItemRequest
      type: object
    AccessRequestItemResponse:
      description: Response model for a single access-request line item.
      properties:
        action:
          title: Action
          type: string
        already_satisfied:
          anyOf:
          - type: boolean
          - type: 'null'
          description: Whether this item's outcome is already in effect (the binding or grant it asks for already exists), letting a reviewer approve manually-fulfilled work instead of re-doing it in the wizard. Populated on single-request GETs for pending credential:bind and scope:grant items; null when not computed (list endpoints, decided items, fulfilment-only intents, an item whose target cannot be determined, an ambiguous credential reference — which approval would refuse as filed — or a credential:bind whose credential is not visible to the caller). API REFERENCES are resolved under the caller's visibility, mirroring decide-time resolution, so False can also mean 'satisfied by a credential this caller cannot see'; explicit-id targets are probed directly.
          title: Already Satisfied
        already_satisfied_by:
          anyOf:
          - type: string
          - type: 'null'
          description: For a satisfied credential:bind, the id of the credential the agent is already bound to — names the exact object so consumers can point the operator at it. Null for other item types and whenever already_satisfied is not true.
          title: Already Satisfied By
        applied_effects:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Applied Effects
        credential_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Name
        decided_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Decided At
        decided_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Decided By
        decision_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Decision Reason
        id:
          title: Id
          type: string
        resource_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Resource Id
        resource_reference:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Resource Reference
        resource_type:
          title: Resource Type
          type: string
        rule_set_id:
          anyOf:
          - type: string
          - type: 'null'
          description: Shared permission rule set attached to a credential:bind item, as an alternative policy carrier to inline rules.
          title: Rule Set Id
        rules:
          anyOf:
          - items:
              additionalProperties: true
              type: object
            type: array
          - type: 'null'
          title: Rules
        status:
          title: Status
          type: string
        to_id:
          anyOf:
          - type: string
          - type: 'null'
          title: To Id
        to_type:
          anyOf:
          - type: string
          - type: 'null'
          title: To Type
        toolkit_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Toolkit Name
      required:
      - id
      - resource_type
      - action
      - status
      title: AccessRequestItemResponse
      type: object
    AccessRequestListResponse:
      description: Paginated list of access requests.
      properties:
        data:
          items:
            $ref: '#/components/schemas/AccessRequestResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: AccessRequestListResponse
      type: object
    AccessRequestOwnerResponse:
      description: |-
        Display info for the filer's human owner (labelling only, not authorization).

        Server-resolved from ``filer_owner_id`` (falling back to ``created_by``
        when the former is null, mirroring what consumers render) so they don't
        need ``users:read`` (or a roster fetch) just to label a row. Absent when
        the id doesn't resolve to a user (service-account filers, purged rows) or
        on mutation responses, which skip the enrichment.
      properties:
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          description: The owner's full name, when set on the profile.
          title: Display Name
        email:
          description: The owner's email address.
          title: Email
          type: string
        id:
          description: The resolved owner's user id (filer_owner_id, or created_by when null).
          title: Id
          type: string
      required:
      - id
      - email
      title: AccessRequestOwnerResponse
      type: object
    AccessRequestResponse:
      description: Response model for an access request envelope.
      properties:
        actor_id:
          title: Actor Id
          type: string
        approve_url:
          title: Approve Url
          type: string
        created_by:
          title: Created By
          type: string
        evaluation:
          anyOf:
          - $ref: '#/components/schemas/EvaluationResponse'
          - type: 'null'
        expires_at:
          format: date-time
          title: Expires At
          type: string
        filed_at:
          format: date-time
          title: Filed At
          type: string
        filer_owner:
          anyOf:
          - $ref: '#/components/schemas/AccessRequestOwnerResponse'
          - type: 'null'
        filer_owner_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Filer Owner Id
        id:
          title: Id
          type: string
        items:
          items:
            $ref: '#/components/schemas/AccessRequestItemResponse'
          title: Items
          type: array
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
        requested_by:
          title: Requested By
          type: string
        status:
          title: Status
          type: string
      required:
      - id
      - actor_id
      - requested_by
      - status
      - approve_url
      - filed_at
      - expires_at
      - created_by
      - items
      title: AccessRequestResponse
      type: object
    ActorListResponse:
      description: Paginated list of actors.
      properties:
        data:
          items:
            $ref: '#/components/schemas/ActorSummaryResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: ActorListResponse
      type: object
    ActorSummaryResponse:
      description: Single actor entry in the actors list.
      properties:
        active:
          title: Active
          type: boolean
        actor_type:
          $ref: '#/components/schemas/ActorType'
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          title: Id
          type: string
        name:
          title: Name
          type: string
      required:
      - id
      - actor_type
      - name
      - active
      - created_at
      title: ActorSummaryResponse
      type: object
    ActorType:
      description: |-
        Type of authenticated actor.

        ``toolkit`` is retired (theme-5 Phase 4): toolkit keys resolve as the
        service accounts the key-retirement job created, so no code path mints a
        toolkit identity. Persisted ``actor_type='toolkit'`` strings survive in
        historical rows (events, audit entries, execution records) until the
        Phase-6b scope-data sweep; read paths must tolerate the string without
        round-tripping it through this enum.
      enum:
      - user
      - agent
      - service_account
      title: ActorType
      type: string
    AgentCreateRequest:
      description: Request body for creating an agent manually.
      properties:
        description:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          title: Description
        name:
          maxLength: 255
          minLength: 1
          title: Name
          type: string
        scopes:
          anyOf:
          - items:
              maxLength: 64
              minLength: 1
              pattern: ^[a-zA-Z0-9_:./-]+$
              type: string
            maxItems: 100
            type: array
          - type: 'null'
          title: Scopes
      required:
      - name
      title: AgentCreateRequest
      type: object
    AgentListResponse:
      description: List of agents.
      properties:
        data:
          items:
            $ref: '#/components/schemas/AgentResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: AgentListResponse
      type: object
    AgentPatchRequest:
      description: Request body for partially updating an agent.
      properties:
        description:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          title: Description
        name:
          anyOf:
          - maxLength: 255
            minLength: 1
            type: string
          - type: 'null'
          title: Name
        owner_id:
          anyOf:
          - maxLength: 255
            minLength: 1
            type: string
          - type: 'null'
          title: Owner Id
      title: AgentPatchRequest
      type: object
    AgentResponse:
      description: Agent representation in API responses.
      properties:
        approved_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Approved At
        approved_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Approved By
        created_at:
          format: date-time
          title: Created At
          type: string
        denial_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Denial Reason
        denied_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Denied By
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        has_api_key:
          default: false
          title: Has Api Key
          type: boolean
        id:
          title: Id
          type: string
        name:
          title: Name
          type: string
        owner_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Owner Id
        parent_agent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Parent Agent Id
        registered_by:
          title: Registered By
          type: string
        status:
          title: Status
          type: string
      required:
      - id
      - name
      - registered_by
      - status
      - created_at
      title: AgentResponse
      type: object
    AgentScopesRequest:
      description: Request body for replacing an agent's scopes.
      properties:
        scopes:
          items:
            maxLength: 64
            minLength: 1
            pattern: ^[a-zA-Z0-9_:./-]+$
            type: string
          maxItems: 100
          title: Scopes
          type: array
      required:
      - scopes
      title: AgentScopesRequest
      type: object
    AgentScopesResponse:
      description: Response containing an agent's current scopes.
      properties:
        scopes:
          items:
            type: string
          title: Scopes
          type: array
      required:
      - scopes
      title: AgentScopesResponse
      type: object
    AmendItemSchema:
      description: A single item amendment.
      properties:
        item_id:
          title: Item Id
          type: string
        resource_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Resource Id
        rule_set_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Rule Set Id
        rules:
          anyOf:
          - items:
              $ref: '#/components/schemas/jentic_one__control__web__schemas__access_requests__PermissionRuleSchema'
            type: array
          - type: 'null'
          title: Rules
      required:
      - item_id
      title: AmendItemSchema
      type: object
    AmendRequest:
      description: Request body for the :amend verb.
      properties:
        items:
          items:
            $ref: '#/components/schemas/AmendItemSchema'
          minItems: 1
          title: Items
          type: array
      required:
      - items
      title: AmendRequest
      type: object
    ApiImportLinksResponse:
      description: Hypermedia links for an import response.
      properties:
        self:
          title: Self
          type: string
      required:
      - self
      title: ApiImportLinksResponse
      type: object
    ApiImportRequest:
      description: Wrapper for a batch of import sources.
      properties:
        sources:
          items:
            discriminator:
              mapping:
                inline: '#/components/schemas/ApiSourceInline'
                url: '#/components/schemas/ApiSourceUrl'
              propertyName: type
            oneOf:
            - $ref: '#/components/schemas/ApiSourceUrl'
            - $ref: '#/components/schemas/ApiSourceInline'
          maxItems: 100
          minItems: 1
          title: Sources
          type: array
      required:
      - sources
      title: ApiImportRequest
      type: object
    ApiImportResponse:
      description: Acknowledgement payload for an asynchronous import job.
      properties:
        _links:
          $ref: '#/components/schemas/ApiImportLinksResponse'
        job_id:
          title: Job Id
          type: string
        status:
          title: Status
          type: string
      required:
      - job_id
      - status
      - _links
      title: ApiImportResponse
      type: object
    ApiInfoResponse:
      description: API identification information.
      properties:
        host:
          anyOf:
          - type: string
          - type: 'null'
          title: Host
        name:
          title: Name
          type: string
        vendor:
          title: Vendor
          type: string
        version:
          title: Version
          type: string
      required:
      - vendor
      - name
      - version
      title: ApiInfoResponse
      type: object
    ApiKeyCreateRequest:
      description: Create request for api_key credentials.
      examples:
      - api:
          name: stripe
          vendor: stripe.com
          version: '2024-04-10'
        field_name: Authorization
        key: sk_live_…
        location: header
        name: Stripe live key
        provider: static
        type: api_key
      properties:
        api:
          $ref: '#/components/schemas/APIReferenceRequest'
          description: Loose (vendor, name, version) API identity tuple.
        field_name:
          description: Header or query-parameter name carrying the key.
          title: Field Name
          type: string
        key:
          description: The API key secret. Stored encrypted; never returned after create.
          title: Key
          type: string
          x-sensitive: true
        location:
          $ref: '#/components/schemas/CredentialLocation'
          description: Where to inject the key on upstream calls.
        name:
          description: Human-readable label for the credential.
          title: Name
          type: string
        provider:
          default: static
          description: Credential provider; 'static' for stored secrets.
          title: Provider
          type: string
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        type:
          const: api_key
          title: Type
          type: string
      required:
      - type
      - name
      - api
      - key
      - location
      - field_name
      title: ApiKeyCreateRequest
      type: object
    ApiKeyHistoryEntryResponse:
      description: A single event in the API key audit trail.
      properties:
        action:
          title: Action
          type: string
        actor_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
        id:
          title: Id
          type: string
        occurred_at:
          format: date-time
          title: Occurred At
          type: string
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
      required:
      - id
      - action
      - occurred_at
      title: ApiKeyHistoryEntryResponse
      type: object
    ApiKeyHistoryResponse:
      description: Audit trail of API key operations.
      properties:
        data:
          items:
            $ref: '#/components/schemas/ApiKeyHistoryEntryResponse'
          title: Data
          type: array
      required:
      - data
      title: ApiKeyHistoryResponse
      type: object
    ApiKeyInfoResponse:
      description: API key metadata — retrievable even after revocation.
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By
        id:
          title: Id
          type: string
        rotated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Rotated At
        status:
          title: Status
          type: string
      required:
      - id
      - status
      - created_at
      title: ApiKeyInfoResponse
      type: object
    ApiKeyResponse:
      description: Response containing a plaintext API key (shown once).
      properties:
        key:
          title: Key
          type: string
      required:
      - key
      title: ApiKeyResponse
      type: object
    ApiKeyUpdateRequest:
      description: Update request for api_key credentials.
      properties:
        active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Active
        field_name:
          anyOf:
          - type: string
          - type: 'null'
          description: Immutable after create. Accepted for backward compatibility; if provided it must equal the stored value, otherwise the request is rejected. Recreate the credential to change the parameter name.
          title: Field Name
        key:
          anyOf:
          - type: string
          - type: 'null'
          title: Key
          x-sensitive: true
        location:
          anyOf:
          - $ref: '#/components/schemas/CredentialLocation'
          - type: 'null'
          description: Immutable after create. Accepted for backward compatibility; if provided it must equal the stored value, otherwise the request is rejected. Recreate the credential to change the injection binding.
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        type:
          const: api_key
          title: Type
          type: string
      required:
      - type
      title: ApiKeyUpdateRequest
      type: object
    ApiLinksResponse:
      description: Hypermedia links for a (local) API resource.
      properties:
        current_revision:
          anyOf:
          - type: string
          - type: 'null'
          title: Current Revision
        revisions:
          title: Revisions
          type: string
        self:
          title: Self
          type: string
      required:
      - self
      - revisions
      title: ApiLinksResponse
      type: object
    ApiListResponse:
      description: Cursor-paginated list of APIs.
      properties:
        data:
          items:
            $ref: '#/components/schemas/ApiResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: ApiListResponse
      type: object
    ApiReferenceResponse:
      description: Core API identifier triple plus derived host.
      properties:
        host:
          anyOf:
          - type: string
          - type: 'null'
          title: Host
        name:
          title: Name
          type: string
        vendor:
          title: Vendor
          type: string
        version:
          title: Version
          type: string
      required:
      - vendor
      - name
      - version
      - host
      title: ApiReferenceResponse
      type: object
    ApiResponse:
      description: |-
        Full API aggregate response.

        ``GET /apis`` is the local registry — every item is an API imported into
        this deployment. The public catalog of importable-but-not-yet-imported APIs
        is a separate surface (``GET /catalog``); the two are not blended.
      properties:
        _links:
          $ref: '#/components/schemas/ApiLinksResponse'
        api:
          $ref: '#/components/schemas/ApiReferenceResponse'
        catalog_api_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Catalog Api Id
        created_at:
          format: date-time
          title: Created At
          type: string
        current_revision_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Current Revision Id
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        icon_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Icon Url
        operation_count:
          title: Operation Count
          type: integer
        origin:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Provenance of the current revision: `catalog` (imported from the public catalog), `overlay` (materialized from a confirmed overlay), or null (manual import).'
          title: Origin
        revision_count:
          title: Revision Count
          type: integer
        security_schemes:
          items:
            type: string
          title: Security Schemes
          type: array
        source_url:
          anyOf:
          - type: string
          - type: 'null'
          description: Upstream spec URL backing the current revision, when known (catalog linkage).
          title: Source Url
        update_available:
          default: false
          description: Whether the upstream spec at `source_url` has a notified update this API hasn't adopted yet (Flow-3). Re-importing clears it.
          title: Update Available
          type: boolean
        updated_at:
          format: date-time
          title: Updated At
          type: string
      required:
      - api
      - catalog_api_id
      - display_name
      - description
      - icon_url
      - current_revision_id
      - revision_count
      - operation_count
      - security_schemes
      - created_at
      - updated_at
      - _links
      title: ApiResponse
      type: object
    ApiSourceInline:
      description: Import source carrying raw OpenAPI/Arazzo content inline.
      properties:
        api_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Name
        content:
          title: Content
          type: string
        filename:
          title: Filename
          type: string
        submitted_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Submitted By
        type:
          const: inline
          title: Type
          type: string
        vendor:
          anyOf:
          - type: string
          - type: 'null'
          title: Vendor
        version:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
      required:
      - type
      - content
      - filename
      title: ApiSourceInline
      type: object
    ApiSourceUrl:
      description: Import source pointing at a fetchable URL.
      properties:
        api_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Name
        submitted_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Submitted By
        type:
          const: url
          title: Type
          type: string
        url:
          format: uri
          maxLength: 2083
          minLength: 1
          title: Url
          type: string
        vendor:
          anyOf:
          - type: string
          - type: 'null'
          title: Vendor
        version:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
      required:
      - type
      - url
      title: ApiSourceUrl
      type: object
    ApiUpdateRequest:
      additionalProperties: false
      description: Partial update payload for an API's presentation fields.
      properties:
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        icon_url:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - type: 'null'
          title: Icon Url
      title: ApiUpdateRequest
      type: object
    AuditListResponse:
      description: Paginated list of audit entries.
      properties:
        data:
          items:
            $ref: '#/components/schemas/AuditResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: AuditListResponse
      type: object
    AuditResponse:
      description: Audit entry representation in API responses.
      properties:
        action:
          title: Action
          type: string
        actor_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
        actor_session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Session Id
        actor_type:
          title: Actor Type
          type: string
        after:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: After
        before:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Before
        diff:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Diff
        id:
          title: Id
          type: string
        ip_address:
          anyOf:
          - type: string
          - type: 'null'
          title: Ip Address
        job_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Job Id
        occurred_at:
          format: date-time
          title: Occurred At
          type: string
        origin:
          anyOf:
          - type: string
          - type: 'null'
          title: Origin
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
        request_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Request Id
        target_id:
          title: Target Id
          type: string
        target_parent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Parent Id
        target_type:
          title: Target Type
          type: string
        trace_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Trace Id
        user_agent:
          anyOf:
          - type: string
          - type: 'null'
          title: User Agent
      required:
      - id
      - occurred_at
      - action
      - target_type
      - target_id
      - actor_type
      title: AuditResponse
      type: object
    AuditTargetType:
      description: Entity types that can be the target of an audited action.
      enum:
      - revision
      - permission
      - user
      - credential
      - toolkit
      - agent
      - job
      - organisation
      - invite_token
      - event
      - execution_record
      - service_account
      - token
      - overlay
      - note
      - api
      - access_request
      - toolkit_key
      - credential_binding
      - permission_rule_set
      - session
      - provider_config
      - oauth_client
      - oauth_grant
      title: AuditTargetType
      type: string
    BasicAuthCreateRequest:
      description: Create request for basic credentials.
      properties:
        api:
          $ref: '#/components/schemas/APIReferenceRequest'
        name:
          title: Name
          type: string
        password:
          title: Password
          type: string
          x-sensitive: true
        provider:
          default: static
          title: Provider
          type: string
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        type:
          const: basic
          title: Type
          type: string
        username:
          title: Username
          type: string
      required:
      - type
      - name
      - api
      - username
      - password
      title: BasicAuthCreateRequest
      type: object
    BasicAuthUpdateRequest:
      description: Update request for basic credentials.
      properties:
        active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Active
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        password:
          anyOf:
          - type: string
          - type: 'null'
          title: Password
          x-sensitive: true
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        type:
          const: basic
          title: Type
          type: string
        username:
          anyOf:
          - type: string
          - type: 'null'
          title: Username
      required:
      - type
      title: BasicAuthUpdateRequest
      type: object
    BearerTokenCreateRequest:
      description: Create request for bearer_token credentials.
      properties:
        api:
          $ref: '#/components/schemas/APIReferenceRequest'
        name:
          title: Name
          type: string
        provider:
          default: static
          title: Provider
          type: string
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        token:
          title: Token
          type: string
          x-sensitive: true
        type:
          const: bearer_token
          title: Type
          type: string
      required:
      - type
      - name
      - api
      - token
      title: BearerTokenCreateRequest
      type: object
    BearerTokenUpdateRequest:
      description: Update request for bearer_token credentials.
      properties:
        active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Active
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        token:
          anyOf:
          - type: string
          - type: 'null'
          title: Token
          x-sensitive: true
        type:
          const: bearer_token
          title: Type
          type: string
      required:
      - type
      title: BearerTokenUpdateRequest
      type: object
    Body_consentAgentCreate:
      properties:
        agent_name:
          title: Agent Name
          type: string
        consent_token:
          title: Consent Token
          type: string
          x-sensitive: true
        create_state:
          title: Create State
          type: string
          x-sensitive: true
      required:
      - consent_token
      - create_state
      - agent_name
      title: Body_consentAgentCreate
      type: object
    Body_consentSubmit:
      properties:
        action:
          title: Action
          type: string
        agent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Id
        consent_token:
          title: Consent Token
          type: string
          x-sensitive: true
      required:
      - consent_token
      - action
      title: Body_consentSubmit
      type: object
    Body_loginSubmit:
      properties:
        csrf:
          title: Csrf
          type: string
          x-sensitive: true
        email:
          title: Email
          type: string
        ls:
          title: Ls
          type: string
        password:
          title: Password
          type: string
          x-sensitive: true
      required:
      - email
      - password
      - ls
      - csrf
      title: Body_loginSubmit
      type: object
    CatalogEntryLinksResponse:
      description: Hypermedia links for a catalog entry.
      examples:
      - github: https://github.com/jentic/jentic-public-apis/tree/main/apis/openapi/stripe.com
        import: /catalog/stripe.com:import
        operations: /catalog/stripe.com/operations
        self: /catalog/stripe.com
      properties:
        github:
          anyOf:
          - type: string
          - type: 'null'
          description: Human-facing GitHub tree URL for the entry, when known.
          title: Github
        import:
          description: URL of the catalog import action (`POST /catalog/{api_id}:import`).
          title: Import
          type: string
        operations:
          description: URL of the entry's operation preview.
          title: Operations
          type: string
        self:
          description: Canonical URL of this catalog entry.
          title: Self
          type: string
      required:
      - self
      - operations
      - import
      title: CatalogEntryLinksResponse
      type: object
    CatalogEntryResponse:
      description: A single browsable catalog entry.
      examples:
      - _links:
          import: /catalog/stripe.com:import
          operations: /catalog/stripe.com/operations
          self: /catalog/stripe.com
        api_id: stripe.com
        path: apis/openapi/stripe.com
        registered: false
        spec_url: https://raw.githubusercontent.com/jentic/jentic-public-apis/main/apis/openapi/stripe.com/main/2024-01-01/openapi.json
        vendor: stripe.com
      properties:
        _links:
          $ref: '#/components/schemas/CatalogEntryLinksResponse'
        api_id:
          description: Catalog identity of the API (manifest domain, e.g. `stripe.com`).
          title: Api Id
          type: string
        path:
          anyOf:
          - type: string
          - type: 'null'
          description: Manifest path of the entry within the public-APIs repo.
          title: Path
        registered:
          description: Whether this entry is already imported locally — its `spec_url` backs a non-archived revision in `GET /apis`.
          title: Registered
          type: boolean
        spec_url:
          anyOf:
          - type: string
          - type: 'null'
          description: Fetchable OpenAPI spec URL the entry resolves to (used for import + coverage).
          title: Spec Url
        update_available:
          default: false
          description: Whether this (registered) entry has an upstream spec update the local revision hasn't adopted yet. Always false for unregistered entries.
          title: Update Available
          type: boolean
        vendor:
          anyOf:
          - type: string
          - type: 'null'
          description: Registrable-domain vendor derived from `api_id` (e.g. `stripe.com`).
          title: Vendor
      required:
      - api_id
      - vendor
      - path
      - spec_url
      - registered
      - _links
      title: CatalogEntryResponse
      type: object
    CatalogListResponse:
      description: List of catalog entries plus status fields for the Discover status row.
      examples:
      - catalog_total: 5231
        data: []
        has_more: true
        manifest_age_seconds: 3600
        next_cursor: eyJpZCI6ICJzdHJpcGUuY29tIn0=
        registered_count: 12
      properties:
        catalog_total:
          description: Total entries in the whole manifest (pre-filter, pre-page).
          title: Catalog Total
          type: integer
        data:
          description: The page of catalog entries.
          items:
            $ref: '#/components/schemas/CatalogEntryResponse'
          title: Data
          type: array
        has_more:
          default: false
          description: Whether another page follows.
          title: Has More
          type: boolean
        manifest_age_seconds:
          anyOf:
          - type: integer
          - type: 'null'
          description: Age of the cached manifest in seconds, or null when the cache is empty.
          title: Manifest Age Seconds
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          description: Opaque keyset cursor for the next page (null when done).
          title: Next Cursor
        outdated_count:
          default: 0
          description: Count of whole-manifest registered entries with an upstream update available.
          title: Outdated Count
          type: integer
        registered_count:
          description: Count of whole-manifest entries already imported locally.
          title: Registered Count
          type: integer
      required:
      - data
      - catalog_total
      - registered_count
      title: CatalogListResponse
      type: object
    CatalogRefreshResponse:
      description: Acknowledgement of a manifest refresh.
      examples:
      - count: 5231
        status: refreshed
      properties:
        count:
          description: Number of entries in the freshly fetched manifest.
          title: Count
          type: integer
        status:
          default: refreshed
          description: Refresh outcome marker.
          title: Status
          type: string
      required:
      - count
      title: CatalogRefreshResponse
      type: object
    CatalogSnoozeRequest:
      description: |-
        Body for ``POST /catalog/{api_id}:snooze`` (C1, #925).

        ``snoozed_until`` is optional: omit or send ``null`` to mute-until-newer (the primary
        per-API affordance — the badge re-lights only when a *newer* upstream digest lands);
        provide an ISO-8601 timestamp for a time-boxed snooze that lapses at that instant.
      properties:
        snoozed_until:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          description: Optional expiry for the snooze (ISO-8601). Null = mute until a newer upstream digest is observed.
          title: Snoozed Until
      title: CatalogSnoozeRequest
      type: object
    ChangePasswordRequest:
      description: Payload for changing own password.
      properties:
        current_password:
          maxLength: 128
          minLength: 1
          title: Current Password
          type: string
          x-sensitive: true
        new_password:
          maxLength: 128
          minLength: 12
          title: New Password
          type: string
          x-sensitive: true
      required:
      - current_password
      - new_password
      title: ChangePasswordRequest
      type: object
    ClaimRequest:
      description: Request body for claiming ownership of a self-registered agent.
      properties:
        token:
          maxLength: 512
          minLength: 1
          title: Token
          type: string
          x-sensitive: true
      required:
      - token
      title: ClaimRequest
      type: object
    ConnectChallengeResponse:
      description: Response from a connect initiation.
      properties:
        authorize_url:
          title: Authorize Url
          type: string
        state:
          title: State
          type: string
      required:
      - authorize_url
      - state
      title: ConnectChallengeResponse
      type: object
    ConnectRequestBody:
      description: Request body for initiating a credential connect flow.
      properties:
        extra:
          additionalProperties:
            type: string
          title: Extra
          type: object
        scopes:
          items:
            type: string
          title: Scopes
          type: array
      title: ConnectRequestBody
      type: object
    ConsentAgentStatusResponse:
      description: |-
        Minimal tri-state for the consent page's pending-agent awaiting page (P4).

        Deliberately carries nothing else — no agent name, owner, or scopes — so
        the anonymous poll endpoint cannot be used to read agent details. The
        poll is keyed by a signed ``agent-status`` blob bound to one agent id,
        never a bare id, and non-terminal lifecycle states (disabled, archived, a
        vanished row) all read as ``pending`` so the endpoint is not a lifecycle
        oracle either.
      properties:
        status:
          enum:
          - pending
          - approved
          - denied
          title: Status
          type: string
      required:
      - status
      title: ConsentAgentStatusResponse
      type: object
    CreateAdminRequest:
      description: Payload for first-run admin creation (one-time setup).
      properties:
        email:
          maxLength: 320
          minLength: 1
          title: Email
          type: string
        first_name:
          default: Admin
          maxLength: 100
          minLength: 1
          title: First Name
          type: string
        last_name:
          default: User
          maxLength: 100
          minLength: 1
          title: Last Name
          type: string
        password:
          maxLength: 128
          minLength: 12
          title: Password
          type: string
          x-sensitive: true
      required:
      - email
      - password
      title: CreateAdminRequest
      type: object
    CredentialAgentListResponse:
      description: Paginated list of agents directly bound to a credential.
      properties:
        data:
          items:
            $ref: '#/components/schemas/CredentialAgentResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: CredentialAgentListResponse
      type: object
    CredentialAgentResponse:
      description: Agent directly bound to a credential (theme 5 phase 1).
      properties:
        agent_id:
          title: Agent Id
          type: string
        agent_name:
          title: Agent Name
          type: string
        bound_at:
          format: date-time
          title: Bound At
          type: string
        rule_set_id:
          anyOf:
          - type: string
          - type: 'null'
          description: Shared permission rule set this binding points at, if any. While attached, the set's ordered list is the binding's effective policy; null means the binding's inline rules apply.
          title: Rule Set Id
        status:
          title: Status
          type: string
        suspended:
          description: 'True when the binding is soft-suspended (reversible cut-off): the binding and its rules survive, but the agent cannot execute through this credential until resumed.'
          title: Suspended
          type: boolean
      required:
      - agent_id
      - agent_name
      - status
      - bound_at
      - suspended
      title: CredentialAgentResponse
      type: object
    CredentialBindRequest:
      description: Request body for directly binding a credential to an agent.
      properties:
        credential_id:
          maxLength: 255
          minLength: 1
          title: Credential Id
          type: string
      required:
      - credential_id
      title: CredentialBindRequest
      type: object
    CredentialBindingEntry:
      description: Direct agent↔credential binding summary for the /me response (theme 5 phase 1).
      properties:
        bound_at:
          format: date-time
          title: Bound At
          type: string
        credential_id:
          title: Credential Id
          type: string
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        rule_set_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Rule Set Id
        serves:
          default: []
          items:
            $ref: '#/components/schemas/ServedApiRef'
          title: Serves
          type: array
        suspended:
          default: false
          title: Suspended
          type: boolean
      required:
      - credential_id
      - bound_at
      title: CredentialBindingEntry
      type: object
    CredentialBindingListResponse:
      description: List of direct credential bindings.
      properties:
        data:
          items:
            $ref: '#/components/schemas/CredentialBindingResponse'
          title: Data
          type: array
      required:
      - data
      title: CredentialBindingListResponse
      type: object
    CredentialBindingResponse:
      description: Direct agent↔credential binding representation in API responses.
      properties:
        agent_id:
          title: Agent Id
          type: string
        bound_at:
          format: date-time
          title: Bound At
          type: string
        credential_id:
          title: Credential Id
          type: string
        id:
          title: Id
          type: string
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        rule_set_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Rule Set Id
        serves:
          default: []
          items:
            $ref: '#/components/schemas/ServedApiRef'
          title: Serves
          type: array
        suspended:
          title: Suspended
          type: boolean
      required:
      - id
      - agent_id
      - credential_id
      - bound_at
      - suspended
      title: CredentialBindingResponse
      type: object
    CredentialCreateResponse:
      description: 'Create response: redacted + secret shown once.'
      properties:
        credential:
          $ref: '#/components/schemas/CredentialRedactedResponse'
        secret:
          additionalProperties: true
          title: Secret
          type: object
          x-sensitive: true
      required:
      - credential
      - secret
      title: CredentialCreateResponse
      type: object
    CredentialListResponse:
      description: Paginated list of credentials.
      properties:
        data:
          items:
            $ref: '#/components/schemas/CredentialRedactedResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: CredentialListResponse
      type: object
    CredentialLocation:
      description: Where an API-key credential is injected into a request.
      enum:
      - header
      - query
      - cookie
      title: CredentialLocation
      type: string
    CredentialRedactedResponse:
      description: Redacted credential response (for read/list/patch).
      examples:
      - active: true
        api:
          name: stripe
          vendor: stripe.com
          version: '2024-04-10'
        created_at: '2026-01-15T09:30:00Z'
        credential_id: cred_01HZX9...
        details:
          field_name: Authorization
          hint: …live_abcd
          location: header
        name: Stripe live key
        provider: static
        type: api_key
      properties:
        active:
          description: Whether the credential is enabled for injection.
          title: Active
          type: boolean
        api:
          $ref: '#/components/schemas/APIReference'
          description: The (vendor, name, version) API this credential targets.
        catalog_api_id:
          anyOf:
          - type: string
          - type: 'null'
          description: Catalog identity slug of the target API (`domain[/sub-api]`), when recorded at create time. Display-only; null for older credentials.
          title: Catalog Api Id
        created_at:
          description: Creation timestamp (UTC).
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - type: string
          - type: 'null'
          description: Identity that created the credential (its owner).
          title: Created By
        credential_id:
          description: Stable credential identifier, prefixed `cred_`.
          title: Credential Id
          type: string
        details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          description: 'Redacted, type-specific projection (hints/last-N chars; never the secret). For oauth2: client_id, token_url, grant_type (authorization_code | client_credentials), scopes, and — for authorization_code only — `connected`, whether the interactive sign-in completed and is still usable (null for other grants).'
          title: Details
        name:
          description: Human-readable label.
          title: Name
          type: string
        provider:
          description: Credential provider; 'static' for stored secrets.
          title: Provider
          type: string
        provider_account_ref:
          anyOf:
          - type: string
          - type: 'null'
          description: Opaque reference to the provider account, when applicable.
          title: Provider Account Ref
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          description: OpenAPI server-variable values for URL template substitution.
          title: Server Variables
        type:
          $ref: '#/components/schemas/CredentialType'
          description: Credential auth type (api_key, bearer_token, basic, oauth2).
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          description: Last update timestamp (UTC).
          title: Updated At
      required:
      - credential_id
      - type
      - name
      - api
      - provider
      - active
      - created_at
      title: CredentialRedactedResponse
      type: object
    CredentialType:
      description: Wire-level credential type used by the provider abstraction.
      enum:
      - bearer_token
      - api_key
      - basic
      - oauth2
      - no_auth
      - sigv4
      title: CredentialType
      type: string
    CurrentUserResponse:
      description: Current user profile.
      properties:
        active:
          title: Active
          type: boolean
        created_at:
          format: date-time
          title: Created At
          type: string
        email:
          title: Email
          type: string
        first_name:
          title: First Name
          type: string
        id:
          title: Id
          type: string
        last_name:
          title: Last Name
          type: string
        must_change_password:
          title: Must Change Password
          type: boolean
        permissions:
          items:
            type: string
          title: Permissions
          type: array
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
      required:
      - id
      - email
      - first_name
      - last_name
      - active
      - permissions
      - must_change_password
      - created_at
      title: CurrentUserResponse
      type: object
    DailyExecutionBucket:
      description: Execution counts for a single day.
      properties:
        date:
          title: Date
          type: string
        failed:
          title: Failed
          type: integer
        success:
          title: Success
          type: integer
        total:
          title: Total
          type: integer
      required:
      - date
      - total
      - success
      - failed
      title: DailyExecutionBucket
      type: object
    DecideItemSchema:
      description: A single item decision.
      properties:
        decision:
          enum:
          - approved
          - denied
          title: Decision
          type: string
        decision_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Decision Reason
        item_id:
          title: Item Id
          type: string
      required:
      - item_id
      - decision
      title: DecideItemSchema
      type: object
    DecideRequest:
      description: Request body for the :decide verb.
      properties:
        items:
          items:
            $ref: '#/components/schemas/DecideItemSchema'
          minItems: 1
          title: Items
          type: array
      required:
      - items
      title: DecideRequest
      type: object
    DuplicatePendingProblem:
      description: |-
        RFC 9457 Problem Details for a 409 on ``POST /access-requests``.

        Filing a request whose target already has a pending request is refused with
        a 409 whose body carries two extension members on top of the standard
        Problem Details shape, so a client can attach to the existing request rather
        than re-file. These are emitted at runtime by the access-request error hook
        (``control/web/errors.py``); this model documents them in the OpenAPI spec so
        the generated SDK exposes a typed 409
        (``FileAccessRequestHTTPResp.ApplicationproblemJSON409``) instead of forcing
        callers to parse the raw body (ARCH-21 Step 0).
      examples:
      - detail: The request body is missing one or more required fields.
        errors:
        - detail: Field 'name' is required.
          pointer: '#/name'
        instance: /v2/capability-sets
        status: 400
        title: Bad Request
        type: about:blank
      properties:
        approve_url:
          description: Console URL to review/approve the existing pending request.
          examples:
          - https://app.jentic.com/access-requests/acr_01HXXY...
          title: Approve Url
          type: string
        code:
          anyOf:
          - maxLength: 50
            type: string
          - type: 'null'
          description: An optional provider-specific code for internal error taxonomy and observability correlation.
          examples:
          - JENTIC-4001
          title: Code
        detail:
          description: A human-readable explanation specific to this occurrence of the problem. MUST be present. Provide actionable information where possible.
          examples:
          - The request body is missing required field 'name'.
          maxLength: 4096
          title: Detail
          type: string
        errors:
          anyOf:
          - items:
              $ref: '#/components/schemas/ErrorItem'
            maxItems: 1000
            type: array
          - type: 'null'
          description: An array of granular error details. Use when multiple validation errors or field-level problems need to be surfaced in a single response.
          title: Errors
        existing_request_id:
          description: The id of the pending access request that already covers the conflicting target.
          examples:
          - acr_01HXXY...
          title: Existing Request Id
          type: string
        instance:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: A URI reference identifying the specific occurrence of the problem. Typically the request path.
          examples:
          - /v2/capability-sets
          title: Instance
        status:
          anyOf:
          - maximum: 599.0
            minimum: 100.0
            type: integer
          - type: 'null'
          description: The HTTP status code for this occurrence of the problem.
          examples:
          - 400
          title: Status
        title:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: A short, human-readable summary of the problem type. Should not change between occurrences except for localisation purposes.
          examples:
          - Bad Request
          title: Title
        type:
          default: about:blank
          description: A URI reference identifying the problem type. When set to 'about:blank', the title SHOULD be the standard HTTP status phrase. Use an IANA-registered type URI where one applies.
          examples:
          - about:blank
          maxLength: 1024
          title: Type
          type: string
      required:
      - detail
      - existing_request_id
      - approve_url
      title: DuplicatePendingProblem
      type: object
    EffectivePermission:
      description: A single effective permission with provenance.
      properties:
        implied_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Implied By
        name:
          title: Name
          type: string
      required:
      - name
      title: EffectivePermission
      type: object
    ErrorItem:
      description: |-
        A granular error detail entry within the errors[] array of a ProblemDetails response.

        At least one of pointer, parameter, or header SHOULD be present to identify the error source.
      properties:
        code:
          anyOf:
          - maxLength: 50
            type: string
          - type: 'null'
          description: An optional provider-specific code identifying this error in internal taxonomy or documentation.
          examples:
          - JENTIC-V-001
          title: Code
        detail:
          description: A human-readable explanation of this specific error. Be precise — name the field, parameter, or header involved.
          examples:
          - Field 'name' must not be blank.
          maxLength: 4096
          title: Detail
          type: string
        header:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: The name of the request header that is the source of this error.
          examples:
          - Authorization
          title: Header
        parameter:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: The name of the query or path parameter that is the source of this error.
          examples:
          - limit
          title: Parameter
        pointer:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: A JSON Pointer (RFC 6901) to the specific request body property that is the source of this error.
          examples:
          - '#/name'
          title: Pointer
      required:
      - detail
      title: ErrorItem
      type: object
    EvaluationCheckResponse:
      description: A single evaluation check result.
      properties:
        blocker:
          anyOf:
          - type: string
          - type: 'null'
          title: Blocker
        check:
          title: Check
          type: string
        passed:
          title: Passed
          type: boolean
      required:
      - check
      - passed
      title: EvaluationCheckResponse
      type: object
    EvaluationResponse:
      description: Computed evaluation of whether the caller can fulfill a request.
      properties:
        can_fulfill:
          title: Can Fulfill
          type: boolean
        checks:
          items:
            $ref: '#/components/schemas/EvaluationCheckResponse'
          title: Checks
          type: array
      required:
      - can_fulfill
      - checks
      title: EvaluationResponse
      type: object
    EventAcknowledgeRequest:
      description: Request body for acknowledging an event.
      properties:
        acknowledged:
          title: Acknowledged
          type: boolean
        note:
          anyOf:
          - maxLength: 2000
            type: string
          - type: 'null'
          title: Note
      required:
      - acknowledged
      title: EventAcknowledgeRequest
      type: object
    EventLinks:
      description: HAL-style links for an event.
      properties:
        action:
          anyOf:
          - type: string
          - type: 'null'
          title: Action
        execution:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution
        job:
          anyOf:
          - type: string
          - type: 'null'
          title: Job
        self:
          title: Self
          type: string
      required:
      - self
      title: EventLinks
      type: object
    EventListResponse:
      description: Paginated list of events.
      properties:
        data:
          items:
            $ref: '#/components/schemas/EventResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: EventListResponse
      type: object
    EventResponse:
      description: Event representation in API responses.
      properties:
        _links:
          $ref: '#/components/schemas/EventLinks'
        acknowledged:
          title: Acknowledged
          type: boolean
        acknowledged_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Acknowledged At
        acknowledged_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Acknowledged By
        actor_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
        actor_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Type
        created_at:
          format: date-time
          title: Created At
          type: string
        data:
          additionalProperties: true
          title: Data
          type: object
        detail:
          anyOf:
          - type: string
          - type: 'null'
          title: Detail
        event_id:
          title: Event Id
          type: string
        requires_action:
          title: Requires Action
          type: boolean
        severity:
          $ref: '#/components/schemas/EventSeverity'
        summary:
          title: Summary
          type: string
        trace_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Trace Id
        type:
          title: Type
          type: string
      required:
      - event_id
      - type
      - severity
      - summary
      - requires_action
      - acknowledged
      - created_at
      - _links
      title: EventResponse
      type: object
    EventSeverity:
      description: Severity level for platform events.
      enum:
      - info
      - warning
      - error
      - critical
      title: EventSeverity
      type: string
    ExecutionListResponse:
      description: Paginated list of executions.
      properties:
        data:
          items:
            $ref: '#/components/schemas/ExecutionResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: ExecutionListResponse
      type: object
    ExecutionRecordLinks:
      description: HATEOAS links for an execution record.
      properties:
        self:
          title: Self
          type: string
      required:
      - self
      title: ExecutionRecordLinks
      type: object
    ExecutionResponse:
      description: Execution record representation in API responses.
      properties:
        _links:
          $ref: '#/components/schemas/ExecutionRecordLinks'
        actor_id:
          title: Actor Id
          type: string
        actor_type:
          title: Actor Type
          type: string
        api:
          anyOf:
          - $ref: '#/components/schemas/ApiInfoResponse'
          - type: 'null'
        created_at:
          format: date-time
          title: Created At
          type: string
        credential_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Id
        credential_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Name
        duration_ms:
          anyOf:
          - type: integer
          - type: 'null'
          title: Duration Ms
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
        execution_id:
          title: Execution Id
          type: string
        http_status:
          anyOf:
          - type: integer
          - type: 'null'
          title: Http Status
        operation_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Operation Id
        origin:
          anyOf:
          - type: string
          - type: 'null'
          title: Origin
        pinned_revisions:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Pinned Revisions
        started_at:
          format: date-time
          title: Started At
          type: string
        status:
          title: Status
          type: string
        toolkit_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Toolkit Id
        toolkit_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Toolkit Name
        trace_id:
          title: Trace Id
          type: string
      required:
      - execution_id
      - trace_id
      - started_at
      - status
      - created_at
      - actor_id
      - actor_type
      - _links
      title: ExecutionResponse
      type: object
    ExecutionStatsResponse:
      description: Aggregated execution statistics for the dashboard.
      properties:
        daily_buckets:
          items:
            $ref: '#/components/schemas/DailyExecutionBucket'
          title: Daily Buckets
          type: array
        success_rate_percent:
          title: Success Rate Percent
          type: number
        top_operations:
          items:
            $ref: '#/components/schemas/TopOperation'
          title: Top Operations
          type: array
        total_executions:
          title: Total Executions
          type: integer
      required:
      - total_executions
      - success_rate_percent
      - daily_buckets
      - top_operations
      title: ExecutionStatsResponse
      type: object
    GovernedHostsResponse:
      description: |-
        The caller's governed host set (canonical order) with its change digest.

        **Hosts only, deliberately**: the set exists for interception scoping
        (divert lists, host filters), so it carries the minimum knowledge — API
        detail stays behind the existing authenticated reads (``GET /apis``).

        Deliberately **unpaginated**: the set is bounded by the caller's own
        credential bindings (tens of hosts, not thousands) and the digest must
        cover the whole set atomically — a paginated digest would be meaningless.
        This is a documented deviation from the list-endpoint pagination
        convention.
      properties:
        data:
          description: Governed host entries, normalised (lowercased, FQDN root dot stripped, internationalised names IDNA/punycode-encoded), deduplicated, and sorted — the literal URL-index hosts the broker's discovery matches for the APIs the caller's bound credentials cover. An entry is a hostname, a bare IP literal, or either followed by a non-default port (`host[:port]` — default ports are already stripped). Compare case-insensitively (lowercase the incoming host before matching); a gate keying on hostname or SNI alone must strip any `:port` suffix from the entry first. Variable-bearing hosts (defaultless `{var}` server variables) are excluded — the broker's discovery never matches them. On a `5xx` retain the last known set; never fall back to an empty (intercept-nothing) list.
          items:
            type: string
          title: Data
          type: array
        digest:
          description: 'SHA-256 hex digest over the newline-joined `data` list; also emitted as the response''s strong `ETag`. To change-poll, send it back quoted — `If-None-Match: "<digest>"` (the bare digest is accepted as a compatibility form) — and expect an empty `304` until the host set changes. Poll at most once per minute.'
          title: Digest
          type: string
      required:
      - data
      - digest
      title: GovernedHostsResponse
      type: object
    GroupBy:
      description: |-
        Grouping dimension for usage statistics.

        ``TOOLKIT`` is deprecated (theme-5 Phase 5b) and will be removed one
        release later, with the toolkit tables (Phase 6b): execution records
        carry a ``credential_id`` since Phase 2 and the direct-binding path
        writes no ``toolkit_id``, so ``CREDENTIAL`` is the replacement axis.
      enum:
      - api
      - toolkit
      - credential
      - agent
      title: GroupBy
      type: string
    HealthResponse:
      description: Health check response for the admin surface.
      properties:
        next_step:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Step
        setup_required:
          title: Setup Required
          type: boolean
        status:
          title: Status
          type: string
        surface:
          title: Surface
          type: string
      required:
      - status
      - surface
      - setup_required
      title: HealthResponse
      type: object
    InstanceIdentityResponse:
      description: |-
        Self-describing identity of the backend serving this request.

        A client can compare ``backend``/``canonical_base_url``/``host`` against where
        it *thinks* it is pointed to confirm it is talking to the intended backend
        (e.g. a local install vs. a remote one) before diagnosing "missing" data.
      properties:
        backend:
          description: 'Operator-declared backend locality (server.backend): ''local'' for a self-hosted install on the operator''s own machine/network, ''remote'' for a hosted install run elsewhere. A hint, not an authorization signal; defaults to ''local''.'
          enum:
          - local
          - remote
          title: Backend
          type: string
        broker_url:
          anyOf:
          - type: string
          - type: 'null'
          description: 'The broker (data plane) base URL a client should send `execute` traffic to, as configured by the operator (server.mcp.broker_url), with any userinfo stripped. Null when the platform cannot honestly advertise one: on a ''remote'' backend a loopback-host value (the config default) describes the control plane''s own machine, not an address any client can dial, so it is withheld rather than published as misleading guidance. Deployment metadata, not a secret — the broker URL is handed to every client expected to call it (issue #1249).'
          title: Broker Url
        canonical_base_url:
          description: The instance's own canonical base URL (auth.canonical_base_url), with any userinfo stripped; '' if unset.
          title: Canonical Base Url
          type: string
        host:
          description: Host (and port, when the canonical base URL declares one) parsed from canonical_base_url; '' if unset or unparseable.
          title: Host
          type: string
        instance_id:
          anyOf:
          - type: string
          - type: 'null'
          description: Opaque digest derived from the telemetry instance id — stable per install, but not the telemetry id itself. Null when telemetry has not resolved an id (e.g. telemetry disabled).
          title: Instance Id
        mcp_enabled:
          default: false
          description: 'Whether this instance serves the daemon-native Streamable HTTP MCP endpoint at /mcp (server.mcp.enabled). Lets clients (notably the UI''s agent MCP card) advertise the HTTP transport only when it exists. Not sensitive: the endpoint''s enabled state is observable by probing /mcp anyway.'
          title: Mcp Enabled
          type: boolean
      required:
      - backend
      - canonical_base_url
      - host
      title: InstanceIdentityResponse
      type: object
    IntrospectRequest:
      description: Introspection endpoint request (form body).
      properties:
        token:
          title: Token
          type: string
          x-sensitive: true
        token_type_hint:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Type Hint
      required:
      - token
      title: IntrospectRequest
      type: object
    IntrospectResponse:
      description: RFC 7662 introspection response.
      properties:
        active:
          title: Active
          type: boolean
        exp:
          anyOf:
          - type: integer
          - type: 'null'
          title: Exp
        scope:
          anyOf:
          - type: string
          - type: 'null'
          title: Scope
        sub:
          anyOf:
          - type: string
          - type: 'null'
          title: Sub
        token_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Type
      required:
      - active
      title: IntrospectResponse
      type: object
    InviteIssuedResponse:
      description: Response when an invite token is issued.
      properties:
        expires_at:
          format: date-time
          title: Expires At
          type: string
        token:
          title: Token
          type: string
          x-sensitive: true
      required:
      - token
      - expires_at
      title: InviteIssuedResponse
      type: object
    InviteState:
      description: State of a user invitation.
      enum:
      - pending
      - redeemed
      - expired
      - accepted
      title: InviteState
      type: string
    JobLinksResponse:
      description: Hypermedia links for a job.
      properties:
        execution:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution
        result:
          anyOf:
          - type: string
          - type: 'null'
          title: Result
        self:
          title: Self
          type: string
      required:
      - self
      title: JobLinksResponse
      type: object
    JobListResponse:
      description: Paginated list of jobs.
      properties:
        data:
          items:
            $ref: '#/components/schemas/JobResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: JobListResponse
      type: object
    JobResponse:
      description: Job representation in API responses.
      properties:
        _links:
          $ref: '#/components/schemas/JobLinksResponse'
        created_at:
          format: date-time
          title: Created At
          type: string
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
        execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Id
        job_id:
          title: Job Id
          type: string
        kind:
          title: Kind
          type: string
        status:
          title: Status
          type: string
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
      required:
      - job_id
      - kind
      - status
      - created_at
      - _links
      title: JobResponse
      type: object
    JwksUpdateRequest:
      description: |-
        Request body for updating an agent's JWKS (public keys).

        The JWKS must contain at least one Ed25519 public key and must not
        contain any private key material.
      properties:
        jwks:
          additionalProperties: true
          description: JWKS containing public keys
          title: Jwks
          type: object
      required:
      - jwks
      title: JwksUpdateRequest
      type: object
    LoginRequest:
      description: Credentials for login.
      properties:
        email:
          maxLength: 320
          minLength: 1
          title: Email
          type: string
        password:
          maxLength: 128
          minLength: 1
          title: Password
          type: string
          x-sensitive: true
      required:
      - email
      - password
      title: LoginRequest
      type: object
    LoginResponse:
      description: JWT token response after successful authentication.
      properties:
        access_token:
          title: Access Token
          type: string
          x-sensitive: true
        expires_in:
          title: Expires In
          type: integer
        must_change_password:
          title: Must Change Password
          type: boolean
        token_type:
          title: Token Type
          type: string
      required:
      - access_token
      - token_type
      - expires_in
      - must_change_password
      title: LoginResponse
      type: object
    McpConfigRegistrationRequest:
      description: Report that an MCP config entry was written for one agent runtime.
      examples:
      - runtime: cursor
      properties:
        runtime:
          $ref: '#/components/schemas/McpConfigRuntime'
          description: The agent runtime whose MCP config entry was written — a closed set; clients map anything unrecognised to `other`.
      required:
      - runtime
      title: McpConfigRegistrationRequest
      type: object
    McpConfigRegistrationResponse:
      description: Acknowledgement of a config-registration report.
      properties:
        recorded:
          description: Whether the report was recorded as an event. `false` means it was accepted but throttled (an identical (actor, runtime) report was already recorded within the last 24h).
          title: Recorded
          type: boolean
        runtime:
          $ref: '#/components/schemas/McpConfigRuntime'
          description: The runtime the report was recorded for.
      required:
      - runtime
      - recorded
      title: McpConfigRegistrationResponse
      type: object
    McpConfigRuntime:
      description: |-
        Closed-enum tag naming the agent runtime an MCP config entry targets.

        Attached only to ``mcp_config_registered`` — one event per runtime whose
        config file/entry ``jentic setup``/``jentic skill init`` writes. A closed
        set (never the raw runtime string) so the config-written → first-session →
        first-execute funnel stays property-free on the wire.
      enum:
      - claude_desktop
      - claude_code
      - cursor
      - codex
      - other
      title: McpConfigRuntime
      type: string
    MeAgent:
      description: Identity response for an agent actor.
      properties:
        approved_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Approved By
        credential_bindings:
          default: []
          items:
            $ref: '#/components/schemas/CredentialBindingEntry'
          title: Credential Bindings
          type: array
        id:
          title: Id
          type: string
        name:
          title: Name
          type: string
        parent_agent_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Parent Agent Id
        scopes:
          items:
            type: string
          title: Scopes
          type: array
        status:
          title: Status
          type: string
        token_scopes:
          items:
            type: string
          title: Token Scopes
          type: array
        toolkit_bindings:
          items:
            $ref: '#/components/schemas/ToolkitBindingEntry'
          title: Toolkit Bindings
          type: array
        type:
          const: agent
          default: agent
          title: Type
          type: string
      required:
      - id
      - name
      - status
      - scopes
      - token_scopes
      - toolkit_bindings
      title: MeAgent
      type: object
    MeServiceAccount:
      description: Identity response for a service-account actor.
      properties:
        approved_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Approved By
        id:
          title: Id
          type: string
        name:
          title: Name
          type: string
        registered_by:
          title: Registered By
          type: string
        scopes:
          items:
            type: string
          title: Scopes
          type: array
        status:
          title: Status
          type: string
        token_scopes:
          items:
            type: string
          title: Token Scopes
          type: array
        type:
          const: service_account
          default: service_account
          title: Type
          type: string
      required:
      - id
      - name
      - status
      - scopes
      - token_scopes
      - registered_by
      title: MeServiceAccount
      type: object
    MeUser:
      description: Identity response for a user actor.
      properties:
        admin:
          title: Admin
          type: boolean
        email:
          title: Email
          type: string
        id:
          title: Id
          type: string
        must_change_password:
          title: Must Change Password
          type: boolean
        name:
          title: Name
          type: string
        scopes:
          items:
            type: string
          title: Scopes
          type: array
        status:
          title: Status
          type: string
        type:
          const: user
          default: user
          title: Type
          type: string
      required:
      - id
      - name
      - email
      - admin
      - status
      - scopes
      - must_change_password
      title: MeUser
      type: object
    MintRequest:
      description: Ephemeral token minting request.
      properties:
        scope:
          title: Scope
          type: string
        target_agent_id:
          title: Target Agent Id
          type: string
        ttl_seconds:
          anyOf:
          - maximum: 3600.0
            minimum: 1.0
            type: integer
          - type: 'null'
          title: Ttl Seconds
      required:
      - scope
      - target_agent_id
      title: MintRequest
      type: object
    MintResponse:
      description: Ephemeral token minting response.
      properties:
        access_token:
          title: Access Token
          type: string
          x-sensitive: true
        expires_in:
          title: Expires In
          type: integer
        token_type:
          default: bearer
          title: Token Type
          type: string
      required:
      - access_token
      - expires_in
      title: MintResponse
      type: object
    NoAuthCreateRequest:
      description: |-
        Create request for no_auth credentials.

        A no-auth credential carries no secret — it represents "this API is called
        without authentication". It still exists as a credential row so a toolkit
        binding (and its permission rules) can hang off it, and the broker resolves
        it as a no-op auth (see broker credential resolver / injection).
      properties:
        api:
          $ref: '#/components/schemas/APIReferenceRequest'
        name:
          title: Name
          type: string
        provider:
          default: static
          title: Provider
          type: string
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        type:
          const: no_auth
          title: Type
          type: string
      required:
      - type
      - name
      - api
      title: NoAuthCreateRequest
      type: object
    NoteApiReference:
      additionalProperties: false
      description: Loose ``(vendor, name, version)`` identity tuple for a registered API.
      properties:
        name:
          title: Name
          type: string
        vendor:
          title: Vendor
          type: string
        version:
          title: Version
          type: string
      required:
      - vendor
      - name
      - version
      title: NoteApiReference
      type: object
    NoteConfidence:
      description: Author-supplied confidence in a note (matches the spec ``NoteConfidence`` enum).
      enum:
      - observed
      - suspected
      - verified
      title: NoteConfidence
      type: string
    NoteCreateRequest:
      additionalProperties: false
      description: Payload for creating a new note.
      properties:
        body:
          maxLength: 4000
          title: Body
          type: string
        confidence:
          anyOf:
          - $ref: '#/components/schemas/NoteConfidence'
          - type: 'null'
        related_execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Related Execution Id
        resource:
          $ref: '#/components/schemas/NoteResource'
        source:
          anyOf:
          - $ref: '#/components/schemas/NoteSource'
          - type: 'null'
        type:
          anyOf:
          - $ref: '#/components/schemas/NoteType'
          - type: 'null'
      required:
      - resource
      - body
      title: NoteCreateRequest
      type: object
    NoteResource:
      additionalProperties: false
      description: Exactly-one-of resource identifier for a note (request side).
      properties:
        api:
          anyOf:
          - $ref: '#/components/schemas/NoteApiReference'
          - type: 'null'
        credential_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Id
        execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Id
        operation_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Operation Id
      title: NoteResource
      type: object
    NoteSource:
      description: Origin tag for a note (matches the spec ``NoteSource`` enum).
      enum:
      - agent
      - human
      - platform
      title: NoteSource
      type: string
    NoteType:
      description: Category of a note (matches the spec ``NoteType`` enum).
      enum:
      - auth_quirk
      - usage_hint
      - execution_feedback
      - correction
      title: NoteType
      type: string
    NoteUpdateRequest:
      additionalProperties: false
      description: Payload for updating an existing note (partial).
      properties:
        body:
          anyOf:
          - maxLength: 4000
            type: string
          - type: 'null'
          title: Body
        confidence:
          anyOf:
          - $ref: '#/components/schemas/NoteConfidence'
          - type: 'null'
        related_execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Related Execution Id
        source:
          anyOf:
          - $ref: '#/components/schemas/NoteSource'
          - type: 'null'
        type:
          anyOf:
          - $ref: '#/components/schemas/NoteType'
          - type: 'null'
      title: NoteUpdateRequest
      type: object
    OAuth2CreateRequest:
      description: |-
        Create request for oauth2 credentials.

        For managed providers (e.g. pipedream, direct_oauth2), token_url/client_id/client_secret
        are optional — the connect flow handles authentication without caller-supplied client details.
      properties:
        api:
          $ref: '#/components/schemas/APIReferenceRequest'
        authorize_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorize Url
        client_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Client Id
        client_secret:
          anyOf:
          - type: string
          - type: 'null'
          title: Client Secret
          x-sensitive: true
        grant_type:
          default: client_credentials
          title: Grant Type
          type: string
        name:
          title: Name
          type: string
        provider:
          default: static
          title: Provider
          type: string
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        scopes:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Scopes
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        token_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Url
        type:
          const: oauth2
          title: Type
          type: string
      required:
      - type
      - name
      - api
      title: OAuth2CreateRequest
      type: object
    OAuth2UpdateRequest:
      description: Update request for oauth2 credentials.
      properties:
        active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Active
        client_secret:
          anyOf:
          - type: string
          - type: 'null'
          title: Client Secret
          x-sensitive: true
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        scopes:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Scopes
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        token_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Url
        type:
          const: oauth2
          title: Type
          type: string
      required:
      - type
      title: OAuth2UpdateRequest
      type: object
    OAuthApprovalDecisionRequest:
      description: |-
        Inline admin approve/deny posted from the approval-pending page.

        ``state`` is the signed approval-state blob minted by ``/authorize`` for
        this exact authorize request — the decision endpoint never accepts a bare
        ``client_id``.

        ``state`` is deliberately NOT marked x-sensitive: the CLI's GEN-21
        redaction backstop unions every sensitive field's BARE name globally, and
        "state" is generic enough to redact unrelated CLI output (e.g. the MCP
        session-diagnosis ``state`` field). The blob is not a lasting bearer
        credential — it is HMAC-signed, purpose-discriminated, TTL'd (600 s), and
        the decision endpoint additionally requires an authenticated admin with
        ``oauth-clients:write`` — so global redaction buys nothing worth that
        collision.
      properties:
        action:
          enum:
          - approve
          - deny
          title: Action
          type: string
        state:
          title: State
          type: string
      required:
      - state
      - action
      title: OAuthApprovalDecisionRequest
      type: object
    OAuthApprovalStatusResponse:
      description: |-
        Minimal tri-state approval status for a pending-client authorize request.

        Deliberately carries nothing else — no client name, redirect URIs, or
        metadata — so the anonymous poll endpoint cannot be used to read client
        details out of the registry.
      properties:
        status:
          enum:
          - pending
          - approved
          - denied
          title: Status
          type: string
      required:
      - status
      title: OAuthApprovalStatusResponse
      type: object
    OAuthClientCreateRequest:
      description: Request body for creating an OAuth client.
      examples:
      - description: My application production deployment
        name: my-app-production
        redirect_uris:
        - https://app.example.com/auth/callback
        require_consent: true
      properties:
        allowed_scopes:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: If set, restricts which scopes this client may request. An empty list denies all non-OIDC scopes. Null means unrestricted (all scopes permitted).
          title: Allowed Scopes
        consent_model:
          default: user
          description: What a user's consent grants for this client. ``user`` keeps today's act-as-user semantics; ``agent`` marks the client for agent-bound consent (the MCP path).
          enum:
          - user
          - agent
          title: Consent Model
          type: string
        description:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional description of the client.
          title: Description
        name:
          description: Human-readable name for the client.
          maxLength: 255
          title: Name
          type: string
        redirect_uris:
          description: Allowed OAuth callback URLs. At least one required.
          items:
            maxLength: 2048
            type: string
          maxItems: 20
          minItems: 1
          title: Redirect Uris
          type: array
        require_consent:
          default: true
          description: Whether to show a consent screen during authorization. Set to false for trusted first-party integrations.
          title: Require Consent
          type: boolean
        token_endpoint_auth_method:
          default: client_secret_basic
          description: Client authentication method at the token endpoint. ``client_secret_basic`` creates a confidential client with a generated secret; ``none`` creates a public (secret-less) client that relies on PKCE alone — no secret is generated or returned.
          enum:
          - client_secret_basic
          - none
          title: Token Endpoint Auth Method
          type: string
      required:
      - name
      - redirect_uris
      title: OAuthClientCreateRequest
      type: object
    OAuthClientCreateResponse:
      description: Returned on creation — includes the one-time plaintext client secret.
      examples:
      - active: true
        approval_status: approved
        client_id: oc_abc123...
        consent_model: user
        created_at: '2026-08-18T12:00:00Z'
        created_by: usr_abc123
        description: My application production deployment
        id: oac_2NxYz...
        name: my-app-production
        redirect_uris:
        - https://app.example.com/auth/callback
        registration_source: admin
        require_consent: true
        token_endpoint_auth_method: client_secret_basic
      properties:
        active:
          title: Active
          type: boolean
        active_grant_count:
          default: 0
          description: Number of active consent→agent grants for this client. Computed on the read endpoints (list/get); write-path responses report 0.
          title: Active Grant Count
          type: integer
        allowed_scopes:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: Scopes this client may request. Null means unrestricted.
          title: Allowed Scopes
        approval_status:
          description: 'Admin approval lifecycle: ``pending``, ``approved``, or ``denied``. Only approved clients may enter OAuth flows; ``active`` remains the independent kill switch.'
          title: Approval Status
          type: string
        client_id:
          description: Public client identifier used in OAuth flows.
          title: Client Id
          type: string
        client_secret:
          anyOf:
          - type: string
          - type: 'null'
          description: The client secret. Shown only once at creation — store it securely. Null for public (secret-less) clients.
          title: Client Secret
          x-sensitive: true
        consent_model:
          description: 'What a user''s consent grants for this client: ``user`` or ``agent``.'
          title: Consent Model
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        id:
          description: Internal ID (ksuid).
          title: Id
          type: string
        name:
          title: Name
          type: string
        redirect_uris:
          items:
            type: string
          title: Redirect Uris
          type: array
        registration_source:
          description: 'How the client entered the registry: ``admin`` or ``dcr``.'
          title: Registration Source
          type: string
        require_consent:
          description: Whether a consent screen is shown during authorization.
          title: Require Consent
          type: boolean
        software_id:
          anyOf:
          - type: string
          - type: 'null'
          description: RFC 7591 software identifier claimed at registration, if any.
          title: Software Id
        token_endpoint_auth_method:
          description: 'Client authentication method at the token endpoint: ``client_secret_basic`` (confidential) or ``none`` (public, PKCE-only).'
          title: Token Endpoint Auth Method
          type: string
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
      required:
      - id
      - client_id
      - name
      - description
      - redirect_uris
      - allowed_scopes
      - active
      - require_consent
      - token_endpoint_auth_method
      - consent_model
      - registration_source
      - software_id
      - approval_status
      - created_at
      - updated_at
      - created_by
      - client_secret
      title: OAuthClientCreateResponse
      type: object
    OAuthClientDenyRequest:
      description: Request body for denying an OAuth client.
      properties:
        reason:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: Optional reason recorded in the audit trail.
          title: Reason
      title: OAuthClientDenyRequest
      type: object
    OAuthClientListResponse:
      description: A list of OAuth clients.
      properties:
        data:
          items:
            $ref: '#/components/schemas/OAuthClientResponse'
          title: Data
          type: array
      required:
      - data
      title: OAuthClientListResponse
      type: object
    OAuthClientRegistrationRequest:
      description: POST /oauth-clients request body (RFC 7591 client metadata subset).
      properties:
        application_type:
          anyOf:
          - maxLength: 32
            type: string
          - type: 'null'
          description: Accepted and echoed ('native' for desktop/CLI apps, per the 2026-07-28 MCP spec revision); localhost http redirect URIs are allowed regardless.
          title: Application Type
        client_name:
          maxLength: 255
          minLength: 1
          title: Client Name
          type: string
        grant_types:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: Subset of ['authorization_code', 'refresh_token'].
          title: Grant Types
        redirect_uris:
          description: 1-20 redirect URIs. `https` always; `http` for loopback hosts only; RFC 8252 §7.1 private-use (custom) schemes are accepted for native apps (browser-executable and other dangerous schemes are rejected).
          items:
            type: string
          maxItems: 20
          minItems: 1
          title: Redirect Uris
          type: array
        response_types:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: Only ['code'] is supported.
          title: Response Types
        scope:
          anyOf:
          - maxLength: 6500
            type: string
          - type: 'null'
          title: Scope
        software_id:
          anyOf:
          - maxLength: 255
            type: string
          - type: 'null'
          title: Software Id
        software_version:
          anyOf:
          - maxLength: 255
            type: string
          - type: 'null'
          title: Software Version
        token_endpoint_auth_method:
          anyOf:
          - type: string
          - type: 'null'
          description: Must be 'none' if supplied — this endpoint only registers public (secret-less, PKCE-only) clients.
          title: Token Endpoint Auth Method
      required:
      - client_name
      - redirect_uris
      title: OAuthClientRegistrationRequest
      type: object
    OAuthClientRegistrationResponse:
      description: |-
        POST /oauth-clients response (201 created, or 200 on a D8 dedupe hit).

        RFC 7591-compatible: ``client_id`` plus the registered metadata. No
        ``client_secret`` (public clients only) and no
        ``registration_access_token`` (D12 — no RFC 7592 self-management surface;
        clients retry ``/authorize``, they don't poll).
      properties:
        application_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Application Type
        client_id:
          title: Client Id
          type: string
        client_id_issued_at:
          description: Seconds since the Unix epoch at which the client_id was issued.
          title: Client Id Issued At
          type: integer
        client_name:
          title: Client Name
          type: string
        grant_types:
          default:
          - authorization_code
          - refresh_token
          items:
            type: string
          title: Grant Types
          type: array
        redirect_uris:
          items:
            type: string
          title: Redirect Uris
          type: array
        response_types:
          default:
          - code
          items:
            type: string
          title: Response Types
          type: array
        scope:
          description: Space-separated scope ceiling granted to the client (the request's scope capped to the MCP tool-scope set).
          title: Scope
          type: string
        software_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Software Id
        software_version:
          anyOf:
          - type: string
          - type: 'null'
          title: Software Version
        token_endpoint_auth_method:
          default: none
          title: Token Endpoint Auth Method
          type: string
      required:
      - client_id
      - client_id_issued_at
      - client_name
      - redirect_uris
      - scope
      title: OAuthClientRegistrationResponse
      type: object
    OAuthClientResponse:
      description: An OAuth client in API responses.
      examples:
      - active: true
        approval_status: approved
        client_id: oc_abc123...
        consent_model: user
        created_at: '2026-08-18T12:00:00Z'
        created_by: usr_abc123
        description: My application production deployment
        id: oac_2NxYz...
        name: my-app-production
        redirect_uris:
        - https://app.example.com/auth/callback
        registration_source: admin
        require_consent: true
        token_endpoint_auth_method: client_secret_basic
      properties:
        active:
          title: Active
          type: boolean
        active_grant_count:
          default: 0
          description: Number of active consent→agent grants for this client. Computed on the read endpoints (list/get); write-path responses report 0.
          title: Active Grant Count
          type: integer
        allowed_scopes:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: Scopes this client may request. Null means unrestricted.
          title: Allowed Scopes
        approval_status:
          description: 'Admin approval lifecycle: ``pending``, ``approved``, or ``denied``. Only approved clients may enter OAuth flows; ``active`` remains the independent kill switch.'
          title: Approval Status
          type: string
        client_id:
          description: Public client identifier used in OAuth flows.
          title: Client Id
          type: string
        consent_model:
          description: 'What a user''s consent grants for this client: ``user`` or ``agent``.'
          title: Consent Model
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        id:
          description: Internal ID (ksuid).
          title: Id
          type: string
        name:
          title: Name
          type: string
        redirect_uris:
          items:
            type: string
          title: Redirect Uris
          type: array
        registration_source:
          description: 'How the client entered the registry: ``admin`` or ``dcr``.'
          title: Registration Source
          type: string
        require_consent:
          description: Whether a consent screen is shown during authorization.
          title: Require Consent
          type: boolean
        software_id:
          anyOf:
          - type: string
          - type: 'null'
          description: RFC 7591 software identifier claimed at registration, if any.
          title: Software Id
        token_endpoint_auth_method:
          description: 'Client authentication method at the token endpoint: ``client_secret_basic`` (confidential) or ``none`` (public, PKCE-only).'
          title: Token Endpoint Auth Method
          type: string
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
      required:
      - id
      - client_id
      - name
      - description
      - redirect_uris
      - allowed_scopes
      - active
      - require_consent
      - token_endpoint_auth_method
      - consent_model
      - registration_source
      - software_id
      - approval_status
      - created_at
      - updated_at
      - created_by
      title: OAuthClientResponse
      type: object
    OAuthClientRotateSecretResponse:
      description: Returned on secret rotation — the new one-time plaintext secret.
      properties:
        client_secret:
          description: The new client secret. Store it securely; the previous secret is now invalid.
          title: Client Secret
          type: string
          x-sensitive: true
      required:
      - client_secret
      title: OAuthClientRotateSecretResponse
      type: object
    OAuthClientUpdateRequest:
      description: Request body for updating an OAuth client.
      properties:
        active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Active
        allowed_scopes:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: Scope restriction list. Null means no change; an empty list denies all non-OIDC scopes; the wildcard ``["*"]`` resets to unrestricted.
          title: Allowed Scopes
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        name:
          anyOf:
          - maxLength: 255
            type: string
          - type: 'null'
          title: Name
        redirect_uris:
          anyOf:
          - items:
              maxLength: 2048
              type: string
            maxItems: 20
            minItems: 1
            type: array
          - type: 'null'
          title: Redirect Uris
        require_consent:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Require Consent
      title: OAuthClientUpdateRequest
      type: object
    OAuthGrantAdminListResponse:
      description: A paginated list of OAuth grants (admin cross-view).
      properties:
        data:
          items:
            $ref: '#/components/schemas/OAuthGrantAdminResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: OAuthGrantAdminListResponse
      type: object
    OAuthGrantAdminResponse:
      description: One consent→agent grant in the admin cross-view.
      properties:
        agent_id:
          description: The agent this grant binds the client to.
          title: Agent Id
          type: string
        agent_status:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Lifecycle state of the bound agent (`active`, `disabled`, `archived`, …). A grant on a non-active agent is dormant: the row stays `active` (disable is reversible — re-enable restores the standing consent without a new consent round) but no token resolves while the agent is non-active. Lets listings tell a working connection from a dormant one (#1233).'
          title: Agent Status
        can_revoke:
          description: Whether the CALLER may revoke this grant (the consenting user, or an admin holding the revoke permission set). May be false even for callers who can list — e.g. a read-only admin.
          title: Can Revoke
          type: boolean
        client_name:
          anyOf:
          - type: string
          - type: 'null'
          description: Display name of the registered client, if the row still exists.
          title: Client Name
        client_origin:
          anyOf:
          - type: string
          - type: 'null'
          description: Origin (scheme://host) of the client's first redirect URI — the 'authorized apps' display pattern.
          title: Client Origin
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          description: Grant ID (ksuid, `ocg_` prefix).
          title: Id
          type: string
        last_used_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          description: Last time the client obtained tokens under this grant (stamped at exchange/refresh, not per request).
          title: Last Used At
        oauth_client_id:
          description: The client's public client_id — the same identifier stamped on tokens minted under this grant.
          title: Oauth Client Id
          type: string
        revoked_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Revoked At
        scopes:
          description: Scopes granted at consent (the D2 intersection).
          items:
            type: string
          title: Scopes
          type: array
        status:
          description: 'Grant lifecycle state: ``active`` or ``revoked``.'
          title: Status
          type: string
        user_id:
          description: 'The consenting user who approved this grant. Shown even after an agent ownership transfer: the grant stays with the original consenter (it is their consent, not the agent''s).'
          title: User Id
          type: string
      required:
      - id
      - oauth_client_id
      - client_name
      - client_origin
      - user_id
      - agent_id
      - scopes
      - status
      - created_at
      - revoked_at
      - last_used_at
      - can_revoke
      title: OAuthGrantAdminResponse
      type: object
    OAuthGrantListResponse:
      description: A paginated list of OAuth grants.
      properties:
        data:
          items:
            $ref: '#/components/schemas/OAuthGrantResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: OAuthGrantListResponse
      type: object
    OAuthGrantResponse:
      description: One consent→agent grant in API responses.
      properties:
        agent_id:
          description: The agent this grant binds the client to.
          title: Agent Id
          type: string
        agent_status:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Lifecycle state of the bound agent (`active`, `disabled`, `archived`, …). A grant on a non-active agent is dormant: the row stays `active` (disable is reversible — re-enable restores the standing consent without a new consent round) but no token resolves while the agent is non-active. Lets listings tell a working connection from a dormant one (#1233).'
          title: Agent Status
        can_revoke:
          description: Whether the CALLER may revoke this grant (the consenting user, or an admin holding the revoke permission set). May be false even for callers who can list — e.g. the agent's new owner after an ownership transfer, or a read-only admin.
          title: Can Revoke
          type: boolean
        client_name:
          anyOf:
          - type: string
          - type: 'null'
          description: Display name of the registered client, if the row still exists.
          title: Client Name
        client_origin:
          anyOf:
          - type: string
          - type: 'null'
          description: Origin (scheme://host) of the client's first redirect URI — the 'authorized apps' display pattern.
          title: Client Origin
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          description: Grant ID (ksuid, `ocg_` prefix).
          title: Id
          type: string
        last_used_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          description: Last time the client obtained tokens under this grant (stamped at exchange/refresh, not per request).
          title: Last Used At
        oauth_client_id:
          description: The client's public client_id — the same identifier stamped on tokens minted under this grant.
          title: Oauth Client Id
          type: string
        revoked_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Revoked At
        scopes:
          description: Scopes granted at consent (the D2 intersection).
          items:
            type: string
          title: Scopes
          type: array
        status:
          description: 'Grant lifecycle state: ``active`` or ``revoked``.'
          title: Status
          type: string
        user_id:
          description: 'The consenting user who approved this grant. Shown even after an agent ownership transfer: the grant stays with the original consenter (it is their consent, not the agent''s).'
          title: User Id
          type: string
      required:
      - id
      - oauth_client_id
      - client_name
      - client_origin
      - user_id
      - agent_id
      - scopes
      - status
      - created_at
      - revoked_at
      - last_used_at
      - can_revoke
      title: OAuthGrantResponse
      type: object
    OAuthSessionContinueRequest:
      description: |-
        Front-channel session-continue exchange posted from the login page.

        ``state`` is the signed ``login``-purpose carry-through token (``ls``)
        minted by rung 3 of ``flow.resolve_identity_gate`` for this exact
        authorize request — the exchange never accepts bare flow parameters, so a
        caller cannot probe arbitrary client ids through it.

        ``state`` is deliberately NOT marked x-sensitive, for the same reason as
        ``OAuthApprovalDecisionRequest.state``: the CLI's redaction backstop
        unions bare field names globally, and the blob is not a lasting bearer
        credential (HMAC-signed, purpose-discriminated, TTL'd; the endpoint
        additionally requires an authenticated platform user).
      properties:
        state:
          title: State
          type: string
      required:
      - state
      title: OAuthSessionContinueRequest
      type: object
    OAuthSessionContinueResponse:
      description: |-
        The resume leg for a successful session-continue exchange.

        ``redirect_url`` is a same-origin, relative ``/authorize`` URL re-running
        the ORIGINAL authorize request plus the short-TTL ``session``-purpose
        continuation blob (``sc``) pinning the platform user at exchange time.
        Relative by design (mirrors the approval-pending page's resume URL): the
        page script navigates within its own origin, never to a caller-influenced
        absolute URL.
      properties:
        redirect_url:
          title: Redirect Url
          type: string
      required:
      - redirect_url
      title: OAuthSessionContinueResponse
      type: object
    OperationPreviewListResponse:
      description: |-
        Capped, offset-paginated operation preview for a catalog entry.

        Unlike list endpoints (cursor-paginated), the preview uses simple
        offset/limit pagination deliberately: it reads a single, already-fetched spec
        document and is hard-capped at ``PREVIEW_MAX_OPERATIONS`` operations, so there
        is no large/mutating result set that would justify keyset cursors.
      examples:
      - data: []
        info:
          description: Payments.
          title: Stripe
          version: '2024-01-01'
        offset: 0
        security_schemes: {}
        total: 42
        truncated: true
      properties:
        data:
          description: The page of previewed operations.
          items:
            $ref: '#/components/schemas/PreviewOperationResponse'
          title: Data
          type: array
        info:
          $ref: '#/components/schemas/PreviewInfoResponse'
          description: The spec's `info` block.
        offset:
          description: Offset of the returned window.
          title: Offset
          type: integer
        security_schemes:
          additionalProperties:
            additionalProperties: true
            type: object
          description: Slimmed `components.securitySchemes` projection.
          title: Security Schemes
          type: object
        total:
          description: Total operations in the spec (pre-page).
          title: Total
          type: integer
        truncated:
          description: Whether more operations follow this window.
          title: Truncated
          type: boolean
      required:
      - data
      - total
      - offset
      - truncated
      - info
      - security_schemes
      title: OperationPreviewListResponse
      type: object
    OperationResultResponse:
      description: A single search result matching the OperationResult spec.
      properties:
        _links:
          $ref: '#/components/schemas/SearchLinksResponse'
        api:
          $ref: '#/components/schemas/ApiReferenceResponse'
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        method:
          title: Method
          type: string
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        operation_id:
          title: Operation Id
          type: string
        relevance_score:
          title: Relevance Score
          type: number
        type:
          const: operation
          default: operation
          title: Type
          type: string
        url:
          title: Url
          type: string
      required:
      - api
      - operation_id
      - method
      - url
      - relevance_score
      - _links
      title: OperationResultResponse
      type: object
    OperationSummaryLinksResponse:
      description: Hypermedia links for an operation summary.
      properties:
        inspect:
          title: Inspect
          type: string
      required:
      - inspect
      title: OperationSummaryLinksResponse
      type: object
    OperationSummaryListResponse:
      description: Cursor-paginated list of operations.
      properties:
        data:
          items:
            $ref: '#/components/schemas/OperationSummaryResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: OperationSummaryListResponse
      type: object
    OperationSummaryResponse:
      description: Single operation in a paginated list.
      properties:
        _links:
          $ref: '#/components/schemas/OperationSummaryLinksResponse'
        api:
          $ref: '#/components/schemas/ApiReferenceResponse'
        deprecated:
          default: false
          title: Deprecated
          type: boolean
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        method:
          title: Method
          type: string
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        operation_id:
          title: Operation Id
          type: string
        path:
          title: Path
          type: string
        revision_id:
          title: Revision Id
          type: string
        tags:
          items:
            type: string
          title: Tags
          type: array
      required:
      - operation_id
      - method
      - path
      - api
      - revision_id
      - _links
      title: OperationSummaryResponse
      type: object
    OverlayConfirmRequest:
      additionalProperties: false
      description: Payload for confirming an overlay.
      properties:
        execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Id
      title: OverlayConfirmRequest
      type: object
    OverlayLinksResponse:
      description: |-
        Hypermedia links for an overlay resource.

        Links are advertised based on the overlay's *state validity* (mirroring the
        revisions resource's promote/archive links), so a surface renders an action only
        when it is applicable to the current status. They are not permission-scoped — the
        ``overlays:confirm`` gate on confirm/rollback is still enforced server-side (403).
      properties:
        api:
          title: Api
          type: string
        confirm:
          anyOf:
          - type: string
          - type: 'null'
          title: Confirm
        deprecate:
          anyOf:
          - type: string
          - type: 'null'
          title: Deprecate
        rollback:
          anyOf:
          - type: string
          - type: 'null'
          title: Rollback
        self:
          title: Self
          type: string
      required:
      - self
      - api
      title: OverlayLinksResponse
      type: object
    OverlayListResponse:
      description: Cursor-paginated list of overlays.
      properties:
        data:
          items:
            $ref: '#/components/schemas/OverlayResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: OverlayListResponse
      type: object
    OverlayResponse:
      description: Full overlay resource response.
      properties:
        _links:
          $ref: '#/components/schemas/OverlayLinksResponse'
        api_id:
          title: Api Id
          type: string
        confirmed_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Confirmed At
        confirmed_by_execution_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Confirmed By Execution Id
        confirmed_revision_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Confirmed Revision Id
        contributed_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Contributed By
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By
        deprecated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Deprecated At
        deprecated_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Deprecated Reason
        document:
          additionalProperties: true
          title: Document
          type: object
        id:
          title: Id
          type: string
        status:
          title: Status
          type: string
        superseded_revision_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Superseded Revision Id
        target_revision_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Revision Id
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
      required:
      - id
      - api_id
      - status
      - document
      - target_revision_id
      - contributed_by
      - confirmed_by_execution_id
      - created_at
      - updated_at
      - confirmed_at
      - deprecated_at
      - _links
      title: OverlayResponse
      type: object
    OverlaySubmitRequest:
      additionalProperties: false
      description: Payload for submitting a new overlay.
      properties:
        contributed_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Contributed By
        document:
          additionalProperties: true
          title: Document
          type: object
        target_revision_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Revision Id
      required:
      - document
      title: OverlaySubmitRequest
      type: object
    OverlayUpdateRequest:
      additionalProperties: false
      description: Payload for updating an existing overlay.
      properties:
        document:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Document
        target_revision_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Revision Id
      title: OverlayUpdateRequest
      type: object
    PermissionListResponse:
      description: List of available permissions.
      properties:
        data:
          items:
            $ref: '#/components/schemas/PermissionResponse'
          title: Data
          type: array
      required:
      - data
      title: PermissionListResponse
      type: object
    PermissionResponse:
      description: A single permission entry from the catalogue.
      properties:
        description:
          title: Description
          type: string
        grantable_by_caller:
          title: Grantable By Caller
          type: boolean
        implies:
          items:
            type: string
          title: Implies
          type: array
        name:
          title: Name
          type: string
      required:
      - name
      - description
      - implies
      - grantable_by_caller
      title: PermissionResponse
      type: object
    PermissionRuleListResponse:
      description: List of permission rules.
      properties:
        data:
          items:
            $ref: '#/components/schemas/PermissionRuleReadSchema'
          title: Data
          type: array
      required:
      - data
      title: PermissionRuleListResponse
      type: object
    PermissionRuleReadSchema:
      description: Permission rule response (includes system fields).
      properties:
        _comment:
          anyOf:
          - type: string
          - type: 'null'
          title: Comment
        _system:
          default: false
          title: System
          type: boolean
        effect:
          enum:
          - allow
          - deny
          title: Effect
          type: string
        match_mode:
          default: regex
          enum:
          - regex
          - prefix
          - exact
          title: Match Mode
          type: string
        methods:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Methods
        operations:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Operations
        path:
          anyOf:
          - type: string
          - type: 'null'
          title: Path
      required:
      - effect
      title: PermissionRuleReadSchema
      type: object
    PermissionTestRequest:
      additionalProperties: false
      description: Request body for :test — dry-run a request shape against the binding's rules.
      properties:
        method:
          description: HTTP method of the hypothetical request (case-insensitive).
          title: Method
          type: string
        operation_id:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional OpenAPI operation id resolved from the request URL.
          title: Operation Id
        path:
          description: Path of the hypothetical request as the broker would see it.
          title: Path
          type: string
      required:
      - method
      - path
      title: PermissionTestRequest
      type: object
    PermissionTestResponse:
      description: Dry-run result matching :class:`PermissionTestResult`.
      properties:
        allowed:
          description: Whether the broker would allow this request under the binding's rules.
          title: Allowed
          type: boolean
        credential_id:
          anyOf:
          - type: string
          - type: 'null'
          description: The binding whose rule list contributed the matching rule.
          title: Credential Id
        effect:
          anyOf:
          - type: string
          - type: 'null'
          description: Effect of the matching rule (`allow`/`deny`); null when no match.
          title: Effect
        is_system:
          anyOf:
          - type: boolean
          - type: 'null'
          description: True when the matching rule was written by the system; null when no match.
          title: Is System
        matched:
          description: Whether any rule matched; when false, the outcome is default-deny.
          title: Matched
          type: boolean
        rule_index:
          anyOf:
          - type: integer
          - type: 'null'
          description: Zero-based index in the binding's ordered rule list; null when no match.
          title: Rule Index
      required:
      - allowed
      - matched
      title: PermissionTestResponse
      type: object
    Permissions:
      description: 'Structured permissions view: assigned + effective.'
      properties:
        assigned:
          items:
            type: string
          title: Assigned
          type: array
        effective:
          items:
            $ref: '#/components/schemas/EffectivePermission'
          title: Effective
          type: array
      required:
      - assigned
      - effective
      title: Permissions
      type: object
    PermissionsPatchRequest:
      description: Patch permission rules — add and/or remove.
      properties:
        add:
          anyOf:
          - items:
              $ref: '#/components/schemas/jentic_one__control__web__schemas__permission_rules__PermissionRuleSchema'
            type: array
          - type: 'null'
          title: Add
        remove:
          anyOf:
          - items:
              type: integer
            type: array
          - type: 'null'
          title: Remove
      title: PermissionsPatchRequest
      type: object
    PreviewInfoResponse:
      description: The `info` block fields surfaced in a preview.
      examples:
      - description: Payments.
        title: Stripe
        version: '2024-01-01'
      properties:
        description:
          anyOf:
          - type: string
          - type: 'null'
          description: Spec description.
          title: Description
        title:
          anyOf:
          - type: string
          - type: 'null'
          description: Spec title.
          title: Title
        version:
          anyOf:
          - type: string
          - type: 'null'
          description: Spec version.
          title: Version
      required:
      - title
      - version
      - description
      title: PreviewInfoResponse
      type: object
    PreviewOperationResponse:
      description: A slimmed operation in a catalog spec preview.
      examples:
      - description: ''
        method: GET
        operation_id: listCharges
        parameters: []
        path: /v1/charges
        security:
        - BearerAuth
        summary: List charges
        tags:
        - charges
      properties:
        description:
          description: Operation description (empty string when absent).
          title: Description
          type: string
        method:
          description: Upper-case HTTP method.
          title: Method
          type: string
        operation_id:
          anyOf:
          - type: string
          - type: 'null'
          description: OpenAPI operationId, when declared.
          title: Operation Id
        parameters:
          description: Merged path- and operation-level parameters.
          items:
            $ref: '#/components/schemas/PreviewParameterResponse'
          title: Parameters
          type: array
        path:
          description: Operation path template.
          title: Path
          type: string
        security:
          description: Flattened names of the security schemes that apply.
          items:
            type: string
          title: Security
          type: array
        summary:
          description: Operation summary (empty string when absent).
          title: Summary
          type: string
        tags:
          description: Operation tags.
          items:
            type: string
          title: Tags
          type: array
      required:
      - method
      - path
      - summary
      - description
      - operation_id
      - parameters
      - security
      - tags
      title: PreviewOperationResponse
      type: object
    PreviewParameterResponse:
      description: A slimmed parameter in an operation preview.
      examples:
      - description: Page size.
        in: query
        name: limit
        required: false
      properties:
        description:
          description: Parameter description (empty string when absent).
          title: Description
          type: string
        in:
          description: OpenAPI parameter location.
          title: In
          type: string
        name:
          description: Parameter name.
          title: Name
          type: string
        required:
          description: Whether the parameter is required.
          title: Required
          type: boolean
      required:
      - name
      - in
      - required
      - description
      title: PreviewParameterResponse
      type: object
    ProblemDetail:
      description: |-
        RFC 9457 Problem Details for HTTP APIs.

        This is the standard error response format for all Jentic APIs.
        Content-Type: application/problem+json

        See: https://www.rfc-editor.org/rfc/rfc9457.html
      examples:
      - detail: The request body is missing one or more required fields.
        errors:
        - detail: Field 'name' is required.
          pointer: '#/name'
        instance: /v2/capability-sets
        status: 400
        title: Bad Request
        type: about:blank
      properties:
        code:
          anyOf:
          - maxLength: 50
            type: string
          - type: 'null'
          description: An optional provider-specific code for internal error taxonomy and observability correlation.
          examples:
          - JENTIC-4001
          title: Code
        detail:
          description: A human-readable explanation specific to this occurrence of the problem. MUST be present. Provide actionable information where possible.
          examples:
          - The request body is missing required field 'name'.
          maxLength: 4096
          title: Detail
          type: string
        errors:
          anyOf:
          - items:
              $ref: '#/components/schemas/ErrorItem'
            maxItems: 1000
            type: array
          - type: 'null'
          description: An array of granular error details. Use when multiple validation errors or field-level problems need to be surfaced in a single response.
          title: Errors
        instance:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: A URI reference identifying the specific occurrence of the problem. Typically the request path.
          examples:
          - /v2/capability-sets
          title: Instance
        status:
          anyOf:
          - maximum: 599.0
            minimum: 100.0
            type: integer
          - type: 'null'
          description: The HTTP status code for this occurrence of the problem.
          examples:
          - 400
          title: Status
        title:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          description: A short, human-readable summary of the problem type. Should not change between occurrences except for localisation purposes.
          examples:
          - Bad Request
          title: Title
        type:
          default: about:blank
          description: A URI reference identifying the problem type. When set to 'about:blank', the title SHOULD be the standard HTTP status phrase. Use an IANA-registered type URI where one applies.
          examples:
          - about:blank
          maxLength: 1024
          title: Type
          type: string
      required:
      - detail
      title: ProblemDetail
      type: object
    ProviderConfigListResponse:
      description: A list of stored provider configs (secrets redacted).
      properties:
        data:
          items:
            $ref: '#/components/schemas/ProviderConfigResponse'
          title: Data
          type: array
      required:
      - data
      title: ProviderConfigListResponse
      type: object
    ProviderConfigResponse:
      description: A stored provider config with secret fields redacted.
      examples:
      - config:
          client_id: client_abc123
          client_secret: '***'
          environment: production
          kind: pipedream
          project_id: proj_abc123
        created_at: '2026-06-29T12:00:00Z'
        name: pipedream
        updated_at: '2026-06-29T12:00:00Z'
      properties:
        config:
          additionalProperties: true
          description: Stored config with secret fields redacted.
          title: Config
          type: object
        created_at:
          format: date-time
          title: Created At
          type: string
        name:
          description: Provider name (e.g. 'pipedream').
          title: Name
          type: string
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
      required:
      - name
      - config
      - created_at
      title: ProviderConfigResponse
      type: object
    ProviderConfigSetRequest:
      description: |-
        Request body for setting a provider config.

        Fields are provider-specific and validated server-side by provider name.
        For ``pipedream`` the recognised fields are ``project_id``, ``client_id``,
        ``client_secret`` (write-only), and optional ``environment``,
        ``connect_base_url``, ``expiry_skew_seconds``.
      examples:
      - client_id: client_abc123
        client_secret: <redacted>
        environment: production
        project_id: proj_abc123
      properties:
        config:
          additionalProperties: true
          description: Provider-specific configuration fields, validated by provider name.
          title: Config
          type: object
      required:
      - config
      title: ProviderConfigSetRequest
      type: object
    ProviderDiscoveryEntryResponse:
      description: Discovery metadata for a single credential provider.
      properties:
        callback_url:
          anyOf:
          - type: string
          - type: 'null'
          description: OAuth2 redirect URI for providers that require it.
          title: Callback Url
        configured:
          description: Whether the provider is fully configured and operational.
          title: Configured
          type: boolean
        id:
          description: Provider identifier (registry key).
          title: Id
          type: string
        label:
          description: Human-readable provider name.
          title: Label
          type: string
        managed:
          description: Whether the provider handles vendor sign-in on behalf of the user.
          title: Managed
          type: boolean
        types:
          description: Wire-level credential types this provider supports.
          items:
            $ref: '#/components/schemas/CredentialType'
          title: Types
          type: array
      required:
      - id
      - label
      - managed
      - types
      - configured
      title: ProviderDiscoveryEntryResponse
      type: object
    ProviderDiscoveryResponse:
      description: Discovery response listing all available credential providers.
      properties:
        providers:
          items:
            $ref: '#/components/schemas/ProviderDiscoveryEntryResponse'
          title: Providers
          type: array
      required:
      - providers
      title: ProviderDiscoveryResponse
      type: object
    RedeemInviteRequest:
      description: Payload for redeeming an invite token.
      properties:
        invite_token:
          maxLength: 512
          minLength: 1
          title: Invite Token
          type: string
          x-sensitive: true
        password:
          maxLength: 128
          minLength: 12
          title: Password
          type: string
          x-sensitive: true
      required:
      - invite_token
      - password
      title: RedeemInviteRequest
      type: object
    RegisterRequest:
      description: POST /register request body.
      properties:
        client_name:
          maxLength: 255
          minLength: 1
          title: Client Name
          type: string
        grant_types:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Grant Types
        jwks:
          additionalProperties: true
          description: A JSON Web Key Set containing at least one Ed25519 public key (kty=OKP, crv=Ed25519). RSA and other key types are not accepted.
          example:
            keys:
            - crv: Ed25519
              kid: agent-key-1
              kty: OKP
              x: 11qYAYKxCrfVS_7TyWQHOg7hcvPapiMlrwIaaPcHURo
          title: Jwks
          type: object
        scope:
          anyOf:
          - maxLength: 6500
            type: string
          - type: 'null'
          title: Scope
        token_endpoint_auth_method:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Endpoint Auth Method
      required:
      - client_name
      - jwks
      title: RegisterRequest
      type: object
    RegisterResponse:
      description: POST /register 201 response.
      properties:
        claim_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Claim Token
          x-sensitive: true
        client_id:
          title: Client Id
          type: string
        grant_types:
          default:
          - urn:ietf:params:oauth:grant-type:jwt-bearer
          items:
            type: string
          title: Grant Types
          type: array
        registration_access_token:
          title: Registration Access Token
          type: string
          x-sensitive: true
        registration_client_uri:
          title: Registration Client Uri
          type: string
        status:
          title: Status
          type: string
        token_endpoint_auth_method:
          default: private_key_jwt
          title: Token Endpoint Auth Method
          type: string
      required:
      - client_id
      - registration_access_token
      - registration_client_uri
      - status
      title: RegisterResponse
      type: object
    RegistrationStatusResponse:
      description: GET /register/{agent_id} response.
      properties:
        client_id:
          title: Client Id
          type: string
        grant_types:
          default:
          - urn:ietf:params:oauth:grant-type:jwt-bearer
          items:
            type: string
          title: Grant Types
          type: array
        status:
          title: Status
          type: string
        token_endpoint_auth_method:
          default: private_key_jwt
          title: Token Endpoint Auth Method
          type: string
      required:
      - client_id
      - status
      title: RegistrationStatusResponse
      type: object
    RevokeRequest:
      description: |-
        Revocation endpoint request (RFC 7009) — JSON or form-encoded.

        ``token`` is required either way. ``token_type_hint``
        (``access_token``/``refresh_token``) is a lookup-order optimization only —
        the server falls through both types regardless (RFC 7009 §2.1).
        ``client_id`` belongs to the form-encoded public-client arm (G11): the
        secret-less client's lineage binding; ignored on the bearer-authenticated
        JSON arm, where the platform identity scopes the revocation instead.
      properties:
        client_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Client Id
        token:
          title: Token
          type: string
          x-sensitive: true
        token_type_hint:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Type Hint
      required:
      - token
      title: RevokeRequest
      type: object
    RuleSetAttachRequest:
      description: Point a direct agent↔credential binding at a shared rule set.
      properties:
        rule_set_id:
          description: Id (`prs_…`) of the rule set to attach.
          title: Rule Set Id
          type: string
      required:
      - rule_set_id
      title: RuleSetAttachRequest
      type: object
    RuleSetCreateRequest:
      description: Create a shared permission rule set (theme 5 phase 1, Q-04).
      properties:
        description:
          anyOf:
          - maxLength: 1000
            type: string
          - type: 'null'
          title: Description
        name:
          description: Unique human-readable name.
          maxLength: 255
          title: Name
          type: string
        rules:
          description: Initial ordered rule list (first-match-wins, default-deny).
          items:
            $ref: '#/components/schemas/jentic_one__control__web__schemas__permission_rules__PermissionRuleSchema'
          maxItems: 100
          title: Rules
          type: array
      required:
      - name
      title: RuleSetCreateRequest
      type: object
    RuleSetListResponse:
      description: Paginated list of rule sets.
      properties:
        data:
          items:
            $ref: '#/components/schemas/RuleSetSummaryResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: RuleSetListResponse
      type: object
    RuleSetResponse:
      description: Rule set detail — the ordered rules plus its referencing-binding count.
      properties:
        binding_count:
          description: How many agent-credential bindings currently point at this set.
          title: Binding Count
          type: integer
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        name:
          title: Name
          type: string
        rule_set_id:
          title: Rule Set Id
          type: string
        rules:
          items:
            $ref: '#/components/schemas/PermissionRuleReadSchema'
          title: Rules
          type: array
      required:
      - rule_set_id
      - name
      - rules
      - binding_count
      - created_at
      title: RuleSetResponse
      type: object
    RuleSetSummaryResponse:
      description: Rule set list entry.
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        name:
          title: Name
          type: string
        rule_count:
          title: Rule Count
          type: integer
        rule_set_id:
          title: Rule Set Id
          type: string
      required:
      - rule_set_id
      - name
      - rule_count
      - created_at
      title: RuleSetSummaryResponse
      type: object
    RuleSetUpdateRequest:
      description: Rename or re-describe a rule set.
      properties:
        description:
          anyOf:
          - maxLength: 1000
            type: string
          - type: 'null'
          title: Description
        name:
          anyOf:
          - maxLength: 255
            type: string
          - type: 'null'
          title: Name
      title: RuleSetUpdateRequest
      type: object
    RuntimeConfig:
      description: Optional per-upstream-call overrides.
      properties:
        headers:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Headers
        query_params:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Query Params
      title: RuntimeConfig
      type: object
    SearchLinksResponse:
      description: Hypermedia links for a search result row.
      properties:
        inspect:
          title: Inspect
          type: string
      required:
      - inspect
      title: SearchLinksResponse
      type: object
    SearchRequest:
      additionalProperties: false
      description: POST /search request body.
      properties:
        apis:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: Restrict results to these APIs. Each entry is a 'vendor[/name[/version]]' identifier of an imported API (e.g. 'github-com/api-github-com/1.1.4'); vendor and name are normalized like ingest, so raw spellings such as 'stripe.com/api' also resolve. The legacy colon-separated form 'vendor[:name[:version]]' is also accepted.
          title: Apis
        cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
        limit:
          default: 10
          maximum: 100.0
          minimum: 1.0
          title: Limit
          type: integer
        query:
          minLength: 1
          title: Query
          type: string
        revision_pins:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          description: Pin specific APIs to a revision for this search. Keys are full 'vendor/name/version' identifiers (colon-separated also accepted); values are revision UUIDs.
          title: Revision Pins
      required:
      - query
      title: SearchRequest
      type: object
    SearchResponse:
      description: Cursor-paginated search results page.
      properties:
        data:
          items:
            $ref: '#/components/schemas/OperationResultResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: SearchResponse
      type: object
    SecuritySchemeFlowResponse:
      description: A single OAuth2 flow within a security scheme.
      properties:
        authorization_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorization Url
        flow_type:
          title: Flow Type
          type: string
        refresh_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Refresh Url
        scopes:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Scopes
        token_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Url
      required:
      - flow_type
      title: SecuritySchemeFlowResponse
      type: object
    SecuritySchemeListResponse:
      description: List of security schemes for an API's current revision.
      properties:
        data:
          items:
            $ref: '#/components/schemas/SecuritySchemeResponse'
          title: Data
          type: array
      required:
      - data
      title: SecuritySchemeListResponse
      type: object
    SecuritySchemeResponse:
      description: Full security scheme detail for a revision.
      properties:
        bearer_format:
          anyOf:
          - type: string
          - type: 'null'
          title: Bearer Format
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        flows:
          items:
            $ref: '#/components/schemas/SecuritySchemeFlowResponse'
          title: Flows
          type: array
        in_location:
          anyOf:
          - type: string
          - type: 'null'
          title: In Location
        name:
          title: Name
          type: string
        open_id_connect_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Open Id Connect Url
        param_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Param Name
        scheme:
          anyOf:
          - type: string
          - type: 'null'
          title: Scheme
        type:
          title: Type
          type: string
      required:
      - name
      - type
      title: SecuritySchemeResponse
      type: object
    ServedApiRef:
      description: |-
        An API served by a toolkit's bound credential, keyed by its stored identity.

        Distinct from ``APIReference`` on purpose: this carries the *stored* credential
        identity, where ``api_name``/``api_version`` may be NULL (the "covers all
        names/versions" wildcard, #775) — so they're optional here, unlike the strict
        all-required ``APIReference``. Shared so the auth service schema and the
        ``/me`` web schema use ONE model instead of two identical copies.
      properties:
        api_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Name
        api_vendor:
          title: Api Vendor
          type: string
        api_version:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Version
      required:
      - api_vendor
      title: ServedApiRef
      type: object
    ServiceAccountCreateRequest:
      description: Request body for creating a service account.
      properties:
        description:
          anyOf:
          - maxLength: 1024
            type: string
          - type: 'null'
          title: Description
        name:
          maxLength: 255
          minLength: 1
          title: Name
          type: string
        scopes:
          anyOf:
          - items:
              maxLength: 64
              minLength: 1
              pattern: ^[a-zA-Z0-9_:./-]+$
              type: string
            maxItems: 100
            type: array
          - type: 'null'
          title: Scopes
      required:
      - name
      title: ServiceAccountCreateRequest
      type: object
    ServiceAccountListResponse:
      description: List of service accounts.
      properties:
        data:
          items:
            $ref: '#/components/schemas/ServiceAccountResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: ServiceAccountListResponse
      type: object
    ServiceAccountResponse:
      description: ServiceAccount representation in API responses.
      properties:
        approved_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Approved At
        approved_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Approved By
        created_at:
          format: date-time
          title: Created At
          type: string
        denial_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Denial Reason
        denied_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Denied By
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        id:
          title: Id
          type: string
        name:
          title: Name
          type: string
        owner_id:
          title: Owner Id
          type: string
        registered_by:
          title: Registered By
          type: string
        status:
          title: Status
          type: string
      required:
      - id
      - name
      - owner_id
      - registered_by
      - status
      - created_at
      title: ServiceAccountResponse
      type: object
    ServiceAccountScopesRequest:
      description: Request body for replacing a service account's scopes.
      properties:
        scopes:
          items:
            maxLength: 64
            minLength: 1
            pattern: ^[a-zA-Z0-9_:./-]+$
            type: string
          maxItems: 100
          title: Scopes
          type: array
      required:
      - scopes
      title: ServiceAccountScopesRequest
      type: object
    ServiceAccountScopesResponse:
      description: Response containing a service account's current scopes.
      properties:
        scopes:
          items:
            type: string
          title: Scopes
          type: array
      required:
      - scopes
      title: ServiceAccountScopesResponse
      type: object
    SetPermissionsRequest:
      description: Request body for setting user permissions.
      properties:
        permissions:
          items:
            type: string
          maxItems: 50
          title: Permissions
          type: array
      required:
      - permissions
      title: SetPermissionsRequest
      type: object
    Sigv4CreateRequest:
      description: Create request for sigv4 (AWS Signature V4) credentials.
      properties:
        access_key_id:
          description: AWS access key id (public identifier).
          title: Access Key Id
          type: string
        api:
          $ref: '#/components/schemas/APIReferenceRequest'
          description: Loose (vendor, name, version) API identity tuple.
        aws_region:
          description: Signing region, e.g. 'us-east-1'.
          title: Aws Region
          type: string
        aws_service:
          description: Signing service, e.g. 'aoss', 'execute-api', 's3'.
          title: Aws Service
          type: string
        name:
          description: Human-readable label for the credential.
          title: Name
          type: string
        provider:
          default: static
          description: Credential provider; 'static' for stored secrets.
          title: Provider
          type: string
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        secret_access_key:
          description: AWS secret access key. Stored encrypted; never returned after create.
          title: Secret Access Key
          type: string
          x-sensitive: true
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        session_token:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional temporary-credential session token (STS). Expires; re-save when it does.
          title: Session Token
          x-sensitive: true
        type:
          const: sigv4
          title: Type
          type: string
      required:
      - type
      - name
      - api
      - access_key_id
      - secret_access_key
      - aws_region
      - aws_service
      title: Sigv4CreateRequest
      type: object
    Sigv4UpdateRequest:
      description: Update request for sigv4 credentials (key rotation / scope edit).
      properties:
        access_key_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Access Key Id
        active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Active
        aws_region:
          anyOf:
          - type: string
          - type: 'null'
          title: Aws Region
        aws_service:
          anyOf:
          - type: string
          - type: 'null'
          title: Aws Service
        clear_session_token:
          default: false
          title: Clear Session Token
          type: boolean
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        runtime_config:
          anyOf:
          - $ref: '#/components/schemas/RuntimeConfig'
          - type: 'null'
        secret_access_key:
          anyOf:
          - type: string
          - type: 'null'
          title: Secret Access Key
          x-sensitive: true
        server_variables:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Server Variables
        session_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Token
          x-sensitive: true
        type:
          const: sigv4
          title: Type
          type: string
      required:
      - type
      title: Sigv4UpdateRequest
      type: object
    TokenResponse:
      description: Token endpoint success response.
      properties:
        access_token:
          title: Access Token
          type: string
          x-sensitive: true
        expires_in:
          title: Expires In
          type: integer
        id_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Id Token
          x-sensitive: true
        refresh_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Refresh Token
          x-sensitive: true
        scope:
          anyOf:
          - type: string
          - type: 'null'
          description: Space-delimited effective scopes of the minted access token (RFC 6749 §3.3), computed the way the platform's resolvers enforce them (live scope grants ∩ client ceiling ∩ consent-grant scopes for agent and service-account tokens), so the granted set may be narrower than requested and clients must not assume they got what they asked for. Present on every response whose token carries at least one scope; OMITTED (never the ABNF-invalid empty string) only when the effective set is empty — reachable solely on legs where the client requested no scopes at the token endpoint (the token request carries no scope parameter, and consent fails closed on an empty intersection).
          title: Scope
        token_type:
          default: bearer
          title: Token Type
          type: string
      required:
      - access_token
      - expires_in
      title: TokenResponse
      type: object
    ToolkitBindingEntry:
      description: Toolkit binding summary for the /me response.
      properties:
        bound_at:
          format: date-time
          title: Bound At
          type: string
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        serves:
          default: []
          items:
            $ref: '#/components/schemas/ServedApiRef'
          title: Serves
          type: array
        toolkit_id:
          title: Toolkit Id
          type: string
      required:
      - toolkit_id
      - bound_at
      title: ToolkitBindingEntry
      type: object
    TopOperation:
      description: Aggregated execution counts for a single operation.
      properties:
        api_name:
          title: Api Name
          type: string
        api_vendor:
          title: Api Vendor
          type: string
        failed:
          title: Failed
          type: integer
        operation_id:
          title: Operation Id
          type: string
        total:
          title: Total
          type: integer
      required:
      - api_vendor
      - api_name
      - operation_id
      - total
      - failed
      title: TopOperation
      type: object
    UsageBucket:
      description: Execution counts for a single time bucket.
      properties:
        avg_ms:
          title: Avg Ms
          type: number
        failed:
          title: Failed
          type: integer
        success:
          title: Success
          type: integer
        total:
          title: Total
          type: integer
        ts:
          title: Ts
          type: integer
      required:
      - ts
      - total
      - success
      - failed
      - avg_ms
      title: UsageBucket
      type: object
    UsageResponse:
      description: Full usage statistics response.
      properties:
        bucket_seconds:
          title: Bucket Seconds
          type: integer
        buckets:
          items:
            $ref: '#/components/schemas/UsageBucket'
          title: Buckets
          type: array
        group_by:
          title: Group By
          type: string
        since:
          title: Since
          type: integer
        stats:
          $ref: '#/components/schemas/UsageStatsBlock'
        top:
          items:
            $ref: '#/components/schemas/UsageTopRow'
          title: Top
          type: array
        until:
          title: Until
          type: integer
      required:
      - since
      - until
      - bucket_seconds
      - group_by
      - stats
      - buckets
      - top
      title: UsageResponse
      type: object
    UsageStatsBlock:
      description: Overall usage statistics for a time window.
      properties:
        active_now:
          title: Active Now
          type: integer
        avg_ms:
          title: Avg Ms
          type: number
        failed:
          title: Failed
          type: integer
        p50_ms:
          anyOf:
          - type: number
          - type: 'null'
          title: P50 Ms
        p95_ms:
          anyOf:
          - type: number
          - type: 'null'
          title: P95 Ms
        pending:
          title: Pending
          type: integer
        success:
          title: Success
          type: integer
        total:
          title: Total
          type: integer
      required:
      - total
      - success
      - failed
      - pending
      - avg_ms
      - p50_ms
      - p95_ms
      - active_now
      title: UsageStatsBlock
      type: object
    UsageTopRow:
      description: Top entity row with sparkline trend.
      properties:
        avg_ms:
          title: Avg Ms
          type: number
        failed:
          title: Failed
          type: integer
        key:
          title: Key
          type: string
        label:
          title: Label
          type: string
        success:
          title: Success
          type: integer
        total:
          title: Total
          type: integer
        trend:
          items:
            type: integer
          title: Trend
          type: array
      required:
      - key
      - label
      - total
      - success
      - failed
      - avg_ms
      - trend
      title: UsageTopRow
      type: object
    UserCreateRequest:
      description: Request body for creating a new user.
      properties:
        email:
          maxLength: 320
          minLength: 1
          title: Email
          type: string
        first_name:
          maxLength: 255
          minLength: 1
          title: First Name
          type: string
        last_name:
          maxLength: 255
          minLength: 1
          title: Last Name
          type: string
        permissions:
          default: []
          items:
            type: string
          title: Permissions
          type: array
      required:
      - email
      - first_name
      - last_name
      title: UserCreateRequest
      type: object
    UserCreatedResponse:
      description: Response after user creation including invite token.
      properties:
        invite_expires_at:
          format: date-time
          title: Invite Expires At
          type: string
        invite_token:
          title: Invite Token
          type: string
          x-sensitive: true
        user:
          $ref: '#/components/schemas/UserResponse'
      required:
      - user
      - invite_token
      - invite_expires_at
      title: UserCreatedResponse
      type: object
    UserListResponse:
      description: Paginated list of users.
      properties:
        data:
          items:
            $ref: '#/components/schemas/UserResponse'
          title: Data
          type: array
        has_more:
          title: Has More
          type: boolean
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - data
      - has_more
      title: UserListResponse
      type: object
    UserResponse:
      description: User representation in API responses.
      properties:
        active:
          title: Active
          type: boolean
        auth_provider:
          title: Auth Provider
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        email:
          title: Email
          type: string
        external_subject_id:
          anyOf:
          - type: string
          - type: 'null'
          title: External Subject Id
        first_name:
          title: First Name
          type: string
        id:
          title: Id
          type: string
        invite_state:
          title: Invite State
          type: string
        last_name:
          title: Last Name
          type: string
        must_change_password:
          title: Must Change Password
          type: boolean
        name:
          title: Name
          type: string
        permissions:
          $ref: '#/components/schemas/Permissions'
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
      required:
      - id
      - email
      - first_name
      - last_name
      - name
      - active
      - auth_provider
      - invite_state
      - must_change_password
      - permissions
      - created_at
      title: UserResponse
      type: object
    UserUpdateRequest:
      description: Request body for updating a user.
      properties:
        email:
          anyOf:
          - maxLength: 320
            minLength: 1
            type: string
          - type: 'null'
          title: Email
        first_name:
          anyOf:
          - maxLength: 255
            minLength: 1
            type: string
          - type: 'null'
          title: First Name
        last_name:
          anyOf:
          - maxLength: 255
            minLength: 1
            type: string
          - type: 'null'
          title: Last Name
      title: UserUpdateRequest
      type: object
    VersionResponse:
      description: The running app version and the latest release known to this backend.
      properties:
        current:
          description: The version of jentic-one currently running on this backend.
          title: Current
          type: string
        latest:
          anyOf:
          - type: string
          - type: 'null'
          description: The latest published release of jentic-one, without a leading 'v'. Null when the backend can't determine it (update check disabled, air-gapped, a remote backend, or GitHub was unreachable).
          title: Latest
        update_available:
          description: True when `latest` is a newer release than `current`. Matches the verdict `jenticctl update` would print.
          title: Update Available
          type: boolean
      required:
      - current
      - update_available
      title: VersionResponse
      type: object
    jentic_one__auth__web__schemas__agents__DenyRequest:
      description: Request body for denying an agent.
      properties:
        reason:
          maxLength: 1024
          minLength: 1
          title: Reason
          type: string
      required:
      - reason
      title: DenyRequest
      type: object
    jentic_one__auth__web__schemas__service_accounts__DenyRequest:
      description: Request body for denying a service account.
      properties:
        reason:
          maxLength: 1024
          minLength: 1
          title: Reason
          type: string
      required:
      - reason
      title: DenyRequest
      type: object
    jentic_one__control__web__schemas__access_requests__PermissionRuleSchema:
      additionalProperties: false
      description: Permission rule for an access request item.
      properties:
        effect:
          enum:
          - allow
          - deny
          - require-approval
          title: Effect
          type: string
        match_mode:
          default: regex
          description: 'How `path` is interpreted: `regex` (full-match), `prefix` (string prefix), or `exact` (equality). Defaults to `regex` for backwards compatibility.'
          enum:
          - regex
          - prefix
          - exact
          title: Match Mode
          type: string
        methods:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: HTTP methods to match (case-insensitive). None matches all.
          title: Methods
        operations:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: OpenAPI operation IDs to match. None matches all operations.
          title: Operations
        path:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Path pattern to match. Interpreted per `match_mode`: `regex` uses full-match semantics (the pattern must describe the whole path); `prefix` and `exact` are literal. None matches all paths.'
          title: Path
      required:
      - effect
      title: PermissionRuleSchema
      type: object
    jentic_one__control__web__schemas__permission_rules__PermissionRuleSchema:
      additionalProperties: false
      description: |-
        Permission rule for an agent↔credential binding.

        Rules are evaluated first-match-wins. If no rule matches, the request is
        denied (default-deny). A binding with zero rules therefore blocks all
        operations — users must explicitly add at least one allow rule.
      properties:
        effect:
          description: Whether this rule allows or denies the matched request.
          enum:
          - allow
          - deny
          title: Effect
          type: string
        match_mode:
          default: regex
          description: 'How `path` is interpreted: `regex` (full-match), `prefix` (string prefix), or `exact` (equality). Defaults to `regex` for backwards compatibility.'
          enum:
          - regex
          - prefix
          - exact
          title: Match Mode
          type: string
        methods:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: HTTP methods to match (case-insensitive). None matches all.
          title: Methods
        operations:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: OpenAPI operation IDs to match. None matches all operations.
          title: Operations
        path:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Path pattern to match. Interpreted per `match_mode`: `regex` uses full-match semantics (the pattern must describe the whole path); `prefix` and `exact` are literal. None matches all paths.'
          title: Path
      required:
      - effect
      title: PermissionRuleSchema
      type: object
  securitySchemes:
    BearerAuth:
      description: |-
        Opaque bearer access token (prefixed `at_`), sent as `Authorization: Bearer <token>`. It is **not** a JWT — it is a random string validated server-side by lookup, so it cannot be decoded by the client.

        Obtain one from `POST /oauth/token` using the grant for your actor type:
        - **Agents** — `grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer` with a JWT assertion signed by the key registered via `POST /register` (the JWT is the *assertion*, not the resulting access token).
        - **Service accounts** — `grant_type=client_credentials` with `client_id` + `client_secret`.
        - **Users** — `grant_type=authorization_code` (interactive) or `grant_type=password`; refresh either with `grant_type=refresh_token`.

        Service accounts can also mint short-lived, scope-narrowed task tokens for agents via `POST /oauth/mint`.

        Per-endpoint scope and actor-type requirements are not modelled in this document (OpenAPI cannot faithfully express the OR-of-scopes / `org:admin` bypass / service-layer enforcement); see `GET /reference/endpoints.json` for the authoritative authorization reference.
      scheme: bearer
      type: http
info:
  contact:
    email: support@jentic.com
    name: Jentic Platform Team
    url: https://jentic.com/
  description: |
    ## Overview ##
    The **Jentic Control Plane API** is the unified HTTP surface of the
    Jentic platform's control plane — every administrative and
    metadata-facing endpoint outside the Broker data plane.

    The control plane is the administrative counterpart to the
    [Broker](../broker/swagger) data plane: where the Broker executes
    upstream API calls on behalf of agents, the control plane is where
    humans, operators, and agents configure *what* the Broker is
    allowed to do, *for whom*, *with which credentials*, *against
    which registered APIs*, and *with what telemetry retained*.

    ## Components ##
    High-level components for the control plane API.
    - **[Core / Access](#tag/Credentials)** — credentials, direct
      agent↔credential bindings, and PBAC grants and runtime params. Owns *who can
      call what, with which secret*.
    - **[Registry](#tag/APIs)** — registered APIs, immutable
      revisions, operations, search, lookup. Owns *what APIs are
      registered for this deployment and what they look like at any
      given revision*.
    - **[Admin / Audit](#tag/Users)** — users, permissions,
      execution telemetry, async-job tracker, and dashboard event
      stream. Owns *who can sign in with what permissions* together
      with *what actually happened* — the human roster, the
      append-only records of brokered executions, the unified
      `Job` resource that both Registry imports and Broker
      async-execution calls feed into, and a curated `/events`
      surface for dashboards and operators.

    ## Authentication ##

    Today the platform ships a local username + password identity
    provider for human users, alongside agent identity (Dynamic Client
    Registration + RFC 7523 JWT-bearer assertions) and service-account
    client credentials — every authenticated operation expects
    `Authorization: Bearer <token>` (`BearerAuth`, an opaque `at_`
    token) except `GET /health`, `POST /auth/login`,
    `POST /users:create-admin`, and `POST /users:redeem-invite`.
    Human tokens are issued by `POST /auth/login` with a fixed 1-hour
    TTL and can be re-minted before expiry via `POST /auth/refresh`
    (sliding session, bounded by an absolute window —
    `admin.auth.session_ttl_seconds`, 12 hours by default); agents and
    service accounts obtain tokens from `POST /oauth/token` (see
    `BearerAuth`). The `permissions` claim on a token is a snapshot at
    issue time; permission changes take effect at the next re-issue —
    the next refresh or re-login (≤ 1 hour with the default TTL).

    The platform ships **no default credentials**. On a fresh
    install the users table is empty, so `GET /health` returns
    `setup_required: true` with `next_step: create_admin`. The
    operator creates the first administrator with `POST
    /users:create-admin {email, password}` — a one-time,
    unauthenticated, self-closing endpoint: it succeeds only while
    the users table is empty and returns `410 setup_already_complete`
    once any user exists (a single-row setup sentinel is the
    concurrency backstop, so two racing callers — even with different
    emails — cannot both bootstrap). It grants `org:admin`, sets the operator's chosen
    password (so `must_change_password` is **false** — no forced
    rotation), and returns a ready-to-use `LoginResponse`
    (auto-login). After the first user exists `setup_required` flips
    to `false`. See the **Users** tag for the full onboarding
    walkthrough.

    External SSO providers (Okta, AWS Cognito, Auth0, Azure AD)
    plug in via the `User.auth_provider: external` hook plus an
    `external_subject_id` mapping; no concrete provider ships in
    this revision. Agent identity (separate from human users) uses
    Dynamic Client Registration (`POST /register`) with the agent's
    JWKS, then RFC 7523 JWT-bearer assertions exchanged at
    `POST /oauth/token` for an opaque access token.

    ## Cross-cutting conventions

    - Every API reference uses the loose
      `APIReference(vendor, name, version)` data structure. There is no
      foreign key between Core, Registry, and Admin / Audit — the
      tuple is the only join.
    - **Error shape.** Every error response is RFC 9457 Problem
      Details (`application/problem+json`) using the shared
      `ProblemDetails` schema from
      [jentic/api-problem-details](https://github.com/jentic/api-problem-details).
      Field-level validation failures (`422`) populate `errors[]`;
      transport-level conditions reuse the same shape with status-
      and `type`-specific payloads.
    - **JSON casing.** Field names in request and response bodies are
      `snake_case` throughout, including discriminator values
      (`type: api_key`, `type: bearer_token`, …). The one historical
      camelCase exception is the OpenAPI keyword `apiKey` itself when
      it appears in re-exported `securitySchemes.type` values; that's
      an OpenAPI artefact, not platform style.
    - **Pagination.** List endpoints are cursor-paginated with opaque
      `next_cursor` round-trips. `has_more=false` means done. List
      envelopes are intentionally slim — `{data, has_more,
      next_cursor}` — and **do not** carry HAL-style `_links` (no
      `self`, `next`, or `prev`); cursors are forward-only and
      `next_cursor` already encodes the next page. Individual resource
      responses still use `_links` where it carries non-trivial
      information (e.g. `Job._links.result`, `AsyncQueuedResponse._links.self`,
      `Event._links`).
    - **Authentication.** All operations require `Authorization:
      Bearer <token>` — an opaque access token (prefixed `at_`, not a
      JWT), obtained from `POST /oauth/token` (see `BearerAuth` for the
      per-actor grants) — except the unauthenticated
      `GET /health`, `POST /auth/login`, `POST /users:create-admin`,
      and `POST /users:redeem-invite`. See the `Users` tag for the
      first-run setup and invite-redemption flows.
    - **Header namespace.** All Jentic-specific HTTP headers — request
      and response, broker and control-plane — live under the
      `Jentic-*` namespace **without** the legacy `X-` prefix
      (RFC 6648 deprecates `X-` for new protocols). `Jentic-*` is
      unambiguously a platform header; anything else on a brokered
      response belongs to the upstream API. Standard headers
      (`Authorization`, `Prefer`, `Idempotency-Key`, `Accept`,
      `traceparent`, `tracestate`) keep their RFC names.
    - **Append-only audit.** Admin / Audit records are never mutated
      after the fact; retention is enforced via purge policy, not
      edit.
    - **Trace correlation.** Every Broker call records a W3C
      `trace_id` on its `ExecutionRecord` and on any `Event` derived
      from it. To walk all executions under one logical request, use
      `GET /executions?trace_id=…`.
    - **Event stream.** The dashboard / operator UI consumes the
      `/events` resource (paginated list + SSE stream). Some events
      are informational, some require action — see the `Events` tag.
    - **Revision pinning.** Every `Api` carries one `published`
      revision (`current_revision_id`) plus zero or more `draft` and
      `archived` siblings. The Broker's hot path resolves against
      `current_revision_id` by default; agents can target a specific
      `draft` revision per call via the Broker's `Jentic-Revision`
      header. Control-plane reads (`Search`, `Inspect`,
      `Lookup`) accept matching `revision_id` / `revision_pins`
      parameters so dashboards can preview an unpromoted revision
      before flipping `:promote`.
    - **ID prefixes.** Resource identifiers use a stable short prefix.
      Treat the prefix as part of the ID — never strip it. The
      complete table:

      | Prefix | Resource | Notes |
      |---|---|---|
      | `tk_` | Toolkit ID | Retired (theme-5 Phase 5b): the toolkit management surface is gone. Ids still appear in stored records (bindings, audit) until the tables retire in Phase 6b. |
      | `ck_` | Toolkit-key record | Retired (theme-5 Phase 4): no new keys are issued and the key-management routes are gone (Phase 5b). Each surviving plaintext authenticates as the service account it was migrated to. |
      | `cred_` | Credential ID | |
      | `exec_` | Execution record | Returned in the `Jentic-Execution-Id` response header on every brokered call. |
      | `job_` | Async job | UUIDs also accepted on inputs for backward compatibility. |
      | `evt_` | Event | ULID-shaped. |
      | `op_` | Registered operation | |
      | `rev_` | API revision | ULID-shaped. |
      | `usr_` | User | Org member. Resolves via `GET /users/{user_id}`. Used in `acknowledged_by`, `decided_by`, and similar audit references. |
      | `inv_` | Invite token | One-time token issued at user creation. Plaintext value shown **once** at issue / re-issue; `:redeem-invite` consumes it. |
      | `areq_` | Access request | Human-approval ticket for scope grants and credential bindings; see the `Access Requests` tag. |
      | `note_` | Note | ULID-shaped. Free-form annotation attached to a registry resource — see the `Notes` tag. |
      | `ovr_` | Overlay | ULID-shaped. OpenAPI Overlay 1.0 document attached to an `Api` aggregate — see the `Overlays` tag. |
      | `jntc_live_` | Plaintext toolkit API key value (retired) | Never issued anymore (issuance died in Phase 4, the management routes in Phase 5b). A surviving value keeps authenticating — as its migrated service account — for the deprecation window; rotate holders to `sak_` keys. |

      Surfaces still being designed (agent identity, OAuth brokers)
      will add their own prefixes when they land.
  license:
    identifier: Apache-2.0
    name: Apache-2.0
  summary: HTTP surface of the Jentic platform's control plane — Core / Access (credentials, direct agent bindings), Registry (registered APIs, operations, search), and Admin / Audit (execution telemetry, async jobs).
  title: Jentic Control Plane API
  version: 0.40.0
openapi: 3.1.0
paths:
  /.well-known/jwks.json:
    get:
      description: Return the JWKS document with the active public signing keys (ES256).
      operationId: jwks
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Jwks
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: &id001
                detail: The request was malformed or failed a precondition.
                instance: /example/path
                status: 400
                title: Bad Request
                type: bad_request
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: &id002
                detail: Request validation failed; see errors[] for details.
                errors:
                - detail: Field 'name' must not be blank.
                  pointer: '#/name'
                instance: /example/path
                status: 422
                title: Unprocessable Entity
                type: validation_error
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: &id003
                detail: An unexpected error occurred.
                instance: /example/path
                status: 500
                title: Internal Server Error
                type: server_error
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: &id004
                detail: The database is temporarily unavailable; please retry.
                instance: /example/path
                status: 503
                title: Service Unavailable
                type: database_unavailable
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: JSON Web Key Set
      tags:
      - Discovery
  /.well-known/oauth-authorization-server:
    get:
      description: Return RFC 8414 authorization-server metadata (endpoints, grant types, algorithms).
      operationId: oauthAuthorizationServer
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Oauthauthorizationserver
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: OAuth authorization server metadata
      tags:
      - Discovery
  /.well-known/oauth-authorization-server/mcp:
    get:
      description: |-
        RFC 8414 metadata for the path-scoped issuer `{base}/mcp`.

        RFC 8414 §3.1 path insertion: this is the metadata URL clients derive for
        the issuer `{base}/mcp` named by the protected-resource document. It
        advertises the anonymous OAuth-client registration door (`/oauth-clients`)
        and the public-client profile (`none` + PKCE S256) — the root document is a
        separate, unchanged surface whose `registration_endpoint` remains the agent
        `/register`.
      operationId: mcpOauthAuthorizationServer
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Mcpoauthauthorizationserver
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '404':
          description: 'Interactive OAuth for MCP is disabled (`server.mcp.oauth.enabled=false`): the route answers the framework''s plain route-not-found 404, so the gate state is unobservable.'
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: OAuth authorization server metadata for the MCP resource
      tags:
      - Discovery
  /.well-known/oauth-protected-resource:
    get:
      description: |-
        Root-path alias of the MCP protected-resource document.

        Compatibility fallback for clients that probe the root
        `/.well-known/oauth-protected-resource` instead of following the 401's
        `resource_metadata` pointer (the documented Claude Code behaviour). Safe
        because this deployment has exactly one OAuth-protected resource, so the
        root and path-scoped documents are the same body.

        Two acknowledged trades:

        - RFC 9728 §3 says this well-known path corresponds to resource identifier
          ``{base}`` (no path), and §3.3 has clients validate ``resource`` against
          the resource they queried. This body claims ``resource={base}/mcp`` — a
          strict path-deriving validator would reject it, but Claude's fallback
          validates against the MCP server URL (``{base}/mcp``), which is exactly
          what the alias exists to satisfy. That is the intended trade.
        - The alias squats the deployment's only root PRM slot: a future non-MCP
          protected resource at ``{base}`` cannot get its own root document
          without breaking this fallback. The mounted MCP app must re-confirm the
          "exactly one OAuth-protected resource" premise before adding one.
      operationId: mcpOauthProtectedResourceRootAlias
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Mcpoauthprotectedresourcerootalias
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '404':
          description: 'Interactive OAuth for MCP is disabled (`server.mcp.oauth.enabled=false`): the route answers the framework''s plain route-not-found 404, so the gate state is unobservable.'
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: OAuth protected resource metadata (root alias for the MCP resource)
      tags:
      - Discovery
  /.well-known/oauth-protected-resource/mcp:
    get:
      description: |-
        RFC 9728 protected-resource metadata for `{base}/mcp`.

        Names the /mcp-scoped authorization server and the MCP tool scopes. The
        same body is also served at the root well-known path for clients that
        ignore the 401's `resource_metadata` pointer.
      operationId: mcpOauthProtectedResource
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Mcpoauthprotectedresource
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '404':
          description: 'Interactive OAuth for MCP is disabled (`server.mcp.oauth.enabled=false`): the route answers the framework''s plain route-not-found 404, so the gate state is unobservable.'
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: OAuth protected resource metadata for the MCP resource
      tags:
      - Discovery
  /access-requests:
    get:
      description: List access requests with cursor-based pagination.
      operationId: listAccessRequests
      parameters:
      - in: query
        name: actor_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
      - in: query
        name: status
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: &id005
                detail: Authentication is required or the bearer token is invalid.
                instance: /example/path
                status: 401
                title: Unauthorized
                type: unauthorized
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: &id006
                detail: The authenticated principal lacks the required permission.
                instance: /example/path
                status: 403
                title: Forbidden
                type: forbidden
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List access requests
      tags:
      - Access Requests
    post:
      description: File a new access request.
      operationId: fileAccessRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessRequestFileRequest'
        required: true
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '409':
          content:
            application/problem+json:
              example: &id008
                detail: The request conflicts with the current state of the resource.
                instance: /example/path
                status: 409
                title: Conflict
                type: conflict
              schema:
                $ref: '#/components/schemas/DuplicatePendingProblem'
          description: A pending request already exists for the same resource.
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: File access request
      tags:
      - Access Requests
  /access-requests/{request_id}:
    get:
      description: Get a single access request by ID.
      operationId: getAccessRequest
      parameters:
      - in: path
        name: request_id
        required: true
        schema:
          title: Request Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: &id007
                detail: The requested resource does not exist or is not visible to you.
                instance: /example/path
                status: 404
                title: Not Found
                type: not_found
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get access request
      tags:
      - Access Requests
  /access-requests/{request_id}:amend:
    post:
      description: Amend pending items on an access request.
      operationId: amendAccessRequest
      parameters:
      - in: path
        name: request_id
        required: true
        schema:
          title: Request Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AmendRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Amend access request
      tags:
      - Access Requests
  /access-requests/{request_id}:decide:
    post:
      description: Decide (approve/deny) items on an access request.
      operationId: decideAccessRequest
      parameters:
      - in: path
        name: request_id
        required: true
        schema:
          title: Request Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DecideRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Decide access request items
      tags:
      - Access Requests
  /access-requests/{request_id}:withdraw:
    post:
      description: Withdraw a pending access request.
      operationId: withdrawAccessRequest
      parameters:
      - in: path
        name: request_id
        required: true
        schema:
          title: Request Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessRequestResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Withdraw access request
      tags:
      - Access Requests
  /actors:
    get:
      description: List all actors (users, agents, service accounts) for UI cache hydration.
      operationId: listActors
      parameters:
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 1000
          maximum: 5000
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActorListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Actors
      tags:
      - Actors
  /admin/config/providers:
    get:
      description: List all stored provider configs, with secret fields redacted.
      operationId: listProviderConfigs
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProviderConfigListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List credential provider configs
      tags:
      - Configuration
  /admin/config/providers/{name}:
    get:
      description: Get a stored provider config by name, with secret fields redacted.
      operationId: getProviderConfig
      parameters:
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProviderConfigResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get a credential provider config
      tags:
      - Configuration
    put:
      description: |-
        Set (create or update) a credential provider config at runtime.

        The payload is validated by provider name (e.g. ``pipedream``). Secret fields
        are encrypted at rest. A successful write rebuilds the in-process provider
        registry so the change takes effect without a restart. The response redacts
        secret fields.
      operationId: setProviderConfig
      parameters:
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProviderConfigSetRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProviderConfigResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Set a credential provider config
      tags:
      - Configuration
  /admin/health:
    get:
      description: Return admin service health status.
      operationId: health
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Health
      tags:
      - System
  /admin/oauth-clients:
    get:
      description: List all registered OAuth clients, optionally filtered by approval status.
      operationId: listOauthClients
      parameters:
      - in: query
        name: include_inactive
        required: false
        schema:
          default: false
          title: Include Inactive
          type: boolean
      - description: 'Filter by approval lifecycle state: pending, approved, or denied. Filtering on pending or denied implies include_inactive=true — those rows are always inactive until approved.'
        in: query
        name: approval_status
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Filter by approval lifecycle state: pending, approved, or denied. Filtering on pending or denied implies include_inactive=true — those rows are always inactive until approved.'
          title: Approval Status
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List OAuth clients
      tags:
      - OAuth Clients
    post:
      description: |-
        Register a new OAuth client for third-party application integration.

        The generated ``client_id`` and ``client_secret`` are returned in the
        response. The secret is shown only once — store it securely.
      operationId: createOauthClient
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OAuthClientCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientCreateResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Register OAuth client
      tags:
      - OAuth Clients
  /admin/oauth-clients/{id}:
    delete:
      description: |-
        Disable an OAuth client — the reversible kill switch (sets active=false).

        Disabled clients can no longer initiate authorization flows and their
        outstanding tokens stop resolving. Re-enable by patching ``active: true``.
        The row is kept; this is not a delete.
      operationId: deactivateOauthClient
      parameters:
      - in: path
        name: id
        required: true
        schema:
          title: Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Disable OAuth client
      tags:
      - OAuth Clients
    get:
      description: Get an OAuth client by its internal ID.
      operationId: getOauthClient
      parameters:
      - in: path
        name: id
        required: true
        schema:
          title: Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get OAuth client
      tags:
      - OAuth Clients
    patch:
      description: Update an OAuth client's name, description, redirect_uris, or active status.
      operationId: updateOauthClient
      parameters:
      - in: path
        name: id
        required: true
        schema:
          title: Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OAuthClientUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update OAuth client
      tags:
      - OAuth Clients
  /admin/oauth-clients/{id}/rotate-secret:
    post:
      description: |-
        Generate a new client secret. The previous secret is immediately invalidated.

        The new secret is shown only once — store it securely.
      operationId: rotateOauthClientSecret
      parameters:
      - in: path
        name: id
        required: true
        schema:
          title: Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientRotateSecretResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Rotate client secret
      tags:
      - OAuth Clients
  /admin/oauth-clients/{id}:approve:
    post:
      description: |-
        Approve an OAuth client — sets approval_status=approved and active=true.

        Also re-approves a previously denied client (deny is reversible; rows are
        never deleted, so the client's cached client_id becomes valid again).
      operationId: approveOauthClient
      parameters:
      - in: path
        name: id
        required: true
        schema:
          title: Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Approve OAuth client
      tags:
      - OAuth Clients
  /admin/oauth-clients/{id}:delete:
    post:
      description: |-
        Permanently delete an OAuth client. This cannot be undone.

        Terminal, unlike the reversible kill switch (``DELETE`` on this
        resource, which only sets ``active=false``): every active grant is
        revoked, every token carrying the client's lineage is revoked, and the
        registration row is removed — connected applications are fully
        disconnected. The audit trail survives. A client that later re-registers
        via dynamic client registration is a NEW registration and re-enters the
        approval queue as pending; it is never re-attached to the deleted one.
      operationId: deleteOauthClient
      parameters:
      - in: path
        name: id
        required: true
        schema:
          title: Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete OAuth client
      tags:
      - OAuth Clients
  /admin/oauth-clients/{id}:deny:
    post:
      description: |-
        Deny an OAuth client — sets approval_status=denied and active=false.

        The row is retained so a later approve can reverse the decision.
      operationId: denyOauthClient
      parameters:
      - in: path
        name: id
        required: true
        schema:
          title: Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/OAuthClientDenyRequest'
              - type: 'null'
              title: Body
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Deny OAuth client
      tags:
      - OAuth Clients
  /admin/oauth-grants:
    get:
      description: |-
        List consent→agent grants across all clients and agents.

        The admin cross-view over the grant registry: filter by client, agent,
        consenting user, or status. Each item carries the client's display name
        and redirect-URI origin plus the consenting ``user_id`` — after an agent
        ownership transfer the grant stays with the original consenter, so this
        column is how an admin spots stranded grants.
      operationId: listOauthGrants
      parameters:
      - description: Filter by the client's public client_id.
        in: query
        name: client_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by the client's public client_id.
          title: Client Id
      - description: Filter by bound agent.
        in: query
        name: agent_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by bound agent.
          title: Agent Id
      - description: Filter by consenting user.
        in: query
        name: user_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter by consenting user.
          title: User Id
      - description: Filter by grant lifecycle state.
        in: query
        name: status
        required: false
        schema:
          anyOf:
          - enum:
            - active
            - revoked
            type: string
          - type: 'null'
          description: Filter by grant lifecycle state.
          title: Status
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthGrantAdminListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List OAuth grants
      tags:
      - OAuth
  /agents:
    get:
      description: List agents — scoped by identity via dynamic query scoping.
      operationId: listAgents
      parameters:
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: status
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Agents
      tags:
      - Agents
    post:
      description: Create a new agent manually.
      operationId: createAgent
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Create Agent
      tags:
      - Agents
  /agents/{agent_id}:
    delete:
      description: |-
        Archive an agent — terminal-but-kept.

        The row is retained for history, but the action is not reversible and
        the agent's authority is swept: scope grants, credential bindings, and
        OAuth consent grants are revoked. For the reversible kill switch use
        ``:disable`` / ``:enable`` instead.
      operationId: archiveAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Archive Agent
      tags:
      - Agents
    get:
      description: Get agent by ID — requires agents:read or self-read.
      operationId: getAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Agent
      tags:
      - Agents
    patch:
      description: Partially update an agent — name, description, or owner_id.
      operationId: updateAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentPatchRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update Agent
      tags:
      - Agents
  /agents/{agent_id}/api-key:
    get:
      description: Get API key metadata for an agent. Returns info even after revocation.
      operationId: getAgentApiKeyInfo
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                anyOf:
                - $ref: '#/components/schemas/ApiKeyInfoResponse'
                - type: 'null'
                title: Response Getagentapikeyinfo
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Agent Api Key Info
      tags:
      - Agents
  /agents/{agent_id}/api-key/history:
    get:
      description: Get the audit history of API key operations for an agent.
      operationId: getAgentApiKeyHistory
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyHistoryResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Agent Api Key History
      tags:
      - Agents
  /agents/{agent_id}/credentials:
    get:
      description: List direct credential bindings for an agent — requires agents:read or self.
      operationId: listAgentCredentials
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialBindingListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Credentials
      tags:
      - Agents
    post:
      description: |-
        Directly bind a credential to an agent (theme 5 phase 1).

        The caller must own the target credential (or hold ``org:admin``); a
        credential that does not exist or that the caller does not own returns 404.
      operationId: bindAgentCredential
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CredentialBindRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialBindingResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Bind Credential
      tags:
      - Agents
  /agents/{agent_id}/credentials/{credential_id}:
    delete:
      description: Unbind a credential from an agent — suspend by default, purge on request.
      operationId: unbindAgentCredential
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - description: 'Default false: the binding is suspended (reversible; its permission rules survive and :resume restores access). true deletes the binding row outright.'
        in: query
        name: purge
        required: false
        schema:
          default: false
          description: 'Default false: the binding is suspended (reversible; its permission rules survive and :resume restores access). true deletes the binding row outright.'
          title: Purge
          type: boolean
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Unbind Credential
      tags:
      - Agents
  /agents/{agent_id}/credentials/{credential_id}:resume:
    post:
      description: Lift a suspended credential binding — the reverse of the default unbind.
      operationId: resumeAgentCredentialBinding
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialBindingResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Resume Credential Binding
      tags:
      - Agents
  /agents/{agent_id}/jwks:
    put:
      description: |-
        Update an agent's JWKS (public keys for JWT-bearer authentication).

        The JWKS must contain at least one Ed25519 public key and must not contain
        any private key material. This enables the agent to authenticate via
        JWT-bearer assertions signed with the corresponding private key.
      operationId: updateAgentJwks
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/JwksUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update Agent Jwks
      tags:
      - Agents
  /agents/{agent_id}/oauth-grants:
    get:
      description: |-
        List OAuth consent grants binding clients to this agent.

        The "Connected clients" surface: every grant carries the client's display
        name and redirect-URI origin, the granted scopes, the consenting user,
        and created/last-used timestamps. Allowed for the agent's owner or an
        admin — authorization is enforced in the service layer, mirroring the
        ``:revoke`` semantics.
      operationId: listAgentOauthGrants
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      - description: Filter by grant lifecycle state.
        in: query
        name: status
        required: false
        schema:
          anyOf:
          - enum:
            - active
            - revoked
            type: string
          - type: 'null'
          description: Filter by grant lifecycle state.
          title: Status
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthGrantListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List agent OAuth grants
      tags:
      - Agents
  /agents/{agent_id}/scopes:
    get:
      description: List scopes granted to an agent.
      operationId: getAgentScopes
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentScopesResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Agent Scopes
      tags:
      - Agents
    put:
      description: Replace all scopes for an agent.
      operationId: replaceAgentScopes
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentScopesRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentScopesResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Replace Agent Scopes
      tags:
      - Agents
  /agents/{agent_id}:approve:
    post:
      description: Approve a pending agent.
      operationId: approveAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Approve Agent
      tags:
      - Agents
  /agents/{agent_id}:claim:
    post:
      description: |-
        Claim ownership of a self-registered agent using its claim token.

        Authenticated by the platform bearer token but requires **no** agent
        permission — the single-use claim token minted at ``/register`` is the proof,
        so the registering human (even a plain member) can take ownership. Sets
        ``owner_id`` to the caller; the existing scoping + approve paths then apply.

        Restricted to ``USER`` actors: ``Agent.owner_id`` is a FK to ``users.id``, so
        only a human can own an agent. The ``require_actor_type`` gate rejects a
        non-user actor (agent/service-account) at the boundary with a 403;
        ``AgentService.claim`` re-checks the same invariant as defense-in-depth.

        ``allow_expired_password=True`` is intentional (matching ``GET /agents/{id}``):
        claiming is an onboarding step a brand-new user may hit before they have
        rotated a temporary password, so a must-change-password state must not block
        it. The claim only sets ownership — it grants no scopes and cannot act as the
        agent — so allowing it under an expired password is low-risk.
      operationId: claimAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClaimRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Claim Agent
      tags:
      - Agents
  /agents/{agent_id}:deny:
    post:
      description: Deny a pending agent.
      operationId: denyAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/jentic_one__auth__web__schemas__agents__DenyRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Deny Agent
      tags:
      - Agents
  /agents/{agent_id}:disable:
    post:
      description: Disable an active agent.
      operationId: disableAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Disable Agent
      tags:
      - Agents
  /agents/{agent_id}:enable:
    post:
      description: Enable a disabled agent.
      operationId: enableAgent
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Enable Agent
      tags:
      - Agents
  /agents/{agent_id}:generate-api-key:
    post:
      description: Generate a new API key for an active agent. Rotates any existing key.
      operationId: generateAgentApiKey
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Generate Agent Api Key
      tags:
      - Agents
  /agents/{agent_id}:revoke-api-key:
    post:
      description: Revoke an agent's API key without generating a new one.
      operationId: revokeAgentApiKey
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Revoke Agent Api Key
      tags:
      - Agents
  /apis:
    get:
      description: |-
        List locally registered APIs with optional vendor filter and pagination.

        This is the **imported** registry — APIs present in this deployment. The
        public catalog of importable-but-not-yet-imported APIs is a separate surface
        (``GET /catalog``); the two are not blended.
      operationId: listApis
      parameters:
      - in: query
        name: vendor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Vendor
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Apis
      tags:
      - APIs
    post:
      description: Import OpenAPI/Arazzo content as new API revisions (async).
      operationId: importApis
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApiImportRequest'
        required: true
      responses:
        '202':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Import Apis
      tags:
      - APIs
  /apis/{vendor}/{name}/{version}:
    delete:
      description: Delete an API and all its revisions.
      operationId: deleteApi
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete Api
      tags:
      - APIs
    get:
      description: Retrieve a single API by its (vendor, name, version) identity.
      operationId: getApi
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Api
      tags:
      - APIs
    patch:
      description: Partially update an API's presentation fields.
      operationId: updateApi
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApiUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update Api
      tags:
      - APIs
  /apis/{vendor}/{name}/{version}/openapi:
    get:
      description: Download the OpenAPI spec for the API's current (live) revision.
      operationId: getApiSpec
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: query
        name: overlays
        required: false
        schema:
          default: true
          title: Overlays
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Api Spec
      tags:
      - API Spec
  /apis/{vendor}/{name}/{version}/operations:
    get:
      description: List operations for the API's current (live) revision.
      operationId: listApiOperations
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationSummaryListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Api Operations
      tags:
      - API Operations
  /apis/{vendor}/{name}/{version}/overlays:
    get:
      description: List overlays for an API with optional status filter and cursor pagination.
      operationId: listOverlays
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: status
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverlayListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Overlays
      tags:
      - Overlays
    post:
      description: Submit a new overlay for an API.
      operationId: submitOverlay
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OverlaySubmitRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverlayResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Submit Overlay
      tags:
      - Overlays
  /apis/{vendor}/{name}/{version}/overlays/{overlay_id}:
    delete:
      description: Deprecate an overlay (soft delete).
      operationId: deprecateOverlay
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: overlay_id
        required: true
        schema:
          title: Overlay Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Deprecate Overlay
      tags:
      - Overlays
    get:
      description: Retrieve a single overlay by ID.
      operationId: getOverlay
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: overlay_id
        required: true
        schema:
          title: Overlay Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverlayResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Overlay
      tags:
      - Overlays
    patch:
      description: |-
        Update an overlay's document or target revision.

        Editing a *pending* overlay (or a stuck CONFIRMED-but-unmaterialized one) is an
        ordinary contributor edit — ``apis:write``. Editing a *materialized* overlay
        (CONFIRMED and currently serving) instead **re-materializes** it: the edited document
        is re-applied over the overlay's original pre-overlay base and re-ingested, rewriting
        the API's served spec (D1). That is an operator action, so it additionally requires
        ``overlays:confirm``; a caller with only ``apis:write`` gets a 403
        (``overlay_rematerialize_forbidden``). The re-materialize is refused with a 409 if the
        overlay is no longer the live revision, and with ``overlay_rollback_target_missing``
        if no clean base was recorded to re-apply over.
      operationId: updateOverlay
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: overlay_id
        required: true
        schema:
          title: Overlay Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OverlayUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverlayResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update Overlay
      tags:
      - Overlays
  /apis/{vendor}/{name}/{version}/overlays/{overlay_id}:confirm:
    post:
      description: |-
        Confirm an overlay, materializing it onto the served spec.

        Requires ``overlays:confirm``: confirming rewrites the API's served spec (it
        re-ingests the base spec with the overlay applied and promotes the result to the
        current revision), so it is an operator action, not a contributor one (contributors
        ``submit`` overlays with ``apis:write``; an operator reviews and confirms). This is a
        purpose-scoped downgrade from the former ``org:admin`` gate — narrow enough that an
        owner can grant it to a trusted operator without handing over full admin power.
        ``org:admin`` still satisfies it (it implies ``overlays:confirm``), and the scope is
        deliberately excluded from an agent's self-service grantable set so a low-privilege
        agent cannot escalate into it.
      operationId: confirmOverlay
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: overlay_id
        required: true
        schema:
          title: Overlay Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OverlayConfirmRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverlayResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Confirm Overlay
      tags:
      - Overlays
  /apis/{vendor}/{name}/{version}/overlays/{overlay_id}:rollback:
    post:
      description: |-
        Un-confirm a materialized overlay, restoring the revision it superseded (A5b).

        Requires ``overlays:confirm`` — the symmetric inverse of confirm. Rolling back
        rewrites the API's served spec (it archives the overlay's materialized revision and
        promotes the prior revision back to current), so it is the same operator action as
        confirm, not a contributor one. The overlay must be CONFIRMED, currently live, and
        carry a recorded superseded revision that is still restorable; otherwise a 409 is
        returned (``overlay_conflict`` or ``overlay_rollback_target_missing``) and nothing
        changes.
      operationId: rollbackOverlay
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: overlay_id
        required: true
        schema:
          title: Overlay Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Rollback Overlay
      tags:
      - Overlays
  /apis/{vendor}/{name}/{version}/revisions:
    get:
      description: List revisions for an API with optional state filter and cursor pagination.
      operationId: listApiRevisions
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: query
        name: state
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: State
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Api Revisions
      tags:
      - APIs
  /apis/{vendor}/{name}/{version}/revisions/{revision_id}:
    delete:
      description: Delete an archived revision.
      operationId: deleteRevision
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: revision_id
        required: true
        schema:
          title: Revision Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete Revision
      tags:
      - APIs
    get:
      description: Retrieve a single revision by ID.
      operationId: getApiRevision
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: revision_id
        required: true
        schema:
          title: Revision Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Api Revision
      tags:
      - APIs
  /apis/{vendor}/{name}/{version}/revisions/{revision_id}/openapi:
    get:
      description: Download the OpenAPI spec for a specific revision.
      operationId: getApiRevisionSpec
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: revision_id
        required: true
        schema:
          title: Revision Id
          type: string
      - in: query
        name: overlays
        required: false
        schema:
          default: true
          title: Overlays
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Api Revision Spec
      tags:
      - API Spec
  /apis/{vendor}/{name}/{version}/revisions/{revision_id}/operations:
    get:
      description: List operations for a specific revision.
      operationId: listApiRevisionOperations
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: revision_id
        required: true
        schema:
          title: Revision Id
          type: string
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Api Revision Operations
      tags:
      - API Operations
  /apis/{vendor}/{name}/{version}/revisions/{revision_id}:archive:
    post:
      description: Archive a draft revision.
      operationId: archiveRevision
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: revision_id
        required: true
        schema:
          title: Revision Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Archive Revision
      tags:
      - APIs
  /apis/{vendor}/{name}/{version}/revisions/{revision_id}:promote:
    post:
      description: Promote a draft revision to published, archiving the current one.
      operationId: promoteRevision
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      - in: path
        name: revision_id
        required: true
        schema:
          title: Revision Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Promote Revision
      tags:
      - APIs
  /apis/{vendor}/{name}/{version}/security-schemes:
    get:
      description: List security schemes (with OAuth2 flow URLs) for the API's current revision.
      operationId: listApiSecuritySchemes
      parameters:
      - in: path
        name: vendor
        required: true
        schema:
          title: Vendor
          type: string
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      - in: path
        name: version
        required: true
        schema:
          title: Version
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecuritySchemeListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List security schemes for an API
      tags:
      - APIs
  /audit:
    get:
      description: List audit entries with optional filters.
      operationId: listAuditEntries
      parameters:
      - in: query
        name: target_type
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/AuditTargetType'
          - type: 'null'
          title: Target Type
      - in: query
        name: target_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Target Id
      - in: query
        name: actor_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
      - in: query
        name: origin
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Origin
      - in: query
        name: since
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Since
      - in: query
        name: until
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Until
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Audit Entries
      tags:
      - Audit
  /audit/{audit_id}:
    get:
      description: Get a single audit entry by ID.
      operationId: getAuditEntry
      parameters:
      - in: path
        name: audit_id
        required: true
        schema:
          title: Audit Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Audit Entry
      tags:
      - Audit
  /auth/health:
    get:
      description: |-
        Return service health status for this surface.

        Unauthenticated liveness probe. In combined mode this is served under
        the surface prefix (e.g. ``/control/health``) so surfaces don't collide;
        the canonical platform probe is the root ``GET /health``.
      operationId: authHealth
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                title: Response Authhealth
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Auth health
      tags:
      - System
  /auth/idp:
    get:
      description: |-
        Public capability hint: whether IdP login is enabled, and which provider.

        The SPA reads this before login to decide whether to show a "Continue with
        <provider>" button. Unauthenticated and secret-free — it advertises only the
        enabled flag and the provider name (never client secrets or endpoints).
      operationId: authIdpDescriptor
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Authidpdescriptor
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: External IdP login descriptor
      tags:
      - Discovery
  /auth/login:
    post:
      description: Authenticate and return a JWT token bundle.
      operationId: login
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Log in
      tags:
      - Users
  /auth/refresh:
    post:
      description: |-
        Re-mint the caller's login JWT before it expires (sliding session).

        Only user login JWTs are refreshable; permissions and the
        ``must_change_password`` gate are re-read from the database at re-mint.
        Refusal modes: 401 ``session_expired`` once the original authentication is
        older than the absolute window (``admin.auth.session_ttl_seconds``), 401
        ``invalid_credentials`` for deactivated users, non-user tokens, or opaque
        (non-JWT) credentials. Clients should then send the user back to login.
      operationId: refreshSession
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Refresh session token
      tags:
      - Users
  /authorize:
    get:
      description: |-
        RFC 6749 Authorization endpoint with PKCE (S256 only).

        If an external IdP is configured, redirects to the upstream provider.
        Otherwise returns an error (direct login requires a separate credential exchange).
      operationId: authorizeEndpoint
      parameters:
      - in: query
        name: response_type
        required: true
        schema:
          title: Response Type
          type: string
      - in: query
        name: client_id
        required: true
        schema:
          title: Client Id
          type: string
      - in: query
        name: redirect_uri
        required: true
        schema:
          title: Redirect Uri
          type: string
      - in: query
        name: code_challenge
        required: true
        schema:
          title: Code Challenge
          type: string
      - in: query
        name: code_challenge_method
        required: true
        schema:
          title: Code Challenge Method
          type: string
      - in: query
        name: scope
        required: false
        schema:
          default: openid
          title: Scope
          type: string
      - in: query
        name: state
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: State
      - in: query
        name: nonce
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Nonce
      - description: Signed session-continuation blob minted by POST /oauth/session/continue (identity-ladder rung 1). Optional; an invalid or absent value leaves the flow byte-identical to before.
        in: query
        name: sc
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Signed session-continuation blob minted by POST /oauth/session/continue (identity-ladder rung 1). Optional; an invalid or absent value leaves the flow byte-identical to before.
          title: Sc
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Authorize Endpoint
      tags:
      - OAuth
  /catalog:
    get:
      description: |-
        List a keyset page of browsable catalog entries (search/filter aware).

        The catalog holds thousands of entries, so this is cursor-paginated like
        ``GET /apis``: follow ``next_cursor`` until ``has_more`` is false.
        ``catalog_total``/``registered_count``/``outdated_count`` count the whole
        manifest, not the page, so the Discover status row stays stable while scrolling.
      operationId: listCatalog
      parameters:
      - in: query
        name: q
        required: false
        schema:
          anyOf:
          - maxLength: 500
            type: string
          - type: 'null'
          title: Q
      - in: query
        name: registered_only
        required: false
        schema:
          default: false
          title: Registered Only
          type: boolean
      - in: query
        name: unregistered_only
        required: false
        schema:
          default: false
          title: Unregistered Only
          type: boolean
      - in: query
        name: outdated_only
        required: false
        schema:
          default: false
          title: Outdated Only
          type: boolean
      - in: query
        name: include_snoozed
        required: false
        schema:
          default: false
          title: Include Snoozed
          type: boolean
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Catalog
      tags:
      - Catalog
  /catalog/{api_id}:
    get:
      description: Retrieve a single catalog entry by api_id.
      operationId: getCatalogEntry
      parameters:
      - in: path
        name: api_id
        required: true
        schema:
          title: Api Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogEntryResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Catalog Entry
      tags:
      - Catalog
  /catalog/{api_id}/operations:
    get:
      description: |-
        Preview the operations of a catalog entry's spec (capped, offset-paginated).

        ``tag`` and ``q`` filter the spec's operations server-side before windowing,
        so the UI's search box covers every operation in the spec and pages the
        filtered set via ``offset``/``limit`` ("Load more").
      operationId: previewCatalogOperations
      parameters:
      - in: path
        name: api_id
        required: true
        schema:
          title: Api Id
          type: string
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          title: Offset
          type: integer
      - in: query
        name: limit
        required: false
        schema:
          default: 200
          maximum: 200
          minimum: 0
          title: Limit
          type: integer
      - in: query
        name: tag
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Tag
      - in: query
        name: q
        required: false
        schema:
          anyOf:
          - maxLength: 500
            type: string
          - type: 'null'
          title: Q
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationPreviewListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Preview Catalog Operations
      tags:
      - Catalog
  /catalog/{api_id}:import:
    post:
      description: Enqueue an async import of a catalog entry into the local registry.
      operationId: importCatalogEntry
      parameters:
      - in: path
        name: api_id
        required: true
        schema:
          title: Api Id
          type: string
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiImportResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Import Catalog Entry
      tags:
      - Catalog
  /catalog/{api_id}:snooze:
    post:
      description: |-
        Snooze the outstanding update notification for a catalog entry (C1, #925).

        Operator action (``events:write``): quiet the "update available" badge for a
        known-and-accepted upstream change without adopting it. Body is optional — omit or send
        ``{"snoozed_until": null}`` to mute until a newer upstream digest lands (the primary
        per-API affordance); send an ISO-8601 ``snoozed_until`` for a time-boxed snooze. A
        genuinely newer upstream change re-lights the badge automatically.
      operationId: snoozeCatalogEntry
      parameters:
      - in: path
        name: api_id
        required: true
        schema:
          title: Api Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/CatalogSnoozeRequest'
              - type: 'null'
              title: Body
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '409':
          content:
            application/problem+json:
              example: *id008
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: The catalog entry has no outstanding update to snooze.
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Snooze Catalog Entry
      tags:
      - Catalog
  /catalog/{api_id}:unsnooze:
    post:
      description: Clear a snooze for a catalog entry (C1). Operator action (``events:write``).
      operationId: unsnoozeCatalogEntry
      parameters:
      - in: path
        name: api_id
        required: true
        schema:
          title: Api Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Unsnooze Catalog Entry
      tags:
      - Catalog
  /catalog:refresh:
    post:
      description: Force a refresh of the catalog cache from the upstream manifest (org:admin).
      operationId: refreshCatalog
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogRefreshResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Refresh Catalog
      tags:
      - Catalog
  /control/health:
    get:
      description: |-
        Return service health status for this surface.

        Unauthenticated liveness probe. In combined mode this is served under
        the surface prefix (e.g. ``/control/health``) so surfaces don't collide;
        the canonical platform probe is the root ``GET /health``.
      operationId: controlHealth
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                title: Response Controlhealth
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Control health
      tags:
      - System
  /credentials:
    get:
      description: List credentials with cursor-based pagination.
      operationId: listCredentials
      parameters:
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: vendor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Vendor
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List credentials
      tags:
      - Credentials
    post:
      description: Create a new credential. The secret is returned once and never readable again.
      operationId: createCredential
      requestBody:
        content:
          application/json:
            schema:
              discriminator:
                mapping:
                  api_key: '#/components/schemas/ApiKeyCreateRequest'
                  basic: '#/components/schemas/BasicAuthCreateRequest'
                  bearer_token: '#/components/schemas/BearerTokenCreateRequest'
                  no_auth: '#/components/schemas/NoAuthCreateRequest'
                  oauth2: '#/components/schemas/OAuth2CreateRequest'
                  sigv4: '#/components/schemas/Sigv4CreateRequest'
                propertyName: type
              oneOf:
              - $ref: '#/components/schemas/BearerTokenCreateRequest'
              - $ref: '#/components/schemas/ApiKeyCreateRequest'
              - $ref: '#/components/schemas/BasicAuthCreateRequest'
              - $ref: '#/components/schemas/OAuth2CreateRequest'
              - $ref: '#/components/schemas/NoAuthCreateRequest'
              - $ref: '#/components/schemas/Sigv4CreateRequest'
              title: Body
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialCreateResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Create credential
      tags:
      - Credentials
  /credentials/oauth/callback:
    get:
      description: |-
        Handle the OAuth callback from the IdP.

        This endpoint is intentionally unauthenticated — it receives redirects
        from external IdPs where the user has no session cookie. Security
        binding is provided by the signed, time-limited state JWT which ties
        the callback to a specific credential and caller.

        Redirects the popup the SPA opened to a public SPA route
        (``/app/oauth/connected``) that owns the user-facing "you can close this"
        experience and self-closes. Two variants, distinguished only by a coarse
        ``status`` query param:

          * Success: ``?status=ok``.
          * Failure: ``?status=error`` — no protocol or provider detail is
            exposed in the redirect URL.

        The parent SPA still learns the real outcome by polling
        ``GET /credentials/{id}`` — never from this redirect. The actual cause
        (missing state, connect failure, provider error, etc.) is recorded via
        structured logging for operators.
      operationId: oauthCallback
      parameters:
      - in: query
        name: code
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Code
      - in: query
        name: state
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: State
      - in: query
        name: error
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: OAuth connect callback
      tags:
      - Credentials
  /credentials/providers:
    get:
      description: Return discovery metadata for all configured credential providers.
      operationId: listProviders
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProviderDiscoveryResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List credential providers
      tags:
      - Credentials
  /credentials/{credential_id}:
    delete:
      description: Delete a credential.
      operationId: deleteCredential
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete credential
      tags:
      - Credentials
    get:
      description: Get a single credential with redacted secrets.
      operationId: getCredential
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialRedactedResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get credential
      tags:
      - Credentials
    patch:
      description: Update or rotate a credential.
      operationId: updateCredential
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              discriminator:
                mapping:
                  api_key: '#/components/schemas/ApiKeyUpdateRequest'
                  basic: '#/components/schemas/BasicAuthUpdateRequest'
                  bearer_token: '#/components/schemas/BearerTokenUpdateRequest'
                  oauth2: '#/components/schemas/OAuth2UpdateRequest'
                  sigv4: '#/components/schemas/Sigv4UpdateRequest'
                propertyName: type
              oneOf:
              - $ref: '#/components/schemas/BearerTokenUpdateRequest'
              - $ref: '#/components/schemas/ApiKeyUpdateRequest'
              - $ref: '#/components/schemas/BasicAuthUpdateRequest'
              - $ref: '#/components/schemas/OAuth2UpdateRequest'
              - $ref: '#/components/schemas/Sigv4UpdateRequest'
              title: Body
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialRedactedResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update or rotate credential
      tags:
      - Credentials
  /credentials/{credential_id}/agents:
    get:
      description: |-
        List agents directly bound to a credential with cursor-based pagination.

        The reverse lookup for the credential-detail "Agents" view (theme 5
        phase 1) — the direct-binding mirror of ``GET /toolkits/{id}/agents``.
        Suspended bindings are included with their flag set.
      operationId: listCredentialAgents
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CredentialAgentListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List agents bound to credential
      tags:
      - Credentials
  /credentials/{credential_id}/agents/{agent_id}/permissions:
    get:
      description: List the ordered PBAC rules for a direct `(agent, credential)` binding.
      operationId: listAgentCredentialPermissions
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PermissionRuleListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List binding permission rules
      tags:
      - Credentials
    patch:
      description: Additively add and/or remove permission rules on a binding.
      operationId: patchAgentCredentialPermissions
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PermissionsPatchRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PermissionRuleListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Patch binding permission rules
      tags:
      - Credentials
    put:
      description: Replace the full set of permission rules for a binding (idempotent PUT).
      operationId: replaceAgentCredentialPermissions
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              items:
                $ref: '#/components/schemas/jentic_one__control__web__schemas__permission_rules__PermissionRuleSchema'
              maxItems: 100
              title: Body
              type: array
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PermissionRuleListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Replace binding permission rules
      tags:
      - Credentials
  /credentials/{credential_id}/agents/{agent_id}/permissions:test:
    post:
      description: |-
        Answer "what would the broker do for this request?" without calling upstream.

        Unlike the toolkit `:test` there is **no vendor pooling**: the direct
        binding's rules are one ordered first-match-wins list, so the result is
        exactly this binding's policy. Default-deny when nothing matches.
      operationId: testAgentCredentialPermissions
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PermissionTestRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PermissionTestResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Dry-run permission evaluation
      tags:
      - Credentials
  /credentials/{credential_id}/agents/{agent_id}/rule-set:
    delete:
      description: |-
        Detach the binding's shared rule set — its inline rules apply again.

        Idempotent: detaching a binding already on inline rules is a no-op 204.
      operationId: detachAgentCredentialRuleSet
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Detach rule set from binding
      tags:
      - Credentials
    put:
      description: |-
        Point the binding at a shared rule set (idempotent PUT).

        While attached, the set's ordered list is the binding's effective policy
        and its inline rules are dormant — `permissions:test` evaluates the set.
        The set must exist (404 `rule_set_not_found`).
      operationId: attachAgentCredentialRuleSet
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuleSetAttachRequest'
        required: true
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Attach rule set to binding
      tags:
      - Credentials
  /credentials/{credential_id}/connect:
    post:
      description: Initiate the OAuth connect flow for a credential.
      operationId: connectCredential
      parameters:
      - in: path
        name: credential_id
        required: true
        schema:
          title: Credential Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConnectRequestBody'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectChallengeResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '409':
          content:
            application/problem+json:
              example: *id008
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Credential is not connectable
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Begin OAuth connect flow
      tags:
      - Credentials
  /error:
    get:
      description: Minimal error endpoint for browser-facing authorization failures.
      operationId: errorPage
      parameters:
      - in: query
        name: error
        required: false
        schema:
          default: unknown_error
          title: Error
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                title: Response Errorpage
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Error Page
      tags:
      - OAuth
  /events:
    get:
      description: List events with optional filters.
      operationId: listEvents
      parameters:
      - in: query
        name: event_type
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Event Type
      - in: query
        name: severity
        required: false
        schema:
          anyOf:
          - items:
              $ref: '#/components/schemas/EventSeverity'
            type: array
          - type: 'null'
          title: Severity
      - in: query
        name: requires_action
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Requires Action
      - in: query
        name: acknowledged
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Acknowledged
      - in: query
        name: from
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: From
      - in: query
        name: to
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: To
      - in: query
        name: trace_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Trace Id
      - in: query
        name: actor_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
      - in: query
        name: actor_type
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Type
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 25
          maximum: 100
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Events
      tags:
      - Events
  /events/stream:
    get:
      description: Stream events as Server-Sent Events.
      operationId: streamEvents
      parameters:
      - in: query
        name: since
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Since
      - in: query
        name: event_type
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Event Type
      - in: query
        name: severity
        required: false
        schema:
          anyOf:
          - items:
              $ref: '#/components/schemas/EventSeverity'
            type: array
          - type: 'null'
          title: Severity
      - in: query
        name: requires_action
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Requires Action
      - in: query
        name: trace_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Trace Id
      - in: query
        name: actor_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
      - in: query
        name: actor_type
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Type
      - in: header
        name: Last-Event-ID
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Last-Event-Id
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Stream Events
      tags:
      - Events
  /events/{event_id}:
    get:
      description: Get an event by ID.
      operationId: getEvent
      parameters:
      - in: path
        name: event_id
        required: true
        schema:
          title: Event Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Event
      tags:
      - Events
    patch:
      description: Acknowledge an event.
      operationId: acknowledgeEvent
      parameters:
      - in: path
        name: event_id
        required: true
        schema:
          title: Event Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventAcknowledgeRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Acknowledge Event
      tags:
      - Events
  /executions:
    get:
      description: List execution records with optional filters.
      operationId: listExecutions
      parameters:
      - in: query
        name: toolkit_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Toolkit Id
      - in: query
        name: trace_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Trace Id
      - in: query
        name: status
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Status
      - in: query
        name: from
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: From
      - in: query
        name: to
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: To
      - in: query
        name: api
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Api
      - in: query
        name: actor_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Id
      - in: query
        name: origin
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Origin
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 25
          maximum: 100
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecutionListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Executions
      tags:
      - Executions
  /executions/{execution_id}:
    get:
      description: Get an execution record by ID.
      operationId: getExecution
      parameters:
      - in: path
        name: execution_id
        required: true
        schema:
          title: Execution Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecutionResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Execution
      tags:
      - Executions
  /governed-hosts:
    get:
      description: |-
        The caller's governed host set (credential-bound hosts) with an ETag digest.

        **Always self-scoped** — derived from the authenticated identity's own
        credential bindings; there is no cross-actor or admin variant. Credentials
        bind to agents and service accounts, so agent-scoped tokens (the OAuth
        agent-consent flow's output) and `sak_` keys are the callers this endpoint
        serves — a plain user token yields an empty set. Suspended bindings and
        inactive credentials still contribute their hosts: keep diverting that
        traffic, so the broker can refuse it — dropping it from the list would
        send it direct to the upstream, unbrokered. The ``digest`` covers exactly
        the ``data`` list and is also emitted as a strong ``ETag``, so integrators
        poll with ``If-None-Match: "<digest>"`` and get an empty ``304`` until
        their host set actually changes (the change-poll seam that replaces
        ``GET /apis`` enumeration for interception scoping). Poll at most once per
        minute; on any ``5xx`` retain the last known set — never fall back to an
        empty (intercept-nothing) list.

        Responses are identity-scoped and marked ``Cache-Control: private,
        no-store`` — a shared cache must never serve one actor's host set to
        another.
      operationId: getGovernedHosts
      parameters:
      - description: 'Change-poll precondition: the `ETag` from a previous response (quoted, `"<digest>"`; the bare digest is accepted as a compatibility form). When it still matches, the response is an empty `304`.'
        in: header
        name: if-none-match
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Change-poll precondition: the `ETag` from a previous response (quoted, `"<digest>"`; the bare digest is accepted as a compatibility form). When it still matches, the response is an empty `304`.'
          title: If-None-Match
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GovernedHostsResponse'
          description: Successful Response
          headers:
            ETag:
              description: 'Strong entity tag: the quoted `digest` (`"<hex>"`). Echo it in `If-None-Match` to change-poll.'
              schema:
                type: string
        '304':
          description: The host set still matches the presented `If-None-Match` — empty body, `ETag` echoed.
          headers:
            ETag:
              description: 'Strong entity tag: the quoted `digest` (`"<hex>"`). Echo it in `If-None-Match` to change-poll.'
              schema:
                type: string
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Governed Hosts
      tags:
      - Governed Hosts
  /health:
    get:
      description: |-
        Liveness probe for the combined control-plane app.

        Unauthenticated and dependency-free so orchestrators and load balancers
        have a stable target. Returns ``{"status": "ok"}`` when the process is up.
      operationId: getHealth
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
      security: []
      summary: Health
      tags:
      - System
  /inspect:
    get:
      description: Inspect an operation — resolve to full structural detail.
      operationId: inspectOperation
      parameters:
      - description: METHOD URL identifier
        in: query
        name: id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: METHOD URL identifier
          title: Id
      - description: Operation ID
        in: query
        name: operation_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Operation ID
          title: Operation Id
      - description: Pin to specific revision
        in: query
        name: revision_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Pin to specific revision
          title: Revision Id
      - in: query
        name: detail
        required: false
        schema:
          default: summary
          enum:
          - summary
          - full
          title: Detail
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Inspect operation
      tags:
      - Inspect
  /instance:
    get:
      description: |-
        Return this backend's self-describing identity.

        Unauthenticated and dependency-free so any client (an MCP server, the
        CLI, an agent) can read which backend it is bound to — local vs. a
        remote install — and label its responses accordingly.
      operationId: getInstance
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InstanceIdentityResponse'
          description: Successful Response
      security: []
      summary: Backend identity
      tags:
      - System
  /jobs:
    get:
      description: List jobs with optional filters.
      operationId: listJobs
      parameters:
      - in: query
        name: kind
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Kind
      - in: query
        name: status
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Status
      - in: query
        name: from
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: From
      - in: query
        name: to
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: To
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 25
          maximum: 100
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Jobs
      tags:
      - Jobs
  /jobs/{job_id}:
    get:
      description: Get a job by ID.
      operationId: getJob
      parameters:
      - in: path
        name: job_id
        required: true
        schema:
          title: Job Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Job
      tags:
      - Jobs
  /jobs/{job_id}/result:
    get:
      description: Get the result of a completed job — polymorphic by kind.
      operationId: getJobResult
      parameters:
      - in: path
        name: job_id
        required: true
        schema:
          title: Job Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Job Result
      tags:
      - Jobs
  /jobs/{job_id}:cancel:
    post:
      description: Cancel an active job.
      operationId: cancelJob
      parameters:
      - in: path
        name: job_id
        required: true
        schema:
          title: Job Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Cancel Job
      tags:
      - Jobs
  /login:
    get:
      description: |-
        Render the local-account login form for an in-flight ``/authorize`` request.

        Verifies the ``ls`` signature/TTL/purpose **before** rendering — an
        expired, forged, or wrong-purpose token never gets a form — re-checks the
        D7 client gate (a client denied or deactivated while the user holds the
        ``ls`` must not be asked for a password), rejects an already-spent ``ls``,
        and embeds ``ls`` plus a fresh single-use CSRF nonce bound to it.
      operationId: loginPage
      parameters:
      - description: Signed authorization-flow state (carry-through token)
        in: query
        name: ls
        required: true
        schema:
          description: Signed authorization-flow state (carry-through token)
          title: Ls
          type: string
      responses:
        '200':
          content:
            text/html:
              schema:
                type: string
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '404':
          description: 'Local-account login is unavailable (`auth.local_login.enabled=false`, or an external IdP is configured — `auth.idp.enabled=true` — which always wins): the route answers the framework''s plain route-not-found 404, so the gate state is unobservable.'
        '422':
          content:
            application/problem+json:
              example: *id002
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Local-account login form (authorization flow)
      tags:
      - OAuth
    post:
      description: |-
        Authenticate the local account and rejoin the authorization flow.

        Success never mints a JWT — it flows straight into code issuance (platform
        client) or the consent handle (registered third-party client), exactly
        where the IdP callback rejoins, and burns the single-use ``ls``.
        Credential failures re-render the form with one generic message: lockout
        state, unknown email, and wrong password are indistinguishable (no
        user-enumeration response oracle), while the shared
        ``AuthService.authenticate`` core still increments the failed-login count
        and applies the lockout threshold. An account flagged
        ``must_change_password`` authenticates but is told to rotate via the UI
        first — the OAuth plane must not hand a fully-scoped token to a
        temporary-password principal the UI would have boxed into
        change-password-only.
      operationId: loginSubmit
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Body_loginSubmit'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '404':
          description: 'Local-account login is unavailable (`auth.local_login.enabled=false`, or an external IdP is configured — `auth.idp.enabled=true` — which always wins): the route answers the framework''s plain route-not-found 404, so the gate state is unobservable.'
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Local-account login submit (authorization flow)
      tags:
      - OAuth
  /mcp/config-registrations:
    post:
      description: |-
        Record that an MCP server entry was written for an agent runtime.

        Called by `jentic setup` / `jentic skill init` once per runtime whose MCP
        config entry it wrote (best-effort on the CLI side — a failed report never
        blocks setup). Emits the `mcp.config_registered` internal event; when the
        operator opted into telemetry, the event is forwarded carrying at most the
        closed runtime tag. Reports are throttled per (actor, runtime) within a
        24h in-process window — a throttled repeat is still a 202 with
        `recorded: false`, since the CLI treats the report as fire-and-forget.
        Any authenticated actor may report.
      operationId: reportMcpConfigRegistration
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/McpConfigRegistrationRequest'
        required: true
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/McpConfigRegistrationResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Report MCP config registration
      tags:
      - MCP
  /me:
    get:
      description: Return the caller's identity and context, discriminated by actor type.
      operationId: getMe
      responses:
        '200':
          content:
            application/json:
              schema:
                discriminator:
                  mapping:
                    agent: '#/components/schemas/MeAgent'
                    service_account: '#/components/schemas/MeServiceAccount'
                    user: '#/components/schemas/MeUser'
                  propertyName: type
                oneOf:
                - $ref: '#/components/schemas/MeUser'
                - $ref: '#/components/schemas/MeAgent'
                - $ref: '#/components/schemas/MeServiceAccount'
                title: Response Getme
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Me
      tags:
      - Identity
  /monitoring/executions:
    get:
      description: Return aggregated execution statistics for the dashboard.
      operationId: getExecutionStats
      parameters:
      - in: query
        name: days
        required: false
        schema:
          default: 7
          maximum: 30
          minimum: 1
          title: Days
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExecutionStatsResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Execution Stats
      tags:
      - Monitoring
  /monitoring/usage:
    get:
      description: Return detailed usage statistics for the monitoring overview.
      operationId: getUsageStats
      parameters:
      - in: query
        name: since
        required: false
        schema:
          anyOf:
          - type: integer
          - type: 'null'
          title: Since
      - in: query
        name: until
        required: false
        schema:
          anyOf:
          - type: integer
          - type: 'null'
          title: Until
      - in: query
        name: group_by
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/GroupBy'
          - type: 'null'
          title: Group By
      - in: query
        name: top_limit
        required: false
        schema:
          default: 10
          maximum: 50
          minimum: 1
          title: Top Limit
          type: integer
      - in: query
        name: toolkit_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Toolkit Id
      - in: query
        name: api_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Api Id
      - in: query
        name: agent_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Agent Id
      - in: query
        name: status
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Usage Stats
      tags:
      - Monitoring
  /notes:
    get:
      description: List notes with optional filters and cursor pagination.
      operationId: listNotes
      parameters:
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: api
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Api
      - in: query
        name: operation_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Operation Id
      - in: query
        name: execution_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Execution Id
      - in: query
        name: credential_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Credential Id
      - in: query
        name: type
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Type
      - in: query
        name: created_by
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Notes
      tags:
      - Notes
    post:
      description: Create a new note attached to a registry resource.
      operationId: createNote
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NoteCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Create Note
      tags:
      - Notes
  /notes/{note_id}:
    delete:
      description: Delete a note. Supports optimistic concurrency via If-Match.
      operationId: deleteNote
      parameters:
      - in: path
        name: note_id
        required: true
        schema:
          title: Note Id
          type: string
      - in: header
        name: if-match
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: If-Match
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete Note
      tags:
      - Notes
    get:
      description: Retrieve a single note by ID.
      operationId: getNote
      parameters:
      - in: path
        name: note_id
        required: true
        schema:
          title: Note Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Note
      tags:
      - Notes
    patch:
      description: Update a note (partial). Supports optimistic concurrency via If-Match.
      operationId: updateNote
      parameters:
      - in: path
        name: note_id
        required: true
        schema:
          title: Note Id
          type: string
      - in: header
        name: if-match
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: If-Match
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NoteUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update Note
      tags:
      - Notes
  /oauth-clients:
    post:
      description: |-
        Register a public OAuth client anonymously (RFC 7591 subset).

        Returns 201 with the new ``client_id``, or 200 with the **existing** row's
        ``client_id`` on an exact dedupe match (D8, extended per G13/#1251):
        (``software_id`` + redirect-URI set), falling back to (``client_name`` +
        redirect-URI set) for registrations without a ``software_id`` — so a
        pending client's awaiting-approval retry loop re-attaches instead of
        minting duplicate rows. Re-registering a client an administrator has
        deactivated re-enters the approval queue: the registration re-attaches
        (200) and the client awaits a fresh admin decision (#1312). No
        client_secret is ever issued here and no registration_access_token
        is returned (D12). New rows await admin approval unless the deployment
        auto-approves registrations (D9). The ``server.mcp.oauth.enabled`` gate
        lives on the route class — a disabled door 404s before this handler,
        its body validation, or the rate limiter ever run.
      operationId: registerOauthClientEndpoint
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OAuthClientRegistrationRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthClientRegistrationResponse'
          description: Successful Response
        '400':
          description: 'Invalid client metadata (RFC 7591 §3.2.2): `{"error": "invalid_client_metadata", "error_description": "..."}`.'
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Register OAuth client (anonymous DCR)
      tags:
      - OAuth Clients
  /oauth-grants/{grant_id}:revoke:
    post:
      description: |-
        Revoke a consent→agent grant — one of the three revocation kill radii.

        Allowed for the grant's owner (the consenting user) or an admin. Marks
        the grant ``revoked`` and revokes every outstanding access/refresh token
        minted under it in the same transaction; the live resolvers also re-check
        grant status on every verdict (belt + braces). The client's next token
        use or refresh fails closed. Idempotent on an already-revoked grant.
      operationId: revokeOauthGrant
      parameters:
      - in: path
        name: grant_id
        required: true
        schema:
          title: Grant Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Revoke OAuth grant
      tags:
      - OAuth
  /oauth/approval/decision:
    post:
      description: |-
        Thin wrapper over the admin approval path for the approval-pending page.

        Authorization is byte-identical to ``POST /admin/oauth-clients/{id}:approve``
        / ``:deny`` (``oauth-clients:write``, org:admin implies it) and the
        decision itself is the SAME ``OAuthClientService.approve``/``deny`` calls —
        same audit records, same D7 active/approval_status coupling; this endpoint
        only translates the signed state blob into the client row. CSRF posture
        matches the consent POST: no ambient credential is honored — the browser
        must explicitly present the SPA bearer token, which a cross-site form
        cannot do.
      operationId: approvalDecisionEndpoint
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OAuthApprovalDecisionRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthApprovalStatusResponse'
          description: Successful Response
        '400':
          description: Malformed, tampered, or expired approval-state blob.
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Approve or deny a pending client inline (approval-pending page)
      tags:
      - OAuth
  /oauth/approval/status:
    get:
      description: |-
        Minimal tri-state poll for the approval-pending page.

        Anonymous but keyed by the signed approval-state blob — never a bare
        client_id, so the endpoint cannot be used to enumerate registrations. Any
        verification failure (bad signature, wrong purpose, expired ``iat``,
        malformed blob) is a 400 ``invalid_grant``; the page reacts to a 400 by
        re-running /authorize, which mints a fresh blob. The response carries ONLY
        the tri-state — no names, redirect URIs, or metadata.
      operationId: approvalStatusEndpoint
      parameters:
      - description: Signed approval-state blob minted by /authorize
        in: query
        name: st
        required: true
        schema:
          description: Signed approval-state blob minted by /authorize
          title: St
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthApprovalStatusResponse'
          description: Successful Response
        '400':
          description: Malformed, tampered, or expired approval-state blob.
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Poll client approval status (approval-pending page)
      tags:
      - OAuth
  /oauth/callback:
    get:
      description: External IdP callback — exchanges upstream code and issues platform auth code.
      operationId: authorizeOauthCallback
      parameters:
      - in: query
        name: code
        required: true
        schema:
          title: Code
          type: string
      - in: query
        name: state
        required: true
        schema:
          title: State
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Authorize Oauth Callback
      tags:
      - OAuth
  /oauth/consent:
    get:
      description: Display the OAuth consent screen.
      operationId: consentPage
      parameters:
      - description: Opaque consent-flow handle
        in: query
        name: ch
        required: true
        schema:
          description: Opaque consent-flow handle
          title: Ch
          type: string
      responses:
        '200':
          content:
            text/html:
              schema:
                type: string
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
            text/html:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Consent Page
      tags:
      - OAuth
    post:
      description: |-
        Process the consent form submission. Mints the auth code only on approval.

        ``consent_token`` is the opaque handle emitted by the callback. It never
        leaves the state backend as anything more than an ID — the actual consent
        parameters (user_id, email, scopes, redirect_uri) live server-side and
        can't be tampered with or captured from browser history/proxy logs.

        ``agent_id`` is posted only by the agent-picker variant
        (``consent_model='agent'`` clients); it is validated and the scope math
        recomputed entirely server-side — the browser's selection is never
        trusted.
      operationId: consentSubmit
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Body_consentSubmit'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Consent Submit
      tags:
      - OAuth
  /oauth/consent/agent:
    post:
      description: |-
        Create the consenting user's first agent from the zero-agents consent page (P4).

        The form is rendered only when the consenting user owns zero agents in
        any status (the G12(b) first-run dead-end). This submit verifies the signed
        single-use ``agent-create`` blob (bound to the consent handle AND the
        authenticated subject — no ambient credential is honored, so a cross-site
        form cannot drive it: it would need both the unguessable handle and a
        blob minted for that very handle), re-validates the handle and the D7
        client gate, provisions the user row if deferred provisioning left none
        (an affirmative user action, unlike rendering), re-checks the zero-agents
        predicate (an agent appearing in between skips creation — idempotent),
        creates the agent through the same ``AgentService.create`` path as the
        SPA (owner = the consenting user, default agent scopes, same audit +
        event), and 303-redirects back into ``GET /oauth/consent`` where the new
        agent renders pre-selected.

        Failure arms: expired/tampered/replayed blob and expired handle → the
        consent flow's standard ``invalid_consent`` error redirect; a gated
        client → ``access_denied``; an invalid agent name → the form re-rendered
        with the error inline and a fresh blob.

        Creation posture (security review — the hybrid): the arm is decided
        server-side AFTER the subject is resolved/provisioned, against the same
        effective-permission math as POST /agents' ``agents:write`` gate. A
        permissioned user gets the original behaviour (ACTIVE + 303 re-entry); an
        unpermissioned one gets a PENDING agent (the POST /register posture) and
        the awaiting-approval page. A per-subject ``set_if_absent`` slot claim
        makes N parallel submits (N distinct blobs from N renders) create exactly
        one agent — losers re-enter consent, which renders the picker or the
        awaiting page as appropriate.
      operationId: consentAgentCreate
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Body_consentAgentCreate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Create the consenting user's first agent inline (consent page)
      tags:
      - OAuth
  /oauth/consent/agent/status:
    get:
      description: |-
        Minimal tri-state poll for the pending-agent awaiting page (P4 hybrid).

        Anonymous but keyed by the signed ``agent-status`` blob — never a bare
        agent id, so the endpoint cannot be used to enumerate or probe agents:
        the id it reports on is the one SIGNED into the blob, which only the
        consent flow mints, and only for an agent it verified belongs to the
        handle's subject. The response carries ONLY the tri-state — no name,
        owner, or scopes — and non-terminal lifecycle states (disabled, archived,
        a vanished row) all read as ``pending``, so possession of a blob is not a
        lifecycle oracle either. Any verification failure (bad signature, wrong
        purpose, expired ``iat``, malformed blob) is a 400 ``invalid_grant``; the
        page treats a 400 as terminal ("retry the connection") because the blob
        shares the consent handle's lifetime — a retry re-enters the flow, which
        re-parks on a fresh awaiting page while the agent stays pending. Shares
        the approval-status poll's own per-IP rate bucket (same cadence, same
        caller shape).
      operationId: consentAgentStatus
      parameters:
      - description: Signed agent-status blob minted by the consent flow's pending arm
        in: query
        name: st
        required: true
        schema:
          description: Signed agent-status blob minted by the consent flow's pending arm
          title: St
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsentAgentStatusResponse'
          description: Successful Response
        '400':
          description: Malformed, tampered, or expired agent-status blob.
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Poll pending-agent approval status (consent awaiting page)
      tags:
      - OAuth
  /oauth/introspect:
    post:
      description: Introspect a token (RFC 7662).
      operationId: introspectEndpoint
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IntrospectRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntrospectResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Introspect Endpoint
      tags:
      - OAuth
  /oauth/mint:
    post:
      description: |-
        Mint a short-lived ephemeral token for a task agent.

        The caller must be an authenticated service account. The requested scopes
        must be a subset of the caller's own scopes.
      operationId: mintEndpoint
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MintRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MintResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Mint Endpoint
      tags:
      - OAuth
  /oauth/revoke:
    post:
      description: |-
        Revoke a token (RFC 7009). Always returns 200 for valid requests.

        Two client-authentication arms, negotiated on the request content type by
        ``_RevocationRoute`` (this function body IS the JSON arm — form-encoded
        requests never reach it):

        - **Form-encoded** (`application/x-www-form-urlencoded`, RFC 7009 §2.1 —
          the shape MCP OAuth clients send, G11): `token` + optional
          `token_type_hint` + `client_id`. Public (secret-less) clients
          authenticate by client_id **lineage binding** — the call revokes
          anything only when the token exists and was issued to that `client_id`;
          everything else is a 200 no-op (no token-validity oracle). Revoking an
          access token kills that token only; revoking a **refresh token is a full
          disconnect** — every token of the consent grant AND the grant row itself
          die, so reconnecting requires fresh consent (deliberately beyond the
          RFC 7009 §2.1 SHOULD; one revocation semantics platform-wide). This arm
          is gated by `server.mcp.oauth.enabled` (plain 404 when off), capped at
          64 KiB declared body length, and per-IP rate limited; its errors speak
          the RFC 6749 §5.2 dialect (RFC 7009 §2.2.1), not Problem Details.
        - **JSON** (any other content type — the pre-G11 contract, byte-identical
          including 422 shapes): requires a platform bearer identity; revokes the
          caller's own token (access → that token, refresh → its family). Used by
          `jentic logout`.

        Revocation residual: a revoked **access** token dies on the control-plane
        resolver immediately, but the broker's `CachedTokenValidator` (30 s TTL)
        may honour an already-cached verdict for up to 30 s — the same residual as
        the UI `:revoke` kill switch and the G10 transfer sweep.
      operationId: revokeEndpoint
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RevokeRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RevokeRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          description: 'Form-encoded (RFC 7009) requests only: missing `token` (RFC 6749 §5.2 dialect per RFC 7009 §2.2.1): `{"error": "invalid_request", "error_description": "..."}`.'
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '404':
          description: 'Form-encoded (RFC 7009) requests only: interactive OAuth for MCP is disabled (`server.mcp.oauth.enabled=false`), so the RFC 7009 arm answers the framework''s plain route-not-found 404 (gate state unobservable — same posture as the anonymous DCR door). The bearer-authenticated JSON arm is not gated.'
        '413':
          description: 'Form-encoded (RFC 7009) requests only: declared Content-Length exceeds the 64 KiB raw-body cap (RFC 6749 §5.2 dialect).'
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '429':
          description: 'Form-encoded (RFC 7009) requests only: per-IP rate limit exceeded (`Retry-After` header set; RFC 6749 §5.2 dialect body, `error=slow_down` per RFC 8628 §3.5).'
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Revoke Endpoint
      tags:
      - OAuth
  /oauth/session/continue:
    post:
      description: |-
        Rung 1 of the /authorize identity ladder: reuse the platform session.

        The login page's script posts the pending authorize state (the ``ls``
        carry-through token) with the SPA's bearer token in the Authorization
        header — no cookies, no ambient credentials, so a cross-site form cannot
        drive it (same CSRF posture as the consent POST and the inline approval
        decision). The platform token is validated by the standard auth
        dependency (users only), and the ``active`` / ``must_change_password``
        fences are re-checked with a LIVE user-row read — not the token's baked
        claims — matching rung 3's ``password_rotation_required`` posture, so an
        admin-forced reset fences the exchange immediately even while pre-reset
        SPA tokens are still in flight. The D7 client gate is re-checked, and on
        success the response carries a relative ``/authorize`` resume URL bearing
        a short-TTL, ``session``-purpose continuation blob that pins THIS
        caller's ``user_id`` — the identity is fixed at exchange time, before the
        consent page renders it with its "Not you?" escape.

        Every failure after authentication is the same generic 400: an invalid
        blob must not let the caller learn anything about the client or the flow.
      operationId: sessionContinueEndpoint
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OAuthSessionContinueRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthSessionContinueResponse'
          description: Successful Response
        '400':
          description: Malformed, tampered, expired, or otherwise unusable authorize state — one generic rejection, never a reason.
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          description: 'Local-account login is unavailable (`auth.local_login.enabled=false`, or an external IdP is configured — `auth.idp.enabled=true` — which always wins): the route answers the framework''s plain route-not-found 404, so the gate state is unobservable.'
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Exchange a live platform session for an authorize continuation
      tags:
      - OAuth
  /oauth/token:
    post:
      description: |-
        Exchange a refresh token, JWT assertion, authorization code, or client creds for tokens.

        Error responses speak the RFC 6749 §5.2 dialect (top-level ``error`` +
        ``error_description``), NOT platform Problem Details — reshaped by
        ``_TokenRoute``. Malformed/missing parameters answer ``invalid_request``;
        failed client authentication answers ``invalid_client`` (status 401 with a
        ``WWW-Authenticate: Basic`` challenge when the client attempted HTTP Basic,
        400 otherwise). On the refresh arm, a revoked consent grant answers
        ``invalid_grant`` with ``error_description: "consent grant has been
        revoked"`` — terminal; restart the authorization flow.
      operationId: tokenEndpoint
      requestBody:
        content:
          application/json:
            schema:
              description: Token endpoint request — accepts both JSON and form-encoded (RFC 6749).
              properties:
                assertion:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Assertion
                  x-sensitive: true
                client_id:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Client Id
                client_secret:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Client Secret
                  x-sensitive: true
                code:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Code
                code_verifier:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Code Verifier
                grant_type:
                  title: Grant Type
                  type: string
                redirect_uri:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Redirect Uri
                refresh_token:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Refresh Token
                  x-sensitive: true
              required:
              - grant_type
              title: TokenRequest
              type: object
          application/x-www-form-urlencoded:
            schema:
              description: Token endpoint request — accepts both JSON and form-encoded (RFC 6749).
              properties:
                assertion:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Assertion
                  x-sensitive: true
                client_id:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Client Id
                client_secret:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Client Secret
                  x-sensitive: true
                code:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Code
                code_verifier:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Code Verifier
                grant_type:
                  title: Grant Type
                  type: string
                redirect_uri:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Redirect Uri
                refresh_token:
                  anyOf:
                  - type: string
                  - type: 'null'
                  title: Refresh Token
                  x-sensitive: true
              required:
              - grant_type
              title: TokenRequest
              type: object
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: Successful Response
        '400':
          description: 'RFC 6749 §5.2 error dialect (NOT platform Problem Details — this is a spec-facing endpoint real OAuth/MCP clients parse): `{"error": "invalid_request" | "invalid_grant" | "invalid_client" | "unsupported_grant_type", "error_description": "..."}`. `invalid_request` covers malformed/missing parameters; a revoked consent grant surfaces on the refresh arm as `invalid_grant` with `error_description: "consent grant has been revoked"` — clients should treat it as terminal and restart the authorization flow. A client whose authentication fails after attempting HTTP Basic (`Authorization` header, RFC 6749 §2.3.1) gets the same `invalid_client` dialect body with status 401 and a `WWW-Authenticate: Basic` challenge, per §5.2.'
        '429':
          description: Per-client+IP rate limit exceeded (`Retry-After` header set; RFC 6749 §5.2 dialect body, `error=slow_down` per RFC 8628 §3.5).
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Token Endpoint
      tags:
      - OAuth
  /permission-rule-sets:
    get:
      description: List shared rule sets with per-set rule counts (cursor-paginated).
      operationId: listPermissionRuleSets
      parameters:
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuleSetListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List permission rule sets
      tags:
      - Permission Rule Sets
    post:
      description: |-
        Create a named, shareable ordered rule list (theme 5 rule grouping).

        N agent-credential bindings can point at one set, so `permissions:test`
        and "revoke this operation everywhere" stay single-place edits.
      operationId: createPermissionRuleSet
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuleSetCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuleSetResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '409':
          content:
            application/problem+json:
              example: *id008
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Conflict
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Create permission rule set
      tags:
      - Permission Rule Sets
  /permission-rule-sets/{rule_set_id}:
    delete:
      description: Delete a rule set nothing references (409 `rule_set_in_use` otherwise).
      operationId: deletePermissionRuleSet
      parameters:
      - in: path
        name: rule_set_id
        required: true
        schema:
          title: Rule Set Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '409':
          content:
            application/problem+json:
              example: *id008
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Conflict
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete permission rule set
      tags:
      - Permission Rule Sets
    get:
      description: Get a rule set with its ordered rules and referencing-binding count.
      operationId: getPermissionRuleSet
      parameters:
      - in: path
        name: rule_set_id
        required: true
        schema:
          title: Rule Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuleSetResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get permission rule set
      tags:
      - Permission Rule Sets
    patch:
      description: Rename or re-describe a rule set (creator or org admin).
      operationId: updatePermissionRuleSet
      parameters:
      - in: path
        name: rule_set_id
        required: true
        schema:
          title: Rule Set Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuleSetUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuleSetResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update permission rule set
      tags:
      - Permission Rule Sets
  /permission-rule-sets/{rule_set_id}/rules:
    put:
      description: |-
        Replace the set's full ordered rule list (idempotent PUT).

        Every binding pointing at the set picks the new list up at once — the
        single-place edit rule grouping exists for.
      operationId: replacePermissionRuleSetRules
      parameters:
      - in: path
        name: rule_set_id
        required: true
        schema:
          title: Rule Set Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              items:
                $ref: '#/components/schemas/jentic_one__control__web__schemas__permission_rules__PermissionRuleSchema'
              maxItems: 100
              title: Body
              type: array
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PermissionRuleListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '404':
          content:
            application/problem+json:
              example: *id007
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Not Found
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Replace rule set rules
      tags:
      - Permission Rule Sets
  /permissions:
    get:
      description: List the permission catalogue visible to the caller.
      operationId: listPermissions
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PermissionListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Permissions
      tags:
      - Permissions
  /register:
    post:
      description: Register a new agent client (RFC 7591).
      operationId: registerEndpoint
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Register Endpoint
      tags:
      - Agent Registration
  /register/{agent_id}:
    delete:
      description: Client deletion not supported — returns 403.
      operationId: deleteRegistrationEndpoint
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete Registration Endpoint
      tags:
      - Agent Registration
    get:
      description: Poll registration status (RFC 7592).
      operationId: pollStatusEndpoint
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistrationStatusResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Poll Status Endpoint
      tags:
      - Agent Registration
    put:
      description: Client update not supported — returns 403.
      operationId: updateRegistrationEndpoint
      parameters:
      - in: path
        name: agent_id
        required: true
        schema:
          title: Agent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update Registration Endpoint
      tags:
      - Agent Registration
  /registry/health:
    get:
      description: |-
        Return service health status for this surface.

        Unauthenticated liveness probe. In combined mode this is served under
        the surface prefix (e.g. ``/control/health``) so surfaces don't collide;
        the canonical platform probe is the root ``GET /health``.
      operationId: registryHealth
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                title: Response Registryhealth
                type: object
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Registry health
      tags:
      - System
  /search:
    post:
      description: Lexical (full-text) search over registered API operations.
      operationId: searchOperations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Search operations
      tags:
      - Search
  /service-accounts:
    get:
      description: List service accounts — owner-scoped unless caller is org:admin.
      operationId: listServiceAccounts
      parameters:
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: status
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceAccountListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Service Accounts
      tags:
      - Service Accounts
    post:
      description: Create a new service account.
      operationId: createServiceAccount
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ServiceAccountCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceAccountResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Create Service Account
      tags:
      - Service Accounts
  /service-accounts/{service_account_id}:
    delete:
      description: |-
        Archive a service account — terminal-but-kept.

        The row is retained for history, but the action is not reversible and
        the account's scope grants are revoked. For the reversible kill switch
        use ``:disable`` / ``:enable`` instead.
      operationId: archiveServiceAccount
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Archive Service Account
      tags:
      - Service Accounts
    get:
      description: Get service account by ID — requires service-accounts:read or self-read.
      operationId: getServiceAccount
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceAccountResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Service Account
      tags:
      - Service Accounts
  /service-accounts/{service_account_id}/scopes:
    get:
      description: List scopes granted to a service account.
      operationId: getServiceAccountScopes
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceAccountScopesResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get Service Account Scopes
      tags:
      - Service Accounts
    put:
      description: Replace all scopes for a service account.
      operationId: replaceServiceAccountScopes
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ServiceAccountScopesRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceAccountScopesResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Replace Service Account Scopes
      tags:
      - Service Accounts
  /service-accounts/{service_account_id}:approve:
    post:
      description: Approve a pending service account.
      operationId: approveServiceAccount
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceAccountResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Approve Service Account
      tags:
      - Service Accounts
  /service-accounts/{service_account_id}:deny:
    post:
      description: Deny a pending service account.
      operationId: denyServiceAccount
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/jentic_one__auth__web__schemas__service_accounts__DenyRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceAccountResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Deny Service Account
      tags:
      - Service Accounts
  /service-accounts/{service_account_id}:disable:
    post:
      description: Disable an active service account.
      operationId: disableServiceAccount
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Disable Service Account
      tags:
      - Service Accounts
  /service-accounts/{service_account_id}:enable:
    post:
      description: Enable a disabled service account.
      operationId: enableServiceAccount
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Enable Service Account
      tags:
      - Service Accounts
  /service-accounts/{service_account_id}:generate-api-key:
    post:
      description: Generate a new API key for a service account. Rotates any existing key.
      operationId: generateServiceAccountApiKey
      parameters:
      - in: path
        name: service_account_id
        required: true
        schema:
          title: Service Account Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Generate Service Account Api Key
      tags:
      - Service Accounts
  /system/version:
    get:
      description: |-
        Return the running version and the latest available release.

        Requires an authenticated session (any valid caller; no special
        permission). The SPA reads it from inside the signed-in shell to show the
        current version and, when a newer release is available, an update banner.
        The latest release is resolved best-effort (cached); on any failure it is
        ``null`` and no banner shows.
      operationId: getVersion
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VersionResponse'
          description: Successful Response
      security:
      - BearerAuth: []
      summary: Running and latest-available app version
      tags:
      - System
  /users:
    get:
      description: List all users with cursor-based pagination.
      operationId: listUsers
      parameters:
      - in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      - in: query
        name: invite_state
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/InviteState'
          - type: 'null'
          title: Invite State
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserListResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: List Users
      tags:
      - Users
    post:
      description: Create a new user and issue an invite token.
      operationId: createUser
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserCreatedResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Create User
      tags:
      - Users
  /users/me:
    get:
      description: Return the authenticated user's profile.
      operationId: getCurrentUser
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentUserResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get current user
      tags:
      - Users
  /users/me:change-password:
    post:
      description: |-
        Change the authenticated user's password and return a fresh token.

        A new token is required because the caller's current token still carries the
        stale ``must_change_password`` claim; returning a re-minted token is what
        actually clears the rotation gate client-side.
      operationId: changePassword
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Change own password
      tags:
      - Users
  /users/{user_id}:
    delete:
      description: |-
        Delete a user account (terminal-but-kept).

        The row is retained — the account is anonymized (tombstone email) and
        deactivated so history and audit references stay resolvable — but the
        action is terminal: there is no re-enable arm. For the reversible kill
        switch use ``:disable`` / ``:enable`` instead.
      operationId: deleteUser
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Delete User
      tags:
      - Users
    get:
      description: Get a user by ID.
      operationId: getUser
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Get User
      tags:
      - Users
    patch:
      description: Update a user's profile fields.
      operationId: updateUser
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Update User
      tags:
      - Users
  /users/{user_id}/permissions:
    put:
      description: Set the assigned permissions for a user.
      operationId: setUserPermissions
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetPermissionsRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Set User Permissions
      tags:
      - Users
  /users/{user_id}:disable:
    post:
      description: Disable a user account.
      operationId: disableUser
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Disable User
      tags:
      - Users
  /users/{user_id}:enable:
    post:
      description: Enable a user account.
      operationId: enableUser
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      responses:
        '204':
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Enable User
      tags:
      - Users
  /users/{user_id}:reissue-invite:
    post:
      description: Reissue an invite token for a user.
      operationId: reissueInvite
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InviteIssuedResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '401':
          content:
            application/problem+json:
              example: *id005
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unauthorized
        '403':
          content:
            application/problem+json:
              example: *id006
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Forbidden
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security:
      - BearerAuth: []
      summary: Reissue Invite
      tags:
      - Users
  /users:create-admin:
    post:
      description: |-
        First-run setup: create the first admin user and auto-login.

        Unauthenticated by design — there is no admin to authenticate as yet. The
        operation self-closes once any user exists (returns 410 ``setup_already_complete``
        thereafter), so it is safe to expose only during first boot.
      operationId: createAdmin
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAdminRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '410':
          content:
            application/problem+json:
              example:
                detail: The resource is no longer available and the operation has self-closed.
                instance: /example/path
                status: 410
                title: Gone
                type: gone
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Setup already complete — the first admin exists and this endpoint is closed.
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Create first admin (one-time setup)
      tags:
      - Users
  /users:redeem-invite:
    post:
      description: Redeem an invite token, set password, and return a JWT.
      operationId: redeemInvite
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RedeemInviteRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
          description: Successful Response
        '400':
          content:
            application/problem+json:
              example: *id001
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Bad Request
        '422':
          content:
            application/problem+json:
              example: *id002
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Unprocessable Entity
        '500':
          content:
            application/problem+json:
              example: *id003
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Internal Server Error
        '503':
          content:
            application/problem+json:
              example: *id004
              schema:
                $ref: '#/components/schemas/ProblemDetail'
          description: Service Unavailable
      security: []
      summary: Redeem invite
      tags:
      - Users
security:
- BearerAuth: []
servers:
- description: Same-origin (relative)
  url: /
tags:
- description: Part of the **Core / Access** bounded context — the authoritative record of "who can call what, with which secret". A `Credential` is the auth secret + header configuration the Broker injects on the way out to an upstream API. Credentials reference registry content via the loose `(vendor, name, version)` identity tuple (see `APIReference`) — there is no foreign key between Core and Registry, by design. Cleartext secret material is returned **exactly once** at creation (and on rotation via `PATCH`). Read paths return a redacted projection — last-N characters, hints, never the secret itself.
  name: Credentials
- description: Part of the **Core / Access** bounded context — a `PermissionRuleSet` is a named, shareable ordered rule list (first-match-wins, default-deny) that many direct agent↔credential bindings can point at via their `rule_set_id`, so a policy edit or `permissions:test` stays a single-place operation. A binding with no `rule_set_id` uses its own inline rules instead. Rule sets carry policy, not secrets; deleting one that bindings still reference is refused (409 `rule_set_in_use`).
  name: Permission Rule Sets
- description: |-
    Actor-agnostic, multi-item access-request surface (Core / Access bounded context). When an agent's brokered call is rejected by a permission rule — or the agent needs access it doesn't yet have — the agent files an `AccessRequest` containing one or more line items (`AccessRequestItem`). The request enters `pending` state and surfaces an `approve_url` that the agent presents to a human reviewer.

    Reviewers `:decide` individual items (approve or deny each); filers can `:amend` pending items (adjust rules or target) or `:withdraw` the entire request. Each item transitions independently; the envelope status reflects the aggregate (`pending` while any item is pending, `partially_approved` when some items are decided but others remain, terminal once all items resolve).

    Pending requests carry a TTL (default 7 days, configurable). Envelope lifecycle: `pending → partially_approved → approved | denied | withdrawn | expired`.

    **Identity scoping.** List and get operations are identity-scoped: filers see their own requests, reviewers see their inbox, `org:admin` sees all. Non-owners receive `404` (not `403`) to avoid leaking existence.

    **Security.** Mutating operations (`POST`, `:decide`, `:amend`, `:withdraw`) require `agents:write`; read operations require `agents:read`.
  name: Access Requests
- description: |-
    The `Api` aggregate root — what APIs are registered for this deployment, addressable by the loose `(vendor, name, version)` tuple. Every successful import lands a new immutable `ApiRevision`; the `Api` carries the live `current_revision_id` plus presentation metadata (`display_name`, `description`, `icon_url`).

    Mutation surfaces:

    - `POST /apis` — async import (url / inline). Returns 202 + `Job (kind=import)`.
    - `PATCH /apis/{vendor}/{name}/{version}` — metadata-only updates (display name, description, icon). Cannot mutate revisions or spec content.
    - `POST /apis/{vendor}/{name}/{version}/revisions/{revision_id}:promote` — make a draft revision live.
    - `POST /apis/{vendor}/{name}/{version}/revisions/{revision_id}:archive` — retire a draft revision.
    - `DELETE /apis/{vendor}/{name}/{version}` — deregister the API and all its revisions.

    Newly imported revisions are **never auto-promoted** — callers must explicitly `:promote` to flip the live pointer. Hot-path Broker calls can pin to a specific revision via the `Jentic-Revision` request header for testing without flipping the pointer.

    Sub-resources of an `Api` live under sibling tags: `API Operations` enumerates the operations exposed by a registered revision; `API Spec` returns the underlying OpenAPI document for tooling.
  name: APIs
- description: |-
    The caller's own governed host set — the upstream hosts reachable through its credential bindings (admin bindings → credential scopes, suspended or inactive included → registered APIs), deduplicated and sorted. **Always self-scoped**: the set is derived for the authenticated identity; there is no cross-actor or admin variant.

    The response carries a content-derived SHA-256 `digest` (also emitted as a strong `ETag`), so integrators — e.g. a native gate scoping traffic interception per agent — poll with `If-None-Match: "<digest>"` and receive an empty `304` until the host set actually changes. Deliberately unpaginated: the set is bounded by the caller's own bindings and the digest covers it atomically.
  name: Governed Hosts
- description: |-
    Sub-resource of **APIs** (Registry bounded context). Enumerate the operations exposed by a registered API — either the live (`current_revision_id`) view or a specific revision. Complements `Search` (ranked discovery) and `Inspect` (single-operation detail) by giving callers the deterministic list of every registered operation under one API without ranking, paging through `Search`, or parsing the spec.

    Each row is slim — the structural detail (parameters, response schema, security scheme) lives behind `_links.inspect` for callers that need it.
  name: API Operations
- description: |-
    Sub-resource of **APIs** (Registry bounded context). Download the underlying OpenAPI document for a registered API — either the live (`current_revision_id`) view or a specific revision. Supports `application/json`, `application/openapi+yaml`, and `application/yaml` via `Accept`-header content negotiation, with `Content-Disposition: attachment; filename="..."` so browser downloads work cleanly.

    By default the response includes any deployment-local overlays merged on top of the imported base. Pass `?overlays=false` to download the raw imported spec instead.
  name: API Spec
- description: |-
    The public API **catalog** — a registry-side browse/preview/import surface over the Jentic public-APIs GitHub manifest. Powers the Discover experience: list importable APIs (`GET /catalog`, keyset cursor-paginated), preview an entry's operations (`GET /catalog/{api_id}/operations`), refresh the cached manifest (`POST /catalog:refresh`, `org:admin`), and import an entry into the local registry (`POST /catalog/{api_id}:import`).

    The catalog is a cache of an upstream document, distinct from the local registry (`GET /apis`): `/apis` is what this deployment has imported, `/catalog` is what it *could* import. Each entry's `registered` flag reflects whether its `spec_url` already backs a local API revision. Import resolves to a plain fetchable spec URL and reuses the standard async import job — no catalog identity ever crosses into the importer.
  name: Catalog
- description: Lexical (full-text) search across registered operations, ranked by relevance. Returns a slim row per operation; each row carries `_links.inspect` pointing into the `Inspect` tag for full structural detail.
  name: Search
- description: Resolve a single registered operation to its full structural detail — parameters, response schema, security scheme, server URL. Heavier than `Lookup` (which is the Broker hot path) and separate from `Search` (which only returns ranked summaries). Supports JSON, Markdown, and OpenAPI YAML response formats via `Accept`-header content negotiation.
  name: Inspect
- description: The Broker's read interface into Registry — fast, narrow lookup from a real-world URL to a Jentic `operation_id`. Hot path; not for human use. The capability map names this as the Execution → Registry read interface.
  name: Lookup
- description: |-
    Free-form annotations attached to registry resources — an `Api`, an `ApiRevision`, a registered `operation_id`, an `ExecutionRecord`, or a `Credential`. Used to capture human or agent observations about quirks: *"this endpoint silently truncates email addresses to 64 chars"*, *"this credential rejects calls outside US business hours"*, *"this operation needs `Accept: application/json` even though the spec doesn't say so"*.

    Notes are an information surface for agents — they accumulate over time as the platform learns from real executions and surface back through `_links.notes` on the resources they annotate (woven in by individual surfaces as they need it).

    **Edit model.** Notes are editable via `PATCH`; each successful edit bumps a monotonic `revision` counter on the `Note`. Use the `If-Match` header on `PATCH` and `DELETE` to guard against concurrent modification.

    **Confidence promotion.** Notes carry both a `confidence` value (`observed | suspected | verified`) and a `confidence_source` (`client | platform_promoted`). Today only `client` is written by callers; the `platform_promoted` value is reserved for a future surface that auto-promotes `observed` notes to `verified` once enough matching `execution_feedback` accumulates.

    **Authentication.** Any authenticated principal (agent or human) can post any note type. Per-type policy (e.g. only humans may file `correction`) is enforced platform-side and not modelled in the spec.
  name: Notes
- description: |-
    OpenAPI Overlay 1.0 (`https://spec.openapis.org/overlay/v1.0.0`) documents that correct an imported spec without rewriting it. Overlays capture wrong required fields, missing security schemes, undocumented headers, silent payload truncation, and other places where the upstream spec disagrees with the upstream API's actual behaviour — without forking the spec or waiting for an upstream fix.

    Overlays attach to the `Api` aggregate (not to a specific `ApiRevision`) and continue to apply across revision bumps until materially superseded. The optional `target_revision_id` records the revision the overlay was authored against, for auditing.

    **Lifecycle.** New overlays start in `pending`. The Broker calls `POST /apis/{vendor}/{name}/{version}/overlays/{overlay_id}:confirm` after the first successful upstream execution that exercised an overlay-patched operation, transitioning the overlay to `confirmed`. This is the platform's only documented data-plane → control-plane writeback. Once `confirmed`, an overlay stays `confirmed` — repeated `:confirm` calls return `200` with the existing record unchanged.

    **Edit model.** Overlay documents are editable via `PATCH` only while `pending`. Once `confirmed` (or `deprecated`), the document is immutable — to change a confirmed overlay, submit a new one and let the old one age out via `DELETE` (which soft-deprecates).

    **Deletion.** `DELETE` is a **soft-deprecation** — the row is preserved with `status: deprecated` for audit. Deprecated overlays no longer apply to spec-download merges (`?overlays=false` becomes the default for that overlay) but stay queryable via `GET`. There is no hard-delete in this surface.

    **Authentication.** Any authenticated principal (agent or human) can submit, edit, confirm, or deprecate overlays. Platform-side rate limits gate abuse.
  name: Overlays
- description: Append-only audit log of every Broker call (sync or async) — timing, status, trace IDs, and the upstream API reference (`vendor:name:version`). Bodies are not stored. Records are written by the Broker; this surface is read-only. To walk the history of a single logical request that fanned out into multiple upstream calls, list with `?trace_id={trace_id}`.
  name: Executions
- description: In-flight async-job tracker for both Registry import jobs and Broker async-execution jobs — one uniform `Job` shape keyed by `kind`. Lifecycle only — type-specific result payloads (the imported revisions for `import`, upstream response body for `execution`) live under `/jobs/{job_id}/result`. Result availability follows the organisation-level retention policy (one-shot or TTL); once that expires, both `/jobs/{job_id}` and `/jobs/{job_id}/result` `404`.
  name: Jobs
- description: Curated, severity-tagged event stream surfaced to dashboards and operators. Each event is either **informational** (no action required, e.g. `import.completed`) or **actionable** (requires operator follow-up, e.g. `credential.expiring_soon`, `execution.repeated_failure`). Events reference the underlying `ExecutionRecord` or `Job` via `_links` and share `trace_id` for correlation. Subscribe live via `GET /events/stream` (Server-Sent Events) or poll `GET /events` with a `since=` filter.
  name: Events
- description: |-
    Part of the **Admin / Audit** bounded context — the human roster of the organisation. One organisation, many users; admin surfaces here cover create / list / read / update / delete plus the AIP-style action verbs (`:disable`, `:enable`, `:reissue-invite`). Self-service is intentionally minimal: `GET /users/me` to introspect, `POST /users/me:change-password` to rotate (which re-mints and returns a fresh token, clearing any `must_change_password` gate so the caller need not re-login).

    **First-run setup (no default credentials).** The platform ships with an empty users table — there is no seeded `admin@local` account. On a fresh install `GET /health` reports `setup_required: true` (with `next_step: create_admin`). The operator creates the first administrator via `POST /users:create-admin {email, password}`: a one-time, unauthenticated endpoint that succeeds only while no user exists, grants `org:admin`, and returns an auto-login `LoginResponse` with `must_change_password: false` (the operator chose the password, so there is no forced rotation). It **self-closes** — once any user exists it returns `410 setup_already_complete`, so it is safe to leave exposed during first boot; a single-row setup sentinel is the concurrency backstop against racing callers (even ones using different emails). After the first user exists, `setup_required` flips to `false`.

    **Onboarding flow.** Admins create new users via `POST /users` (no password supplied). The response carries a one-time `inv_…` invite token shown **once**; the admin hands it off out-of-band (Slack, 1Password, email — there is no platform-side email infrastructure in this revision). The new user redeems via `POST /users:redeem-invite {invite_token, password}` and is auto-logged-in via the returned `LoginResponse`. Tokens default to a 7-day TTL and are single-use; `:reissue-invite` issues a fresh one if the original is lost or expired.

    **Authentication.** Today only local (username + password) identity ships. The `User` schema carries `auth_provider: local | external` and an `external_subject_id` field reserved for future SSO integrations (Okta, Cognito, Azure AD); when SSO arrives, those users will be created via a separate provider-specific router. The on-the-wire token contract — `Authorization: Bearer <jwt>` — is unchanged either way.

    **Disabling and revocation.** `:disable` flips `active=false` and rejects subsequent `POST /auth/login` **and** `POST /auth/refresh`. **Existing JWTs keep working until they expire** (≤ 1 hour with the platform's default TTL). This is a deliberate trade-off; sub-minute revocation would require a server-side session table.

    **Session lifetime.** Login JWTs carry a 1-hour TTL. The UI keeps an active session alive by calling `POST /auth/refresh` before expiry (sliding session); refresh re-reads permissions and the `must_change_password` gate from the database and is refused with 401 `session_expired` once the original authentication is older than the absolute window (`admin.auth.session_ttl_seconds`, 12 hours by default), forcing a fresh login.
  name: Users
- description: |-
    Part of the **Admin / Audit** bounded context — the catalogue of grantable permission strings. Permissions are namespaced `resource:action` strings (`users:write`, `capabilities:execute`, `credentials:read`, …) gating every other surface in the API. The set is platform-defined and small; `GET /permissions` returns it in full, with each entry describing what it does, what it implies (the static implication map; e.g. `capabilities:execute` implies `apis:read` and `executions:read`), and whether the calling user is authorised to grant it.

    One reserved superpower short-circuits individual checks in code: `org:admin` (full deployment-wide access, granted via direct DB action). It is not enumerated to non-holders by `GET /permissions`, and is rejected by `PUT /users/{user_id}/permissions` from any caller who doesn't already hold it.

    The same vocabulary is used for `User.permissions` — coarse JWT-embedded scopes — so the catalogue below covers user assignment. The per-binding fine-grained `PermissionRule[]` (the inner PBAC tier) lives separately on the direct agent↔credential bindings under the `Credentials` tag.
  name: Permissions
- description: Operational endpoints for service health and platform tooling. Not part of any bounded context — surfaced here so orchestrators and load balancers have a stable probe target.
  name: System
- description: Identity introspection for the calling principal (human, agent, or service account) — `GET /me` returns the resolved subject, scopes, and permissions behind the presented token.
  name: Identity
- description: Agent actors — autonomous principals that call the Broker. Covers the agent lifecycle (list, read, approve / deny, enable / disable, archive) and the credentials directly bound to each agent.
  name: Agents
- description: Machine principals for non-interactive integrations — lifecycle (create, list, read, approve / deny, enable / disable, archive) mirroring the agent surface.
  name: Service Accounts
- description: OAuth 2.0 / OIDC endpoints exposed by the platform authorization server — the authorize, token, introspection, revocation, and assertion-mint endpoints plus the redirect callback.
  name: OAuth
- description: Dynamic agent registration (RFC 7591-style) — register a new agent client, poll registration status, update, or delete a registration.
  name: Agent Registration
- description: Unauthenticated metadata discovery — the JWKS document and OAuth authorization server metadata under `/.well-known/*`.
  name: Discovery
- description: Unified actor directory — a lightweight read-only view across all actor types (users, agents, service accounts). Returns ID-to-name mappings for UI cache hydration so dashboards can display friendly names wherever an `actor_id` appears.
  name: Actors
- description: Append-only administrative audit trail — who did what, when. Distinct from `Executions` (brokered upstream calls); this records control-plane mutations.
  name: Audit
- description: Dashboard-oriented aggregation endpoints — execution volume, success/failure ratios, and top operations over a bounded time window. Results are cached in-process (TTL ~120 s) so concurrent dashboard viewers never stampede the database.
  name: Monitoring
- description: Runtime, DB-backed platform configuration. Lets an operator set, read, and list configuration that previously required hand-editing backend YAML and restarting the server — starting with credential provider configs (e.g. Pipedream). A successful write rebuilds the in-process provider registry so the change takes effect without a restart. Secret fields (e.g. `client_secret`) are encrypted at rest and redacted on read. **Topology note:** in the combined (single-process) deployment a write takes effect immediately for all surfaces. In a multi-process deployment the control/broker processes pick up the new config at their next boot, not the instant the admin write lands; cross-process propagation is a tracked follow-up.
  name: Configuration
- description: Admin-managed registry of third-party OAuth clients (confidential, secret-bearing). Registered clients integrate with Jentic One via the standard Authorization Code + PKCE flow. Admins can create, list, update, rotate secrets, and disable clients. Disabling a client immediately invalidates all tokens issued through it.
  name: OAuth Clients
- description: MCP (Model Context Protocol) transport reporting. The `jentic mcp` stdio server terminates all MCP protocol traffic locally; the control plane only sees plain HTTP. This tag covers the small reporting surface behind it — today, the config-registration report `jentic setup`/`jentic skill init` send after writing an MCP server entry for a detected agent runtime, which feeds the config-written → first-session → first-execute adoption funnel.
  name: MCP
x-tagGroups:
- name: Core / Access
  tags:
  - Credentials
  - Permission Rule Sets
  - Access Requests
- name: Registry
  tags:
  - APIs
  - API Operations
  - API Spec
  - Catalog
  - Search
  - Inspect
  - Lookup
  - Notes
  - Overlays
- name: Admin / Audit
  tags:
  - Users
  - Actors
  - Permissions
  - Executions
  - Jobs
  - Events
  - Audit
  - Monitoring
  - Configuration
  - OAuth Clients
- name: Platform Actors
  tags:
  - Identity
  - Agents
  - Service Accounts
  - OAuth
  - Agent Registration
  - Discovery
- name: Operations
  tags:
  - System
  - MCP
