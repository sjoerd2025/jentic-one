apiVersion: v2
name: admin
type: application
version: 0.40.0 # x-release-please-version
# common.image falls back to .Chart.AppVersion when no tag is set anywhere, and
# that resolves against THIS file rather than the umbrella's — so the value has
# to be repeated per subchart for a tag-less install to get a real version.
# release-please keeps it in step via the marker.
appVersion: "0.40.0" # x-release-please-version
dependencies:
  - name: common
    version: ">=0.0.0"
    repository: file://../common
